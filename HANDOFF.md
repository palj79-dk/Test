# Fallen Grid — Specification & Handoff

Single-file HTML5 tower defense, post-apocalyptic sci-fi theme, portrait mobile-first, intended to ship on Google Play via a Capacitor WebView wrapper. This document is the source of truth for continuing the work. Part 0 is the operating contract. Part 1 is the technical spec. Part 2 is the continuation brief: quality assessment, backlog, changelog. Part 3 is the **Visual & Map Direction V2** — the re-evaluation of graphics and map layout. Part 4 is the real-3D architecture. Parts 5 and 6 are the V4/V5 retention and balance lines. **The changelog runs to the bottom of the file; the newest entry is always last.**

> **HOW TO READ THIS FILE.** Parts 1-6 were written at the version named in each section and are kept as
> written, so the changelog stays honest about what was believed when. Where a later iteration changed
> something a Part describes, the Part carries an inline note pointing at the version that changed it.
> **When a Part and a changelog entry disagree, the changelog entry wins.** For anything you are about
> to touch, read the newest changelog entries first, then grep the current build.

**Current deliverable: `fallengrid-v6.44.html`.** Everything lives in one file inside a single IIFE. No build step, no CDN: three.js r147 is embedded inline. WebGL via three.js for the world + Canvas2D for UI + WebAudio. Frozen predecessors kept alongside for changelog reference: `fallengrid-v3.html` (V3.6, first real-3D build), `fallengrid-v2.5.html` (last 2D build), `fallengrid.html` (V2.4). **The filename version must match the version label inside the file** — the APK workflow selects by version sort and fails the build if they disagree.

**Current state (V6.44).** Content is complete and the presentation front is closed: 25 sectors in four geometry tiers, 7 towers with a/b specialisations and L4 tiers, 13 enemies with tactical traits, Commander + Airstrike, campaign with mastery medals, Daily Op with rule-changing modifiers, Challenge Lab, Endless, achievements, Codex, per-run telemetry, and a 4-branch 32-node Armory research tree (25,380 Alloy end to end). Both orientations are supported on phone and tablet with aspect-derived layout. The V6.36-V6.38 line rebuilt the in-mission UI around player feedback: a permanent HERO/STRIKE/CORE command strip, a slide-in build panel, and a guided onboarding tour that walks the whole interface.

**Open fronts.** (1) Balance still rests on one tester plus a headless sim — see `docs/ANALYSIS-prerelease.md`. (2) The pre-release checklist in that document is unstarted: dev options are still exposed and `DEV_BUILD` is still on. (3) Monetization is specced but deferred to a V7 line — see `docs/monetization/`.

---

# Part 0 — Operating Contract (read first)

Operate as a senior game-dev collaborator, not a generic assistant. Rules:

Lead with the answer or the working code, then reasoning if needed. Skip preambles, filler affirmations, and needless caveats. Prose over bullets unless the content is naturally list-shaped. No em dashes. No hedging unless uncertainty is genuine. Avoid passive voice.

If a request is ambiguous, make a reasonable assumption, state it in one line, and proceed. Do not stack clarifying questions. If a request has a better framing, say so once, then execute the better version.

All code, config, and data must be copy-paste ready with no placeholders or TODO stubs unless a template was explicitly requested. Flag genuine technical flaws, regressions, or Play-compliance risks directly rather than softening them.

Definition of done is fixed in 2.7 and is non-negotiable: syntax check, hex scan, headless logic/render check, and the two wave-progression guards in 1.10 intact.

---

# Part 1 — Technical Specification

## 1.1 Product intent

Twenty-wave tower defense. The player defends a reactor (Core) against waves of raiders and war-machines by building and upgrading towers on a fixed serpentine path. Run currency is Scrap. A persistent meta-currency, Alloy, is earned each run and spent on permanent talents. The game runs at 60fps on mid-range phones, responds to touch (tap to build/select, pinch to zoom, drag to pan), and never soft-locks between waves.

## 1.2 Coordinate systems (critical concept)

The engine separates two spaces. Get this wrong and everything else breaks.

**Screen space** is a logical canvas. The HUD (top) and tray (bottom/side) are drawn here and never move. The canvas is scaled to the device by a uniform factor in `resize()`, with `devicePixelRatio` applied through `ctx.setTransform`.

> **Updated V6.30 (C1) / V6.33 / V6.34.** This was a fixed **360x640**. It is now **orientation-aware and aspect-derived**: `W, H, HUD_H, TRAY_H, PLAY_TOP, PLAY_BOTTOM, PLAY_H, LANDSCAPE, TRAY_X, TRAY_W` are live `let` bindings recomputed by `layout()` from `resize()`, so all 228 references pick up new values with no call-site changes. **Portrait** holds `W = 360` and derives `H = clamp(round(360 · vh/vw), 600, 900)`, giving the extra height to the play area. **Landscape** holds `H = 360` and derives `W = clamp(round(360 · vw/vh), 560, 1120)`, with the tray becoming a 188px right-hand side panel (`TRAY_X`, `TRAY_W`) instead of a bottom band. Read the viewport from `visualViewport` where available — `innerWidth/innerHeight` lag while the mobile URL bar animates. Two helpers are the single source of truth and should be used instead of raw constants: **`playW()`** (`LANDSCAPE ? TRAY_X : W`) for anything anchored to the right edge of the *play viewport*, and **`trayRect()`** for the tray rectangle in either orientation.

**World space** is the map, independent of the screen. World origin `(0,0)` is the map's top-left corner. The map spans `WORLD_W x WORLD_H` = `704 x 1024`. All gameplay entities live in world coordinates.

A camera projects world into the on-screen play viewport: the screen rectangle from `y = PLAY_TOP` to `y = PLAY_BOTTOM`, spanning `playW()` horizontally (full width in portrait, up to the side panel in landscape). All four are live bindings — see the V6.30/V6.33/V6.34 note above.

```
cam = { x, y, zoom }   // x,y = world coord at viewport top-left; zoom = screen px per world unit
screenToWorld(sx, sy) = { x: sx/zoom + cam.x,  y: (sy - PLAY_TOP)/zoom + cam.y }
worldToScreen(wx, wy) = { x: (wx - cam.x)*zoom, y: (wy - cam.y)*zoom + PLAY_TOP }
```

World rendering each frame: clip to the viewport rect, `translate(0, PLAY_TOP)`, `scale(zoom, zoom)`, `translate(-cam.x, -cam.y)`, draw world, then `restore()`. HUD, tray, ability button, zoom buttons, and tutorial marks draw afterward in raw screen space.

Zoom limits: `MINZOOM = min(W/WORLD_W, PLAY_H/WORLD_H) * 0.98`, `MAXZOOM = 1.9`. `fitCamera()`, `clampCam()`, `zoomAt(sx, sy, factor)` as before.

## 1.3 Layout constants

```
TILE = 64                       (COLS/ROWS/WORLD_W/WORLD_H are per-map, see 1.5)
portrait   W = 360   H = clamp(round(360 * vh/vw), 600, 900)
           HUD_H = 56   TRAY_H = 118   TRAY_W = 0     TRAY_X = W
           PLAY_TOP = HUD_H   PLAY_BOTTOM = H - TRAY_H
landscape  H = 360   W = clamp(round(360 * vw/vh), 560, 1120)
           HUD_H = 46   TRAY_H = 0     TRAY_W = 188   TRAY_X = W - TRAY_W
           PLAY_TOP = HUD_H   PLAY_BOTTOM = H
```

> **Updated V6.30 (C1) / V6.33 / V6.34.** These were fixed constants (`W = 360, H = 640, PLAY_BOTTOM = 522, PLAY_H = 466`, and the map was a fixed `11x16` grid). They are now live `let` bindings recomputed by `layout()`, and the grid is per-map. Do not hardcode any of them, in code **or in tests** — assert the *relationship* (the tray sits at `PLAY_BOTTOM`, the panel stays inside `PLAY_TOP..PLAY_BOTTOM`) rather than a number, which is what let the whole suite survive two geometry changes unedited.

## 1.4 Terrain (built as 3D geometry per map)

`G3D.buildMap()` emits the whole world into **three merged buffers** — `land` (Lambert, vertex
colours + grunge colour map + normal map), `metal` (Standard, PBR) and `glow` (Basic) — so the entire
map is **three draw calls** regardless of size. Tiles are boxes: top face at `topAt(c,r)`, side faces
only where a neighbour is lower.

**Ground colour is sampled at tile *corners*, not centres** (`gcol`/`rcol`, memoised). Two octaves of
`vnoise()` — a smooth value-noise field on the tile lattice — give broad regions plus mid-scale
variation, with a little per-corner grain. Because neighbouring tiles read the same lattice point,
the shared edge has no discontinuity. Slab heights (`slabH`, still 0 / 1.1 / 2.2) come from the same
field, so the broken-slab steps form plateaus.

> **This replaced a per-tile flat fill in V6.41.** Every ground tile used to pick one of two biome
> ashes by a per-tile hash, plus a 2×2-tile `patch` multiplier — which is exactly what read as a
> checkerboard. The suite now asserts the seam delta at shared corners is **0**.

The 2D canvas terrain bake (`buildTerrain`, ~295 lines) was **deleted in V6.41**; it had had no
callers since V3 and its comments actively contradicted the live renderer.

**Relief.** Ground sits on three terraces, `slabH` ∈ {0, STEP, 2·STEP} with **`STEP = 7`** (V6.43).
It used to be 1.1 — 2.2 units of relief on a 64-unit tile, i.e. **3.4%**, invisible at play zoom, so
the ground read as a flat plane however much the colours varied. At 14 units (**22%** of a tile, and
comparable to the 10-unit road trench) the plateaus read as terraces with shadowed cliff faces.
Everything that stands on the ground must therefore anchor to `topAt(c,r)`: all seven prop kinds, the
decals, the pebbles (`tileH`), the towers (`tileH`) and off-path enemies (`enemyGroundY`). A prop left
at `y = 0` floats or sinks.

**Horizon** (V6.43): a dark plain at the base of the cliffs plus two rings of stepped mesas, seeded
per map. Built as a **frame, not a slab** — the middle would be hidden under the island but still
shaded and overdrawn, and a full background quad cost +12.7% frame time under SwiftShader. Distances
are measured outward from the island *edge*, and everything stays inside the fog range (800-2600) or
it is fogged to the sky colour and may as well not exist.

**Props** (`buildDecor`) are placed from a *clustered* density field (V6.42): 4-12 seeded centres per
map, each with a theme, and the field is **normalised** so the expected count hits `DECOR_RATE`
however the clusters land. `DECOR` is read **only** by the 3D build loop — never by `buildable()`,
pathing or combat — so props are purely decorative and their count is a draw-cost question only.

**Blocked tiles** (V6.9 obstacles) are **pooled 3D geometry** since V6.42: a dark pad plus five
rubble chunks per plot, pushed into the scene by `G3D.setObstacles()` whenever `BLOCKED_SET` changes
(`refreshObstacles()`). They used to be painted by `drawObstacles()` on the 2D UI canvas, which sits
*above* the GL canvas — so the marker drew on top of every enemy walking past behind it.
## 1.5 Maps, grid size, and routes

Maps vary in **grid size** and can have **multiple entrances**. `COLS/ROWS/WORLD_W/WORLD_H/MINZOOM` are `let`, set per map by `loadMap(i)` from `MAPS[i].cols/rows`; the baked terrain canvas sizes to the map. Large maps exceed the viewport at readable zoom and are panned (the camera already supports this; `fitCamera` fits width, top-anchored).

Each map defines `routes: [[...corners], ...]` — one or more routes, each a `[col,row]` corner list (consecutive corners share a row/column). **All routes end at the shared base** (the last point); their starts are the entrances. `loadMap` builds `ROUTES` (array of world-point polylines) and `PATH_SET` = the union of all route tiles, so where two routes overlap the roads simply **cross** (baked as an intersection). Enemies carry a `route` index assigned round-robin at spawn (`spawnIdx`, reset each wave); they move along `ROUTES[e.route]` and leak at its end. `drawSpawnAndBase` draws a breach at every route start and one base. `buildable` = any in-bounds tile not in `PATH_SET`. **`buildTerrain()` must never run in the frame loop** — only on map load; `selectMap(i)` re-bakes only on change.

Roster (rising difficulty, **10 maps** in v2.5 / 7 in the v2.4 file): `Outpost` 9×12 ★ 1-route intro · `Serpentine` 11×16 ★★ · `Spiral` 11×14 ★★ single route spiraling inward, base at the center (v2.5) · `Crossroads` 11×16 ★★★ 2-gate crossing · `Gauntlet` 9×18 ★★★ tall single-route switchback ladder (panned) · `Delta` 13×18 ★★★★ **3 entrances** whose routes T-merge (parallel overlap = merge, no bridge) into one final approach · `Sprawl` 13×22 ★★★★ long single route (panned) · `Fracture` 12×16 ★★★★ 2 gates with one perpendicular bridge crossing then a T-merge final (v2.5) · `Twin Gates` 13×20 ★★★★★ 2-gate crossing (panned) · `Terminus` 15×22 ★★★★★ finale: 3 entrances, two bridge crossings + merges, largest board (v2.5). `S.mapIndex` (persisted `map`) selects. In v2.5 the menu map picker is a styled native `<select>` dropdown (`id "mapsel"`, class `.mapsel`, options show name/★/size/gates) — free play only; the v2.4 file keeps the button grid.

**Campaign mode**: the maps double as a mission sequence. Menu button `⚔ Campaign · Mission N/${MAPS.length}` (`id "cp"`) starts at `Store("camp")` (clamped); `S.campaign = true`. On victory the screen becomes "Mission N Secured", persists `camp = mapIndex+1` (clamped to last), and offers `⚔ Next Mission · <name>` (`id "nm"` → next map, fresh run) — or a `⚔ CAMPAIGN COMPLETE` banner on the last map. Free-play Deploy and Endless set `S.campaign = false`; map picker stays independent for free play.

## 1.6 Economy and progression

```
START_SCRAP = 150   START_CORE = 20   TOTAL_WAVES = 20   NEXT_WAVE_COUNTDOWN = 6s
SELL_REFUND = 0.7 (of cumulative cost through current level incl. branch)
Wave clear bonus = round((20 + wave*3) * waveBonusMul())
Difficulty (three tiers in `DIFFS`, chosen on the menu, persisted): Normal hp1.0/spd1.0/core20/alloy1.0 · Hard hp1.7/spd1.1/core18/count1.25/reward0.92/alloy1.5 · Brutal hp2.6/spd1.2/core15/count1.6/reward0.85/alloy2.0. `count` pads each wave's spawn list; `reward` scales scrap per kill. Leaks cost 1 core (brute/sentinel/warden 2, juggernaut 6).
```

Meta (persistent, via `Store`): **Alloy** earned per run = `round((kills + wave*4 + (victory?120:0)) * (hard?1.4:1))`, awarded once (`awardAlloy`, guarded by `S.alloyAwarded`). Spent in the **Armory** on six talents (see 1.15).

## 1.7 Towers

Four archetypes in `TOWERS`, order `[turret, cryo, mortar, tesla]`. Each has a `kind` (firing model), a `dmgType`, two shared levels (L1, L2), then an L2→L3 **branch** choice into one of two specializations. `t.lvl` is 0/1/2; at `lvl 2` stats come from `t.branch` (`"a"`/`"b"`). `tStats(t)` returns the branch spec at L3, else `levels[t.lvl]`.

| id | name | kind | dmgType | L1 cost/dmg/range/rate | branch a | branch b |
|----|------|------|---------|------------------------|----------|----------|
| turret | Auto-Gun | hit (hitscan+tracer) | kinetic | 50/9/152/500 | Gatling: 75, dmg16, rate175 | Breacher: 85, dmg44, rate500, **pierce** |
| cryo | Cryo Emitter | orb (AoE slow) | energy | 65/4/136/720, splash60 slow0.45 | Glacier: 80, splash96, slow0.84 | Shatter: 95, dmg26, **+90% vs slowed** |
| mortar | Mortar | lob (arced splash) | explosive | 85/26/150/1150, splash78 | Carpet: 105, dmg56, splash126 | Buster: 135, dmg158, **pierce** |
| tesla | Rail-Tesla | beam (hitscan bolt) | energy | 95/40/215/1250 | Arc-Coil: 115, dmg58, range250, **chain 3** | Railgun: 155, dmg205, range320 |

Firing models in `fire(t, p, st)` compute `dmg = st.dmg * dmgMul()` (talent) then: `hit` instant + tracer; `beam` instant + jagged bolt (+ chain if `st.chain`, which also hits flyers); `orb` homing projectile → AoE damage + slow (+shatter bonus vs slowed/frost); `lob` arced shell → AoE explosive (skips flyers). `st.pierce` bypasses armor.

Targeting: per-tower `mode` in `[first, strong, close]`. `findTarget` skips flyers for towers whose kind is not direct-fire (`canHitAir(kind)` = `hit || beam`).

## 1.8 Enemies

Ten types in `ENEMIES`. `statsFor(type, wave)` applies HP mul `1 + 0.14*w + 0.013*w²` (quadratic so late waves outpace maxed towers), speed mul `1 + min(0.25, 0.011*w)`, reward mul `1 + 0.035*w`, shield mul `1 + 0.12*w`, and copies trait flags. Non-boss wave counts grow faster (`rd = 3 + 0.7n` capped 22, etc.) and are padded by the difficulty `count` multiplier.

| id | label | base hp | speed | reward | r | shape | trait |
|----|-------|--------:|------:|-------:|--:|-------|-------|
| stalker | Stalker | 16 | 148 | 4 | 15 | stalker | fast runner |
| raider | Raider | 34 | 90 | 5 | 18 | raider | medium |
| brute | Brute | 105 | 54 | 10 | 24 | brute | tank |
| sentinel | Sentinel | 92 | 60 | 13 | 23 | brute | **armor** |
| wraith | Wraith | 38 | 122 | 8 | 16 | wraith | **flying** |
| warden | Warden | 66 | 72 | 12 | 20 | raider | **shield 60** |
| juggernaut | Juggernaut | 1150 | 42 | 130 | 34 | juggernaut | boss, **shield 420** |
| mender | Mender | 55 | 72 | 14 | 19 | mender | **heals** allies in r95 by 7% maxHp every 1.3s (`heal:1`) |
| splitter | Splitter | 110 | 60 | 16 | 23 | splitter | **splits** into 3 spawnlings on death (`split:3`) |
| spawnling | Spawnling | 14 | 138 | 2 | 11 | stalker | fast fragment (spawned only, never in `waveComp`) |

**Behavior traits**: `statsFor` copies `heal`/`split`. Mender pulse lives in `updEnemies` (timer `healT`, fx flag `healFx` drives a green cross-glow + expanding ring; only fires if it actually healed someone) — counterplay is STRONGEST targeting to focus it. Splitter death lives in `hurt()` (after the scorch decal): spawns `e.split` spawnlings via `statsFor("spawnling", S.wave)` with the difficulty hp/speed multipliers applied, on the parent's route/`pi` with position offsets — leaks do NOT split (no `hurt` call), so letting one through costs 1 core, killing it late costs 3 fast runners near your base.

**Counterplay triangle** (in `hurt(e, dmg, type, pierce)`):
- **armor** (unless `pierce`): kinetic `*0.4`, explosive `*1.2`, energy `*1.0`.
- **shield**: absorbs the whole hit (no bleed-through that hit); energy strips `*1.6`, kinetic `*0.7`, explosive `*1.0`. Regenerates `shieldMax*0.22`/s after 3s with no damage (`shieldCd`). Boss shield shown on the boss bar; others as a bubble + thin bar.
- **flying**: only direct-fire towers (turret, tesla) can target; ground splash (mortar, cryo) cannot; splash loops skip flyers. Airstrike hits everything.

> **Updated V6.44.** `waveComp(n, mapIdx)` now takes the map, and a Tier C/D map may carry an
> optional `bias` naming one or two of five profiles (`air`/`cloak`/`armor`/`shield`/`energy`).
> `applyWaveBias()` substitutes units **within an HP class**, so the wave's unit count is unchanged
> and its HP budget moves under 3.5%. Tier A/B maps carry no bias. The bias runs before the
> V6.26 detector rule. Boss waves are excluded. See the V6.44 changelog entry.

`waveComp(n)`: every 5th wave is a boss wave (2 juggernauts every 20th wave, else 1). Non-boss waves add stalkers/raiders/brutes plus `wraith` (n≥3), `sentinel` (n≥6), `warden` (n≥8), `mender` (n≥9), `splitter` (n≥11). Counts are capped (rd≤20, br≤12, wr≤10, se≤10, wd≤8, md≤6, sl≤7, boss-wave extra≤16) so deep **endless** waves stay performant; `statsFor` keeps scaling HP unbounded. Campaign values (n≤20) are below the caps, so the campaign is unchanged. Leaks cost 1 Core (5 for a juggernaut).

**Endless mode** (`S.endless`): reachable from the menu (`∞ Endless`) or by continuing after a campaign victory. Waves never trigger `victory`; `startWave`, the auto-advance countdown, and the HUD deploy control all drop the `TOTAL_WAVES` cap when `S.endless`. Best endless wave persists as `highEndless`. A long endless run is the main Alloy farm (Alloy scales with `S.wave`).

## 1.9 Active ability — Airstrike

`STRIKE_CD = 30 - Meta.val("strike")` effective (`strikeMax()`), `STRIKE_R = 92`. Tap to arm (`S.strikeArm`), then a tap in the play viewport calls `doStrike(wx, wy)`: layered explosions + `explosive`, `pierce` AoE that hits air and ground, brief `stunT`, shake + flash. Armed state shows a red reticle overlay ("TAP A TARGET ZONE") and the button reads CANCEL.

> **Updated V6.26 / V6.36.** The control was a bottom-left circular FAB floating over the map. It became a labelled tray button in V6.26, and in V6.36 it moved into the **permanent command strip** (`drawCmdRow`, pinned to the tray's bottom edge) alongside PULSE and CORE — the strip is drawn in every tray state, so the Airstrike is never hidden and no longer exists in two places at once. `drawAbility()`/`drawHeroFab()` are deleted.

## 1.10 The wave-progression guarantee (do not regress)

Two independent guards make a freeze impossible; both must remain:
1. The Deploy/Start control lives in the persistent top HUD (`drawHUD`), always rendered (`hudBtns` id `"nextwave"`) and checked **first** in the tap handler, so it is always tappable even with a selection panel showing.
2. `NEXT_WAVE_COUNTDOWN` auto-starts the next wave with zero input after a wave resolves (`frame` loop, `S.countdown -= raw`).

**UX note (changed on request):** selection is **no longer** force-cleared when a wave *resolves* — the player's build/upgrade focus stays on the build menu through the between-wave countdown (a wave ending no longer yanks the menu away). This is safe because guards 1–2 alone prevent any softlock (selection panels never gate input in v3; the Deploy button and the countdown both work regardless). Selection is still cleared on `startWave` (a fresh wave begins) and on `reset`, and whenever an ability is armed. Onboarding, airstrike arming, and the Armory remain **non-blocking**. Enemies a mismatched defense cannot kill simply leak and the wave still resolves.

## 1.11 Game loop

`frame(now)` caps dt at 50ms; when `screen === "playing"` runs `updSpawns, updTowers, updEnemies, updProjs, updParts`, shake decay, airstrike cooldown, and the auto-advance countdown. Speed multiplier (1/2/3) scales the sim step, not the render. Render order: world layer under camera (terrain blit → pool glow → scorches → spawn/base → sel range → towers → enemies → gibs → beams → projs → booms → smoke → parts → atmosphere), then screen FX (flash, low-core vignette, strike-arm overlay, boss bar, banner), zoom buttons, ability FAB, HUD, tray, tutorial.

## 1.12 Input model

Pointer events with a `ptrs` Map for multitouch. Tap vs pan by `DRAG_THRESH = 8px`; two-finger pinch zoom + pan. Build flow is plot-first.

`tap(x, y)` priority order **as of V6.38**: tutorial skip → a read-only tour card (swallows the tap and advances) → HUD buttons → zoom buttons → STRIKE → PULSE → armed-airstrike placement → **build-panel buttons** → tray buttons → world tile. The build panel pushes a full-panel `action: "none"` entry last so a tap on its background is absorbed instead of falling through to the map, and `gest.inPlay` excludes the panel so a drag starting on it cannot pan the camera. **Order matters:** anything hit-tested before another control steals its taps — V6.31 caught the DEPLOY button shadowing the landscape side panel, and V6.38 caught SKIP overlapping DEPLOY by 5px. A document-level `pointerdown` calls `Sound.resume()` to satisfy mobile audio autoplay policy.

## 1.13 Persistence and audio

`Store` wraps `localStorage` with an in-memory fallback. Persists (V6.38, 28 keys): `sfx`, `music`, `haptics`, `eco`, `speed`, `map`, `camp`, `difficulty`, `customDiff`, `high`, `highEndless`, `medals`, `ach`, `astat`, `alloy`, `talents` (legacy, migrated), `research`, `spec`, `tutDone`, `seenIntro`, `seenTips`, `ddone`, `dlast`, `dstreak`, `logEnabled`, `telemetry`, `devArmory`, `devUnlock`. `Sound` is **two layers** (V6.40). The **bank** is 38 SFX variants plus a 22.5-second music loop, rendered offline by `tools/render_audio.py`, encoded as Opus and embedded as base64 in **its own `<script>` block** (540 KB) — decoded once on the first user gesture. Each event has 1-4 variants; every shot picks one (never the same twice running) and jitters pitch ±6% and level ±1.5 dB, so no two shots are identical. Combat sounds carrying world coordinates get stereo pan and distance attenuation. SFX and music sit on separate buses into the master gain → tone-shave → `DynamicsCompressor` chain, and loud events duck the music bus to 0.45 for ~1.2 s. The **procedural synth underneath is unchanged and still the fallback**: if `window.__AUDIOBANK` is missing, a decode fails, or WebAudio is refused, every event falls through to the original oscillator/noise synth and the music bed. Both layers are gated by the persisted **SFX** flag; music by the **independent, persisted Music flag**, started on entering play and faded out otherwise. Haptics (`vibrate()`) are gated by the persisted `haptics` flag. All guarded in try/catch.

## 1.14 Onboarding

First-play coach-marks (`S.tut`, persisted `tutDone`), plus a one-time first-launch briefing (`INTRO_CARDS`, persisted `seenIntro`) that is re-readable from the Codex.

**V6.38 guided tour.** `TUT` is a 7-step array walking the whole interface: top bar → build → upgrade → **Commander** → command bar → wave intel → DEPLOY. Each step names one piece of UI, says what it is for, and spotlights the real element. A step with a `done()` predicate waits for the player to perform the action; a step without one is a read-only card that advances on any tap. Two properties keep it from rotting: `msg`/`sub` may be **functions**, so a step follows the player mid-flow (plot → tower row → confirm) instead of stranding them on a stale instruction if they back out; and every `spot()` falls back down a chain (confirm button → panel row → a free plot) so **no step can leave the player without a pointer** — asserted in the suite. `only` on a step locks the build panel to the row being taught via `tutAllows()`. Deploying wave 1 completes the tour. `drawTutorial()` uses `worldToScreen` for world targets and parks its card at whichever end of the play area the spotlight is not.

> Superseded: the three-step version (build → upgrade → deploy) described here before V6.38 never mentioned the Commander, the command bar, the wave intel, or the HUD — a new player reached wave 1 with no hero.

## 1.15 Meta-progression (Armory)

`Meta` module (persisted `alloy`, `talents:{id:level}`). Six talents in `TALENTS`, three levels each, escalating cost:

| id | name | effect (L1/L2/L3) | applied in |
|----|------|-------------------|------------|
| scrap | Reserves | +25/50/75 start scrap | `reset()` |
| core | Reinforced Core | +5/10/15 start core | `reset()` |
| dmg | Munitions | +5/10/15% tower dmg (`dmgMul`) | `fire()` |
| reward | Salvage Rigs | +8/16/25% scrap/kill (`rewardMul`) | `hurt()` |
| strike | Rapid Response | −3/6/9s airstrike cd (`strikeMax`) | `doStrike`, FAB |
| waveb | War Economy | +25/50/75% wave bonus (`waveBonusMul`) | `updSpawns` |

Armory is an HTML overlay (screen `"armory"`) reachable from the main menu and both end screens; rows show current→next effect, level pips, and hex-cost buy buttons. `Meta.buy(id)` deducts Alloy and persists.

> **Superseded V6.24 — this whole flat-talent model is gone.** The Armory is a **research tree**: `TECH` is 4 themed branches (ORDNANCE / ARC / LOGISTICS / COMMAND) of 8 distinct nodes each. Purchased nodes show ✓, the single next node is fully revealed, and everything deeper is fogged to a category tag — so you always know the *kind* of thing coming without knowing the specifics. Each branch carries one **free, reversible** a/b specialization fork (Alloy buys reaching it; flipping the choice is free, so combinations can be tried). New accounts start with `TOWER_START = ["turret", "cryo"]`; Mortar, Tesla and Pyre are unlocked by researching, and the two capstone towers (Siege Battery, Prism) additionally require `reqCamp: 12` campaign missions, enforced by `Tech.blockedBy()`. `Meta` is now a thin Alloy wallet; `Tech.bonus` aggregates node effects and every `*Mul()` getter reads it through `B()`. **Costs total 25,380 Alloy** (re-costed in V6.36 — see `docs/TECHTREE-spec.md` §5, and the V6.36 changelog entry for why the original 10,670 was wrong). Design of record: `docs/TECHTREE-spec.md`.

## 1.16 Debug hook

```js
window.__GAME = { S, cam, Meta, Tech, TECH, Sound, screenToWorld, startWave, tap, fitCamera,
                  render, worldToScreen, __zoomAt, build(c,r,type), deployHero, doPulse,
                  get enemies(){...}, get towers(){...}, get hero(){...},
                  get blocked(){...}, get __decor(){...}, __ENEMIES, __TOWERS,
                  sim(stepMs, maxIters) }
```
Used by the headless/Playwright harnesses. `build(c,r,type)` places a tower for combat tests; `Sound.bankState()` reports the audio bank's decode state (`0` idle, `1` decoding, `2` ready, `-1` unavailable) so suites can assert bank-vs-fallback; `Meta` lets tests grant/inspect Alloy and talents; `sim(stepMs, maxIters)` fast-forwards the simulation with no rendering (returns `{wave, screen, core, kills, iters}`) for auto-battle balancing. The frame update block is factored into `stepSim(stepMs)`, shared by the render loop and `sim`.

---

# Part 2 — Continuation Handoff

## 2.0 Changelog since v1

1. **Graphics overhaul** — procedural but consistent: distance-driven enemy walk cycles + directional facing, shaded per-type bodies (stalker/raider/brute/wraith/juggernaut), death gibs + scorch decals + frost/shield overlays; detailed per-level tower art with emissive trim and muzzle flash; de-checkered organic terrain with a baked light direction; layered explosions, comet-trail orbs, branching bolts, ambient motes.
2. **Strategic depth** — damage-type counterplay triangle; three counterplay enemy traits (armor, shield, flying) + boss shield; L2→L3 branching upgrades per tower; the Airstrike active ability.
3. **Audio** — procedural synth (noise + filters + compressor) replacing the beeps, plus an ambient music bed.
4. **Onboarding** — non-blocking first-play coach-marks.
5. **Meta-progression** — persistent Alloy currency + six-talent Armory.
6. **Content — endless + second map (Plan A)** — `MAPS[]` + `loadMap`/`selectMap` with a menu picker; a second `Switchback` layout; uncapped Endless mode with capped wave counts, `highEndless` best, and a post-victory "continue into endless".
7. **Balance pass (Plan C)** — added a headless auto-battle harness (`__GAME.sim`; the frame update block is now `stepSim`). Equal-count and equal-budget sims showed Tesla was the dominant per-scrap option (solo-cleared at L1) while the other three sat in their intended niches (Mortar/Cryo are air-blind by design, so their low *solo* numbers are the flyer counterplay, not weakness — confirmed by mortar+turret clearing wave 20). Fix was a targeted Tesla nerf (range/dmg/rate down across L1–L3); Tesla is now on par per budget and remains the anti-air/anti-shield specialist. No dead branches; mixed play stays challenging (normal ~clears, hard ~wave 20).
8. **Map variety** — generalized to per-map grid sizes and multi-route maps (see 1.5). Five maps from small to large-and-panned, single- to dual-entrance with crossing roads, rising ★1–★5. Enemies split across a map's entrances (two defensive fronts). Verified: all routes traverse to base, terrain re-bakes once per load, 60fps on the largest (13×22) map.
9. **Per-map biomes** — `BIOMES` table (rust/toxic/ash/ember) tints baked terrain, pools, directional light, and dust motes so maps read as distinct places. `BM = C + biome overrides`, set in `loadMap`.
10. **Hero / Commander (Plan B)** — one deployable, in-run-leveling unit (`hero`): energy auto-fire that hits air, XP from kills near it (5 levels), and an Overload Pulse ability (energy AoE + stun) on a 16s cooldown. Deployed via a second FAB (arm → tap a plot). `HERO` config; the `Command Core` Armory talent (`heroDmgMul`) boosts its damage. The frame update block is `stepSim` (also used by `__GAME.sim`), now including `updHero`.
11. **Graphics uplift (3 commits)** — (a) maps: baked path-edge ambient occlusion, denser+varied decor, new landmark set-pieces (ruined buildings, crashed dropships, sandbag walls, dead trees), richer barrels/rubble/craters/wrecks, road tire tracks; (b) towers: two-layer grounding shadow, glossy specular sheen, brighter emissive trim; (c) enemies: higher-contrast `bodyFill` + a screen-aligned overhead sheen and underside shadow for consistent form lighting. `_hx()` now parses `rgb()` strings so tint chaining is safe. All baked/per-frame-cheap; 60fps held.
12. **V2.1 + V2.2 SHIPPED (game is now branded V2.0)** — the Part 3 projection/composition phases:
    - **Island board**: terrain bakes with a `TMARGIN` ring — void backdrop with distant silhouettes, jagged island rim, extruded cliff faces (2 depth layers + strata + lit rim). Frame blits terrain at `(-TMARGIN,-TMARGIN)` over a `VOID_COL` fill.
    - **Curved roads**: corner tiles render as quarter-annulus asphalt (curb, trench shading, curved tire tracks, arc dashes) via a neighbor-pattern branch in `roadTile`; waypoint pathing unchanged.
    - **Trench walls**: straight/junction road edges get NW shadow gradients + lit SE inner walls, so paths read as sunken channels under the global light.
    - **Bridges**: `loadMap` classifies route overlaps — perpendicular = crossing (`CROSS_MAP`/`BRIDGES`), parallel = merge. Crossings render as live steel decks (`drawBridges`) with correct layering: under-route enemies → deck → over-route enemies (`bridgeUnder` predicate splits `drawEnemies`).
    - **Structures**: breach = ruined tunnel mouth (rock collar, teeth, dark throat, molten glow); base = walled fortress (wall ring, corner bastions with beacons, court) around the bunker.
    - **Authored set-pieces**: optional `MAPS[i].props` hand-place landmarks (reserved before hash decor); Outpost + Twin Gates composed.
    Verified: all 5 maps route-sim to gameover (gameplay untouched), no errors, 60fps in heavy combat on Twin Gates, guards intact. **V2.3 sprite baking SHIPPED**: `bakeSprites()` renders static tower bases (`towerBaseArt`) and enemy bodies (`enemyStatic`, per type incl. armor plating) once at 3x into offscreen canvases (`SPRITES`, `bakeSprite`/`drawSprite`; `ctx` is `let` and retargeted during baking so all shared helpers bake). Frame keeps only animated parts vector: legs/boots/arms/treads/rotor blades, rotating heads/cannon, and glows. Per-enemy sheen/underside overlays are baked generic sprites (`fx_sheen`/`fx_under`) blitted scaled by r — the frame loop constructs no per-entity gradients. Baked sprites carry an extra wear+rim pass (`wearPass`). Verified: no errors, walk anim intact, 57-59fps at max stress on software rendering. **V2.4 bloom/post pass SHIPPED**: a half-res glow buffer (`glowCv`/`renderGlow`) collects pure-glow emitters as cheap sprite blits (pool glows, breach/base/tower/hero/orb glows via baked `gl_*` sprites, beam impacts, boom flashes) under the same camera transform, then composites additively (`compositeGlow`, `lighter`) — bilinear upscale doubles as the blur. Plus drifting cloud shadows (`fx_cloud` sprite, multiply) and a per-biome screen-space color grade (`drawGrade`) that covers entities. **Adaptive quality** (`FX`/`fxTick`): bloom and clouds default on and auto-degrade (bloom first, then clouds) when the rolling 90-frame average exceeds 19ms, so weak devices settle at full frame rate (measured: 58.8fps settled on software rendering; capable GPUs keep everything). Do not re-run shadowBlur-heavy passes inside the glow layer — blits only. **Remaining: V2.5 optional tilt (only if still needed).**
13. **Difficulty rebalance + Brutal tier** — a greedy-player sim (builds/upgrades/branches with real scrap, uses hero + airstrike) showed every scenario cleared wave 20 with near-perfect core, even old-Hard without hero/strike. Fixes: quadratic HP curve, +25% speed creep, denser waves, heavier leak costs (tanks 2, boss 6), and the three-tier `DIFFS` table (Normal/Hard/Brutal) with count/reward/core/alloy knobs. Measured after tuning (same near-optimal AI): Normal full-kit comfortable, Hard full-kit 18 core (humans will bleed), Brutal full-kit 9 core and 8-tower builds DIE — Brutal is the maxed-talents tier. Re-run `scratch`-style sims via `__GAME.sim` after any future data change.
14. **Behavior enemies + 2 maps + Campaign mode** — (a) `mender` (radius heal pulse with cross-glow/ring fx + `gl_heal` bloom hint, wave 9+) and `splitter` (bursts into 3 `spawnling` fragments on kill, wave 11+); trait flags flow through `statsFor`, heal logic in `updEnemies`, split in `hurt()` so leaks don't split. Baked statics + animated bodies for both (mender pods/cross plates, splitter sac with squirming inner blobs + stubby legs). (b) `Gauntlet` 9×18 ash switchback ladder and `Delta` 13×18 toxic **3-entrance** map whose routes T-merge — roster is now 7, all in the menu picker. (c) **Campaign**: `⚔ Campaign · Mission N/7` menu button resumes from persisted `camp`; victory becomes "Mission N Secured" with a Next Mission button (or CAMPAIGN COMPLETE on map 7); Deploy/Endless clear `S.campaign`. Verified: both new maps route-sim to gameover, Delta uses all 3 routes, heal + split observed in a wave-12 sim, campaign start→victory→next→resume→complete flow, 60fps in combat on both maps, zero pageerrors, guards intact.
15. **V2.5 in a new file (`fallengrid-v2.5.html`; `fallengrid.html` frozen at V2.4)** — (a) the oblique tilt experiment — **since reverted to full 2D after user playtest; see Part 3 V2.5 for the verdict** (`TILT = 1` now; scaffolding inert); (b) **3 more maps → 10**: `Spiral` ★★ ember (single route spiraling to a center base), `Fracture` ★★★★ rust (2 gates, one bridge crossing + T-merge final), `Terminus` ★★★★★ ash finale (15×22, 3 entrances, two bridge crossings + merges); campaign is Mission N/10; (c) the free-play map picker is now a **styled `<select>` dropdown** (`#mapsel`) replacing the button grid — campaign is unaffected (it always resumes from `camp`). Verified: 154-probe screen↔world round-trip exact under tilt, tap selects the intended tile, all 10 maps no-tower route-sim to gameover, Terminus spawns on all 3 routes, campaign flow to CAMPAIGN COMPLETE on map 10, 56–60fps in heavy combat, zero pageerrors, guards intact.
16. **V3.0 REAL 3D — `fallengrid-v3.html`** — the documented WebGL exit, taken on user request. three.js r147 (UMD, 607KB) is embedded inline so the game stays a single offline file; the world is a true 3D scene (perspective camera at ~55°, extruded terrain, procedural low-poly meshes, one shadow-casting sun, per-biome sky/fog) while **all game logic, UI panels, HUD, audio, and the sim harness are unchanged** from V2.5. Full architecture in **Part 4**. Verified: 100-tile raycast round-trip exact, taps select the intended tile, all 10 maps route-sim to gameover, campaign flow, distinct biomes, zero pageerrors, ~40fps on SwiftShader *software* GL after adaptive quality (real GPUs run full quality).
17. **V3.1 polish pass on the 3D build** — ACES filmic tone mapping (exposure 1.05) + PCFSoft shadows with a stronger sun/ambient ratio for punchier shading; per-biome **gradient sky** background; additive **glow sprites** on every emitter (reactor core with pulse, breach lava, toxic pools, tesla orbs, cryo dome emissive pulse, tower accent + level studs, muzzle flashes, explosion flashes as camera-facing billboards, orb halos); terrain got **broken-slab height jitter** (0/1.1/2.2 steps — towers/hero/tile-highlight sit on `tileH`), **curb lip strips** where ground meets sunken roads, and stronger color mottling with patch tints; enemies got **swinging legs** driven by `e.walk`, spawn pop-in, spinning wraith ring; gatling barrels spin while targeting; 50 drifting dust motes; screen-space vignette on the UI canvas; pulsing selection ring. Verified: tap accuracy still exact (100/100 + real tap), route sims, campaign, zero pageerrors; software-GL fps unchanged after the adaptive ladder (real GPUs run full quality).
18. **V3.2 material/grounding polish (close-up quality)** — evaluation at max zoom showed towers reading as flat-shaded primitives on plastic slabs with nothing grounding them. Fixes: a **procedural PMREM environment map** (sky-gradient equirect canvas → `PMREMGenerator`) set as `scene.environment`, so **PBR `MeshStandardMaterial` metal** on towers (`std(col, rough, metal)`, roughness ~0.4 / metalness ~0.7) and the hero catch real specular/reflection; tower bases rebuilt as **beveled octagonal machined mounts** (dark foot + bevel + plinth, `rotation.y = π/8`) replacing the flat 50×50 pad; **contact-AO discs** (soft radial-black ground decals, `aoDisc`) under every tower, the hero, and all ground enemies (disc counter-offsets the spawn pop so it stays flat on the ground); a tiling **grunge texture** on the terrain via world-planar UVs (`toMesh(..., uvScale)`, `1/96`) multiplied over the vertex colors to kill the flat-plastic ground; exposure 1.05→1.12, hemi 0.72→0.55, sun 1.35→1.5 for more directional contrast now that the env map fills shadows. Verified: 100/100 tap round-trip + real tap, all 10 route sims, campaign, zero pageerrors, guards intact; software-GL fps ~23 on the largest map (PBR costs more per pixel in software rasterization — adaptive ladder already degrades there; real GPUs run PBR at full rate).
19. **V3.3 real post-processing + normal maps (the "high-quality 3D" pass)** — user note that the 3D still read "old and simple" vs the 2D build. Root cause: the 3D meshes had no per-surface detail and no post pipeline, whereas the 2D build's richness came from baked pixel detail + a bloom/grade pass. Fixes on the SURFACE + FINISH axes (both proven feasible before building): (a) a real `EffectComposer` stack — **SSAO + UnrealBloom + SMAA** — inlined from r147 `examples/js/` legacy modules (global-namespace, no bundling; ~124KB), giving true light-bleed, crevice shading, and clean edges; (b) **procedural tangent-space normal maps** (Sobel of a baked heightfield) on all metal (panel seams + rivets) and terrain (grit + cracks) so surfaces show relief under the sun; (c) a faster degrade-only adaptive ladder (SSAO→composer→shadows→downscale) that keeps normal maps/PBR at every level since they're near-free on real GPUs. See **Part 4.7–4.9**. Verified: 100/100 tap round-trip + real tap, all 10 route sims, campaign, zero pageerrors, guards intact; software-GL full stack ~14fps → ladder floor ~22fps (GPU-cheap post, CPU-rasterization-expensive; real phones run the full stack at cap). The honest remaining gap is FORM (procedural primitive meshes vs sculpted models) — an art-production task, not engineering (2.6).
20. **V3.4 code-authored art assets, pass 1 — towers + hero.** User: take it further / create real art assets. Interpretation stated up front: not purchased/Blender-sculpted GLTF (genuinely outside my ability), but **detailed models authored in code with real craft** — the approach many stylized games ship. Added a **modeling toolkit** (`Model()`/`xf()`) that accumulates primitives (box/cyl/sph/cone/torus) and **merges them per-material via `BufferGeometryUtils.mergeBufferGeometries`** (inlined, global-namespace) so a detailed multi-part unit is still ~2-4 draw calls. Redesigned all four towers as real machines on a shared beveled hex base with a glowing accent groove: **Auto-Gun** armored housing + mantlet + cheek plates + ammo drums + barrel (branches: spinning gatling cluster / long breacher); **Cryo** finned coolant base + emitter prongs + faceted glowing core dome; **Mortar** trunnion mount + recoil pistons + banded tilted tube + breech (branches: triple carpet / massive buster siege tube); **Rail-Tesla** insulator column + stacked coil toroids + top emitter cone + energy orb (branch: railgun rails). Redesigned the **hero** as a command unit: hex dais, armored torso with pauldrons + backpack + antenna, visored head, rotating energy-cannon arm. `emis()` PBR-emissive material zones bloom through the V3.3 pipeline. Tuned bloom (0.34/0.5/0.88) + emissive intensities so glows stay colored, and **fixed the tesla-orb scale** in `syncTowers` (the old `(4.5+lvl)×` multiplier ballooned the new real-radius orb). Verified: all 12 tower level/branch model paths build without error, 100/100 tap round-trip, all 10 route sims, campaign, zero pageerrors, guards intact; ~347 draw calls / 30k tris (trivial for real mobile GPUs). **Enemies (10 types) are pass 2** — same pipeline, next.
21. **V3.5 code-authored art assets, pass 2 — the 10 enemies.** Rebuilt every enemy with the `Model()` toolkit as a detailed, merged creature/machine with a strong silhouette and material zones, preserving the exact `syncEnemies` interface (per-instance tintable `MeshStandardMaterial` body/dark in `mats` for frost-lerp + hit-flash, shared non-tinted `emis()` eyes/emblems merged in, separate animated legs / wraith ring / shield bubble / heal ring / AO disc). Designs: **stalker** sleek capsule hunter-drone (nose cone, swept fins, 4 thin legs); **raider** trooper-bot (torso, chest plate, capsule head, shoulders, arms) — **warden** adds a front shield slab + back emitter; **brute** hulking mech (chest, shoulder yoke, capsule arms + box fists) — **sentinel** adds side armor plates; **wraith** floating icosahedral crystal core + spinning energy torus, no legs; **mender** rounded medbot with side dispenser pods + emissive green cross; **splitter** bulbous subdivided-ico sac with 3 visible inner blobs + stubby legs; **juggernaut** siege tank (hull, turret, cannon, track guards, road wheels, antenna). Toolkit hardened: added `cap`(capsule)/`ico`(icosahedron) primitives and made the merge index-safe (`toNonIndexed` — icosahedra are non-indexed). Verified: all 10 types instantiate through wave 19 with zero errors, 100/100 tap round-trip, all 10 route sims, campaign, guards intact; ~246 draw calls (per-instance PBR enemy materials are heavier on software GL but trivial for real mobile GPUs).
22. **V3.6 code-authored art assets, pass 3 — the maps.** (a) **Focal structures rebuilt with the toolkit**: `buildBreach` (ring of angled rock chunks + teeth + throat rim over the animated molten floor/glow) and `buildBase` (fortress — 4 perimeter walls, 4 corner bastion turrets with emissive beacons, a reactor housing with an emissive trim torus around the glowing core) — added to `world` as merged PBR models, a few draw calls each. (b) **Metallic props buffer**: a third merged buffer (`metal`, `MeshStandardMaterial` + `metalNormal`, roughness .5/metalness .62) so barrels (now with lid rim + mid band), wrecks, the dropship (fuselage + tail + stub wings + nose), building rooftop units/antennae, and bridge decks read as **shiny metal** instead of sharing the matte ground material. (c) **Scattered rocks/debris** on ~15% of buildable tiles (into the free merged land buffer). (d) Buildings gained a two-row lit-window pattern + rooftop greebles. All still baked at map load (never per-frame). Verified: all 10 maps build + route-sim to gameover, taps exact, campaign, zero pageerrors, guards intact; ~257 draw calls.
23. **QoL / settings pass** — (a) **Haptics on/off** (`Haptics` module, persisted `haptics`; `vibrate()` now early-returns when off — gates every buzz in the game; menu toggle paired with Audio in a `.row`, a short buzz confirms when re-enabled). (b) **Reset Campaign** — a menu ghost button (`id "rc"`, shown only when `Store("camp") > 0`) that `confirm()`s then sets `camp = 0`; Alloy/Armory untouched. (c) **Battery Saver** (`id "eco"`, persisted `eco`) — `G3D.setEco(on)` pins a lean fixed quality level (no post-processing, no shadows, full res, PBR+normals kept) and locks the adaptive ladder (`q.locked`); seeded at init from `Store`, a real win for battery/heat on the Android target. (d) **Speed persistence** — the 1x/2x/3x HUD toggle now saves to `Store("speed")` and seeds `S.speed` at init. (e) **Abandon confirm** — the pause-screen Abandon now `confirm()`s so a run isn't lost by a mis-tap. Verified: haptics gating (0 buzzes off / 45 on across a leak-heavy sim), speed + eco persist across reload, eco forces shadows off and releasing restores them, campaign reset clears to Mission 1, abandon returns to menu; route sims + campaign + guards intact.
24. **Audio split + range preview** — (a) **Music vs SFX** are now independent, persisted toggles (`Sound` module holds `sfx = Store("sfx")` gating `osc`/`nz`, and `musc = Store("music")` gating `startMusic`; API `toggleSfx`/`sfxOn`/`toggleMusic`/`musicOn` replace the old single `toggle`/`isOn`). Menu settings are two 2-button rows: Music | SFX and Haptics | Battery. Toggling SFX on plays a confirming blip; music start/stop still keys off `S.screen`. (b) **Tower range preview** — pressing (finger-down) a build tray button shows an **amber range ring** (`preRing`/`preFill` in `G3D.syncSel`) at the selected plot for that tower's L1 range, and pressing an Upgrade/branch button on a selected tower previews the resulting range; `S.rangePreview` is set by `previewFromPoint(x,y)` on pointerdown + updated on drag across the tray, and cleared on release (the release tap then performs the actual build/upgrade). No change to the one-tap build flow. Verified: Music/SFX toggle independently + persist, all four towers preview their correct L1 range (152/136/150/215) and clear on release, real tap-build still works, route sims + campaign + guards intact.

---

# Part 5 — V4 line (retention / liveops), `fallengrid-v4.html`

Copied from `fallengrid-v3.html` (V3.6 art baseline) as **V4.0**; each solution bumps the version +0.1. Sequenced by retention ROI from the competitive analysis (presentation is done; the gap vs successful Android games is retention systems, content cadence, platform integration). Same single-file / verify-before-ship discipline.

**V4.1 — Daily Op (date-seeded challenge + streak).** A once-a-day run with a deterministic map + difficulty + modifier picked from the calendar date (`dayKey` YYYYMMDD → `dhash` → `dailyConfig`), plus a persisted streak. **Modifier system** (reusable foundation): `activeMod` holds a bundle of multipliers folded into the existing balance via `modv(k)` — `diffMul` (hp), enemy `speed`, `modCount()` (wave counts), `reset` scrap/core, `awardAlloy` alloy, and a `ban` field that takes a tower offline in the build menu (`afford=false` + "⊘ OFFLINE" label). 8 modifiers (Swarm, Titan Rush, Blitz, Austerity, Fragile Core, Bounty, No Artillery, Grid Down). `Daily` persists `dstreak`/`dlast`/`ddone`; `complete()` on victory bumps the streak if yesterday was the last clear, resets to 1 on a gap, idempotent same-day; `activeStreak()` shows the live streak. Menu button `◈ Daily Op · <map> · <mod>` with ✓/🔥N; victory screen reads "Daily Op Cleared · 🔥 streak N"; +80 base Alloy bonus on a daily clear. `activeMod`/`S.isDaily` cleared on every non-daily start (Deploy/Campaign/Endless/Play-Again). Verified: config deterministic, modifiers apply (Fragile Core → 4 core), completion + streak increment/reset/idempotent, menu ✓/🔥, **no modifier leak into normal play** (Deploy still 20 core / 150 scrap), all 10 route sims + campaign + taps + guards intact.

**V4.2 — Achievements (24).** `ACH` array of `{id, name, desc, test(ctx)}`; `Ach` persists unlocks (`Store("ach")`) + cumulative stats (`Store("astat")`: kills, wins, alloy, maps-beaten set). Run flags added to `reset` and set in the sim: `S.leaked` (any core loss), `S.usedStrike` (doStrike), `S.builtTypes` (per tray/debug build), plus run-end reads of hero level and max-tier towers. `runAchCheck(won)` runs once per run (guarded by `S.achChecked`) on the gameover/victory render, updates cumulative stats, builds a ctx (won/diff/noLeak/endlessBest/heroLvl/usedStrike/builtCount/maxTower/kills/alloy/wins/mapsBeat/talentsMaxed/campaignDone/dailyStreak), and unlocks any passing achievement; newly-unlocked names show as a gold 🏆 line on the results panel. Armory purchases also call `evalAch(achCumulative())` so talent achievements unlock immediately. Menu button `🏆 Achievements · N/24` opens a scrollable list screen (`S.screen === "ach"`) with 🏆/🔒 per entry. Categories: wins/difficulty/flawless, endless milestones, cumulative kills, maps beaten, campaign, daily streak, tower mastery, hero, airstrike, alloy, talents. Verified: menu screen lists 24, a scripted win unlocked win1/noleak/l3/hero/arsenal/strike, unlocks persist across reload, `runAchCheck` idempotent (stats not double-counted on re-render), route sims + campaign + guards intact.

**V4.3 — Codex.** The old "Field Manual" (`S.screen === "howto"`) is now a **tabbed Codex** (`S.codexTab`: guide / tw / en; menu button renamed "📖 Codex"). **Guide** = the how-to-play bullets (updated for Daily Op). **Towers** = data-driven from `TOWERS`/`TOWER_ORDER`: each archetype with its accent-colored name, ◆ damage type, L1 DMG/RNG/rate, a role line, and both branch specializations with costs — topped by the damage-triangle summary. **Threats** = data-driven from `ENEMIES`: all 10 types in threat order with HP/SPD/reward and a one-line trait + counter hint (`THREAT` map: armor→explosive/pierce, flying→Auto-Gun/Tesla, shield→energy, mender→focus, splitter→kill-early-or-leak, boss→strip-then-sustain). Purely presentational/onboarding; no gameplay change. Verified: menu opens the Codex, all three tabs render their data (tower names/branches/dmgtypes, enemy names/traits/counters present), Back returns, route sim + guards intact.

**V4.4 — Expanded Armory (7 → 14 talents).** Added, each with `vals[3]`/`costs[3]`/`fmt` and a mul folded at one clean site: **Fire Control** `rate` (`rateMul = 1 − val` on `t.cd = st.rate`), **Targeting Optics** `range` (`rangeMul = 1 + val` on the findTarget gate + the 3D range ring + the range-preview + the 2D ring), **Warheads** `splash` (`splashMul` on `st.splash` at the two fire sites), **Salvage Refit** `sell` (`sellRefund = SELL_REFUND + val` on both sell display + sell action), **Contracts** `alloyf` (`alloyFindMul` in `awardAlloy`), **Battle Drills** `hrate` (`heroRateMul` on the hero fire cooldown), **Overcharge** `pulse` (`heroAbilCd = HERO.abilityCd − val` on the pulse cooldown). `TALENT_ORDER` regrouped (offense / economy / abilities / hero). The Armory list scrolls with 14 rows. Verified: 14 talents render, all buy, **base balance unchanged with talents at 0** (all 10 route sims still gameover), and the effects are exact — Overcharge L3 → pulse cd 16→10, Targeting Optics L3 → Tesla range 215→258 (preview reflects it), Salvage Refit L3 → 85-scrap mortar refunds 72 (85%). Guards intact.

**V4.5 — 5th archetype (Pyre) + deeper L4 upgrade tiers.** Two content additions folded into the existing tower pipeline.
- **Deeper tiers (L4):** every branch (`branches.a/.b` on all 5 towers) gained an optional `t4` stat block. The upgrade path is now L1 (build) → L2 (`+cost`) → L3 (branch choice) → **L4** (`t4`). `tStats(t)` returns `br.t4` when `t.lvl >= 3 && br.t4` (branch stats otherwise, `levels[t.lvl]` when unbranched). Wired at one site each: `towerSpent` adds the t4 cost; the tower-menu upgrade button is `canUp = t.lvl < 1 || (t.lvl === 2 && branch && t4)` with the right `upCost`; the upgrade action picks `levels[1].cost` vs `branches[br].t4.cost`; `branched` title flag is `t.lvl >= 2`; 2D `drawTowerArt` `brSpec` and 3D `buildTower` branch geometry both trigger on `lvl/L >= 2` so L4 keeps the branch's silhouette (just bigger stats). L4 values are ~1.5–1.7× the branch (e.g. Railgun→Annihilator 205→340 dmg / 320→352 rng; Buster→Devastator 158→270 dmg).
- **Pyre (5th tower, `kind: "burn"`, explosive, ground-only):** a cheap flame projector that hits **every ground foe in a forward cone** (range + |Δangle| < 0.85) for a small direct hit and stamps a **burn DoT** (`e.burn = {dps, t, pierce, dtype}`) ticked in `updEnemies` (after the `healFx` decay, inside the enemy loop, with an `if (!e.alive) continue` guard). Branches: **Inferno** (wider/hotter) and **Plasma** (`pierce`, ignores armor), each with a `t4`. New 2D art (`drawTowerArt` `else` after splitting tesla into its own `else if (kind==="beam")`): squat body + fuel tanks + flared nozzle + animated pilot flame. New 3D model in `buildTower` (`t.type === "pyre"`): pump body, side fuel tanks, flared nozzle tube + muzzle, plasma rails on the b-branch, and a pulsing emissive flame sphere reusing the `orb` handle so `syncTowers` animates it. `canHitAir("burn")` = false. Added to `TOWER_ORDER` (now 5), the Codex `ROLE` map, and the tray layouts (`drawTrayIdle`/`drawBuildMenu` now compute `bw` from `N = TOWER_ORDER.length` so 5 cells fit the width). **Full Arsenal** achievement → `builtCount >= 5`.
- Verified (Playwright/Chromium, `verify45.py`): V4.5 label; build menu renders 5 towers; all 5 types × both branches at **L4 build in 3D with no page errors**; Pyre places, **applies burn DoT** (`e.burn` observed) and kills (19 kills in a scripted wave); Full Arsenal reaches 5 built types; base balance intact (maps 0/3/7/9 route-sim to gameover with no towers, fresh scrap still 150); campaign next-mission flow intact; **0 page errors**. Guards intact (`nextwave`=1, `countdown -= raw`=1), `node -c` on the 608 KB game script passes.

**V4.6 — Android essentials (Capacitor-ready platform pass).** No gameplay change; makes the single HTML behave as a proper Android app when wrapped in Capacitor, with clean browser fallbacks so it still runs standalone.
- **Hardware/gesture back button:** new `onBack()` state machine — a live run pauses (or first cancels an armed airstrike/hero deploy), the pause screen resumes, sub-screens (howto/armory/ach/gameover/victory/stats) step back to the menu, and only the main menu returns `false` so the OS exits. Wired to `Capacitor.Plugins.App` `backButton` (→ `exitApp()` when unhandled) when present, else a browser/PWA fallback that traps the back gesture with a sentinel `history.pushState` + `popstate` re-push.
- **Lifecycle pause:** `visibilitychange`/`blur`/`pagehide` call `backgroundPause()` → `Sound.suspend()` (stops music + parks the `AudioContext`) and pauses a live run, so a backgrounded game never keeps simulating (and losing) or draining battery. New `Sound.suspend()` on the audio API.
- **Capacitor Haptics bridge:** `vibrate()` now prefers `Capacitor.Plugins.Haptics` when available — arrays/≥40 ms → `vibrate({duration})`, short taps → `impact({style: Medium|Light})` — falling back to `navigator.vibrate`. Still gated by the existing user-toggleable `Haptics.isOn()`.
- **Portrait lock** via `screen.orientation.lock("portrait")` (best-effort, try/catch). **Safe-area insets:** `#overlay` padding switched to `max(20px, env(safe-area-inset-*))` for notches/gesture bars (the in-canvas HUD is already inset by the 360×640 letterbox fit, so it clears notches on tall phones without touching HUD math).
- Verified (Playwright, `verify46.py`/`verify46b.py`): back state machine (playing→paused→playing, armed-strike cancel, all sub-screens→menu, menu→unhandled), a real browser back gesture pauses a running game, `visibilitychange` hidden pauses it, the **Capacitor Haptics path is actually taken** (`deployHero` → `impact("Medium")` observed on a stubbed plugin), safe-area CSS rule present, V4.6 label, and regression intact (route sim → gameover, campaign start). 0 page errors, guards intact, `node -c` passes.

**V4.8 — Camo/detection enemy + 4 new maps + Daily round-robin.** (V4.7 "second hero" skipped by user request — jumped straight to 4.8.)
- **Camo/detection mechanic — new enemy "Phantom" (`camo: true`, ground, hp 46 / spd 116 / ⬡9).** A cloaked infiltrator that towers **cannot target until it is revealed**. New `updDetection(dt)` (runs in `stepSim` *before* `updTowers`): camo foes fade their `e.revealed` timer down each tick, and any **Rail-Tesla tower** (range-based) or the **Hero** (150px) re-reveals every camo enemy in range to `revealed = 0.9` — so once detected, *all* towers can engage it. `findTarget` skips `e.camo && !(e.revealed > 0)`. An **airstrike** also reveals (`revealed = 1.4`) whatever it blankets. `statsFor` carries `camo`, spawn inits `revealed: 0`. Injected into waves from n≥7 (`ph = min(floor((n-4)/3), 8)`). 3D render: camo materials flagged `transparent` in `buildEnemy`; `syncEnemies` drives `opacity = revealed > 0 ? 1 : 0.2` (glowing eyes stay visible as a faint tell since the emissive eye material isn't in `mats`). Codex Threats tab + `ENEMY_LABEL` + Tesla ROLE ("reveals camo") updated.
- **4 new maps → roster 10→14:** Switchback (ember, 10×16), Crucible (toxic, 13×18, 2 routes), Meltline (ash, 12×20), The Maw (rust, 15×22, 3 routes). Free-play picker and campaign pick them up automatically. "Conqueror" achievement retargeted to `mapsBeat >= MAPS.length` ("Beat every map").
- **Daily Op now round-robins through every map.** Replaced the hash-mod map pick with a deterministic day-ordinal rotation: `dayNum(d)` = UTC midnights since epoch, `map = ((dayNum % MAPS.length) + MAPS.length) % MAPS.length`. Guarantees a different map each day, visiting all 14 in turn with no repeat inside a cycle (difficulty + modifier stay hash-driven).
- Verified (Playwright, `verify48.py`): 14 maps in the picker; Daily config over 28 consecutive days = `[0..13,0..13]` (covers all 14, changes daily, periodic, no consecutive repeat); camo unit test — a crafted cloaked Phantom is **not** targeted by a turret with no detector, a placed Tesla sets `revealed=0.9` and the turret then targets it; Phantom actually spawns in waves 7–14; all 4 new maps load (3D world builds) and route-sim to a resolved end; base map0 still route-sims to gameover. 0 page errors, guards intact, `node -c` passes.

**V4.9 — Build/upgrade confirmation with stat preview + early-call wave bounty.** Player-help & wave-flow pass from direct user feedback.
- **Confirm step with stats (build / upgrade / branch).** Tapping a tower in the build tray, or UPGRADE/a branch in the tower menu, no longer commits instantly — it arms `S.pending = {kind, tower, branch}` and the tray renders a **confirm panel**: tower art + name, ◆ damage type, a stat line, cost, and **✓ CONFIRM ⚙cost / ✗** buttons, with the range ring shown persistently on the target plot. Builds show plain stats (`DMG · RNG · rate · ~DPS`); upgrades/branches show **before→after deltas** (`DMG 9→15  RNG 152→168  0.5→0.4s  DPS 18→34`) plus special tags (PIERCE/CHAIN/SPLASH/SLOW/BURN/SHATTER). The mutation moved into a new `doPending()` (re-checks affordability); the old instant-build/upgrade/branch paths in `trayAction` now just set `S.pending`. `✓` disabled (dim) when unaffordable; a world tap, ability-arm, wave start, or `reset()` cancels the pending confirm. `previewFromPoint` early-returns while pending so the hold-preview doesn't fight the persistent ring. Fixes the "it builds the instant I release" problem and surfaces stats before spending.
- **Early-call wave bounty + longer prep window.** `NEXT_WAVE_COUNTDOWN` 6→12 s. Deploying the next wave before the countdown expires pays `earlyCallBonus() = round(countdown × (2 + min(wave,15)×0.5))` bonus scrap (more time forfeited → bigger reward), with a floating `+N EARLY` at the reactor. The DEPLOY button advertises the live bonus (`▶ DEPLOY 8s  +32`). Rewards aggressive pacing while the longer default gives builders more room; base wave-clear bonus unchanged.
- Verified (Playwright, `verify49.py`, driving real taps via exposed `trayBtns`/`hudBtns`): first build tap arms pending + shows the ring but **does not build**; cancel builds nothing; confirm builds and deducts 50; upgrade routes through confirm (no lvl change until confirm, then +1); early-call pays exactly +32 at wave 4 / 8 s left and starts the wave; a no-time call pays 0; countdown lengthened (~11.9 s after a clear); base map0 still route-sims to gameover; campaign start intact. 0 page errors, guards intact, `node -c` passes.

**V4.10 — Continuous wave cadence + send-during-wave bounty.** Reworked the wave loop so the next wave is on a visible, always-running timer (old model only started the countdown *after* the field was fully cleared, and blocked sending a new wave while one was active).
- **Continuous timer / auto-launch:** new `WAVE_GAP = 20 s`. `S.countdown` now runs continuously while more waves remain (frame + headless sim ticks lost their `!S.waveActive` guard); at 0 it auto-launches the next wave. `startWave()` no longer guards on `waveActive` — it **appends** the new wave's spawns to the live queue (`{...e, t, w}`, `w` = wave# so overlapping spawns keep their own `statsFor` tier) and re-arms `WAVE_GAP`, so waves overlap under pressure. `updSpawns` always drains the queue and **derives** `S.waveActive = wave>0 && (queue||enemies)`; victory fires when every wave is launched and the field is clear. Per-wave "survival salvage" (`20 + (wave-1)*3`) now paid on each launch (wave ≥ 2) instead of on clear.
- **Send next wave mid-combat for a bonus:** the HUD button is now always shown while waves remain (not just between waves). Label shows the live countdown + bonus (`▶ NEXT 15s  +36`); left of it shows `☠ N` hostiles while fighting or the incoming-wave dot preview otherwise. Tapping it launches the next wave immediately (even with the current one alive) and pays `earlyCallBonus() = round(countdown × (2 + min(wave,15)×0.5))` — launching wave 1 pays nothing (no prior wave survived).
- Verified (Playwright, `verify410.py`): countdown runs from run start (~20 s, button present) and ticks down; with core pinned high, waves **auto-launch on the timer and overlap** (reached wave 3 with 5 enemies still alive, `waveActive`); mid-wave send pays exactly early+salvage and increments the wave; wave-1 early launch pays 0; a maxed build reaches **victory** (wave 20) and a no-tower run still **gameover**s; campaign start intact. 0 page errors, guards intact, `node -c` passes.

**V5.0 — Anti-slow & anti-mono balance (Roadmap Iter 1).** Breaks the dominant "Tesla-wall + Glacier-stall" strategy and fixes the spawn pile-up.
- **Slow floor** `SLOW_FLOOR = 0.3`: no enemy can be slowed below 30% speed, so stacking slow past 1–2 towers is wasted (over-investing in Glacier is now a disadvantage). Applied at the single cryo/glacier slow site with `slowResist`.
- **Two new enemy types** (data-driven, reuse existing 3D shapes): **Cinder** (`slowResist:1`, hp58/spd128, molten orange, `stalker` shape) — freeze-immune rusher that ignores all slow → needs raw DPS/coverage; **Insulator** (`energyResist:0.6`, hp128/spd66, grey-green, `brute` shape) — takes only ~40% from energy (Tesla/Cryo) → forces kinetic/explosive. `hurt()` applies `energyResist` before shield+hp; `statsFor` carries both traits. Injected into waves from n≥8 (Cinder) / n≥10 (Insulator); Codex Threats + `ENEMY_LABEL` + HUD incoming-preview order updated.
- **Congestion cap** `CONGEST_CAP = 45`: the continuous auto wave-launch **pauses** while ≥45 hostiles are already on the field (anti-spiral; the player can still send manually). Kills the runaway pile-up that the V4.10 overlap + heavy slow produced.
- **HP-bar declutter (bug fix):** the per-enemy bar in `overlay()` is now skipped for undamaged foes (`ratio>=1 && shield full`) → the "growing green wall" at crowded spawn gates (the reported screenshot artifact — full-HP bars of a single-file queue, not an inverted bar) is gone; bars still deplete correctly per enemy.
- Verified (Playwright, `verify50.py`): new enemies spawn in waves 8–15; **slow floor holds at exactly 0.30** and **Cinder is freeze-immune** (`slowF=1`, never slowed); **Insulator takes 40%** of a raider's Hero-Pulse (energy) damage; congestion cap holds the timer at ≥45 then resumes on clear; **balance read at Hard through wave 15: mono-Tesla keeps core 5 vs diverse core 10** — diverse overlapping builds now beat the single-type wall; no-tower→gameover, maxed diverse build→victory, campaign intact. 0 page errors, guards intact, `node -c` passes.

**V5.0.1 — Frost = diminishing-returns slow field (feedback fix).** Player feedback: the hard `SLOW_FLOOR` cliff was crude — a stacked frost tower should still add value, just a *small* one. Replaced it. Slow is no longer applied on orb-hit; instead **`updSlow(dt)`** runs each tick and gives every ground foe inside a cryo/glacier tower's range a slow, with multiple frost towers **stacking with diminishing returns** toward a soft asymptote: `red = strongest; for each extra field red += (MAXRED - red) * (s/sMax) * SLOW_STACK_K` (`SLOW_ASYMPTOTE = 0.28`, `SLOW_STACK_K = 0.35`, `MAXRED = 1 - asymptote`). Cryo slow values retuned down so a single tower is strong-but-not-freeze (Glacier `slow 0.84→0.6`, Absolute t4 `0.9→0.66`, base cryo `0.45/0.55/0.68→0.35/0.42/0.5`, Shatter `0.4/0.42→0.34/0.36`; Glacier desc "near-freeze AoE"→"deep slow field"). Freeze-immune Cinder ignores it; `slowResist` scales the applied strength. Verified (`verify501.py`): measured curve **1 tower → 0.40 speed, 2 → 0.358 (+0.042), 3 → 0.331 (+0.027)**, asymptote ~0.28 — genuine diminishing returns, 2nd tower still adds value, no hard cliff; Cinder stays `slowF=1`; no-tower→gameover, maxed build→victory, campaign intact, 0 page errors.

**V5.1 — Frost on-hit + Custom difficulty + alloy coupling + anti-snowball (Roadmap Iter 2).**
- **Frost on-hit rework (user correction to V5.0.1):** the passive field is gone — slow now requires the **shot to land**. Each cryo orb impact stamps a per-tower slow source on the foes it splashes (`e.slowSrc[sid] = {s, t:1.2}`, `sid` = lazily-assigned `t._sid` carried on the projectile); `updSlow(dt)` decays sources and computes the effective reduction with the same diminishing-returns formula (strongest source is base, each extra closes `(MAXRED−red)·(s/sMax)·SLOW_STACK_K` toward `SLOW_ASYMPTOTE 0.28`). Side effect: Shatter's bonus again requires landed frost (the V5.0.1 always-on-in-field uptime shift is resolved).
- **Custom difficulty:** 4th difficulty button; when selected the menu shows a **slider panel** (Start Scrap 75–300, Enemy HP ×0.5–2, Enemy Speed ×0.5–2, Tower Damage ×0.5–2, Earnings ×0.5–2, Core 5–40) persisted in `Store("customDiff")` via the `CustomDiff` module. `diffCfg()` returns `CustomDiff.cfg()` for custom (hp/spd/core/reward flow through the existing spawn/reset/economy sites); `dmgMul()`/`heroDmgMul()` multiply by the tower-damage slider; `reset()` uses the custom start-scrap.
- **Challenge-score → Alloy:** `CustomDiff.alloyMul() = clamp(√((hp·spd)/(dmg·earn)) · (20/core)^0.35 · (150/scrap)^0.2, 0.2, 3)` feeds `diffCfg().alloy` and is shown live in the panel ("Alloy reward ×N.NN") — easy settings can't farm the Armory (floor 0.2), punishing settings pay up to ×3.
- **Anti-snowball:** `earlyCallBonus()` is damped by field congestion (`× max(0, 1 − hostiles/CONGEST_CAP)`) so chain-sending waves into a strong killbox stops printing scrap; the HUD button reflects the damped number automatically.
- **Consolidated regression suite** `verify_core.py` (scratchpad) — run after every iteration from now on. Verified this pass: no aura-slow (in-range enemy stays `slowF 1` while the tower can't fire), landed hit → 0.40, two landing towers → 0.358 (small extra), Cinder immune; custom scrap 300/core 30 applied, HP×2 doubles spawn HP (16→32), damage×2 doubles turret hits, alloy coupling easy 0.2 / base 1.0 / hard 3.0; early bonus at 30 hostiles damped 35→12 (+ salvage exact); confirm flow, congestion hold, daily rotation, no-tower→gameover, maxed→victory, campaign — all green, 0 page errors. `__GAME` now also exposes `CustomDiff`/`diffCfg`. Difficulty-row buttons restyled to fit 4 across.

**V5.2 — Challenge Modes engine + real Daily (Roadmap Iter 3).**
- **6 new rule-changing modes** on top of the 8 stat modifiers (14 total): **Sudden Death** (`waveGap 8`), **Fortress** (`startScrap 420`, `income ×0.12`), **Glass Cannon** (`dmg ×1.8`, `core ×0.3`), **Meltdown** (`noSlow` — all foes ignore slow), **Shrapnel** (`allowedTowers ["turret","mortar"]`), **Ion Storm** (`allowedTowers ["cryo","tesla"]`). New mode fields plumbed: `waveGap()` (reset+startWave), `baseStartScrap()`, `mul.income` folded into kill reward + wave salvage + `earlyCallBonus`, `activeMod.dmg` into `dmgMul`/`heroDmgMul`, `allowedTowers` whitelist in the build-menu ban check, `noSlow` early-returns `updSlow`.
- **Daily is now a briefing, not an instant start.** The menu Daily button opens `S.screen === "daily"`: mode name + description + an auto-generated **effect list** (`modEffects(mod)` renders each field as human text, e.g. "Waves every 8s (vs 20s)", "Only CRYO & TESLA", "+30% Alloy"), the map, and a **difficulty picker** (Normal/Hard/Brutal/Custom, defaulting to the day's suggested diff) → Deploy. **Streak credit now counts on any difficulty** (verified: a Normal daily win completes the streak); alloy still scales with the chosen difficulty via `diffCfg().alloy` (incl. the Custom challenge score from V5.1). New `startChallenge(mod, mapIdx, diff, isDaily)` helper (reused by the Challenge Lab in Iter 4); hardware-back returns the briefing to the menu.
- Verified (`verify52.py` + full `verify_core.py`): briefing opens with effect bullets + deploy/difficulty controls; player-picked difficulty flows into the run; all 14 modes produce briefing text; Sudden Death countdown 8s, Fortress start-scrap 420 + ~1 scrap/kill, Glass Cannon core 6 + boosted turret damage, Meltdown Glacier can't slow (`slowF 1`), Shrapnel bans cryo/tesla/pyre in the tray; Normal-difficulty daily win credits the streak; regression suite all green, 0 page errors. `__GAME` exposes `startChallenge`/`modEffects`/`MODIFIERS`/`waveGap`/`activeMod`.

**V5.3 — Challenge Lab (Roadmap Iter 4).** New menu entry "🧪 Challenge Lab" → `S.screen === "lab"`: two `<select>`s (modifier — 14 modes + "Standard (no modifier)"; map — all 14) plus a Normal/Hard/Brutal/Custom difficulty row, a live effect-list box (`modEffects`, shared with the daily briefing), and Deploy/Back. Persisted picks in `S.labMod`/`S.labMap`/`S.labDiff`. Deploy calls `startChallenge(mod, map, diff, false, true)` — the V5.2 helper gained an `isLab` 5th param. **Lab runs pay 50% Alloy** (`awardAlloy` × `(S.isLab ? 0.5 : 1)`) and **never credit the daily streak** (`isDaily` false). New `S.isLab` flag is cleared on every standard entry point (Deploy/Campaign/Endless/continue/play-again) so a lab run can't leak into a normal one; hardware-back returns the Lab to the menu. Verified (`verify53.py` + full `verify_core.py`): lab opens with 15 modifier + 14 map options; effect list updates on mode change; deploy sets `{isLab:true, isDaily:false, diff, map, mod}` exactly; **lab alloy = exactly 50% of the same non-lab win (150 vs 300)** and the streak is not credited; Shrapnel enforced inside a lab run (cryo/tesla/pyre banned); starting a normal game after a lab clears `isLab`+`activeMod`; regression suite all green, 0 page errors.

**V5.4 — Wave-1 manual start + onboarding + slow readability (Roadmap Iter 5).**
- **Wave 1 waits for a manual deploy:** `reset()` now sets `S.countdown = 0` (was `waveGap()`), so the continuous timer doesn't auto-launch the first wave — the player builds, then taps DEPLOY (which starts wave 1 and arms `waveGap()` for wave 2 onward). This also makes the existing coach-mark's "TAP DEPLOY" step wait properly. **Harness impact:** any sim-to-completion must now call `startWave()` first — `verify_core.py`'s maxed-victory test updated accordingly, plus a new wave-1-manual assertion (idles at wave 0 for 10s, then a HUD tap starts wave 1 and arms the timer).
- **Contextual one-time tips** (`TIPS`/`queueTip`, persisted in `Store("seenTips")`, rendered by `drawTips(raw)` as a word-wrapped toast under the HUD): the **timer/early-send** tip fires on the first deploy; **Phantom** (cloaked → Tesla/Hero reveals), **Cinder** (freeze-immune → raw firepower), **Insulator** (energy-resist → kinetic/explosive) tips fire the first time each new foe spawns. One-shot (never repeat once seen); never overlaps the first-run coach-marks.
- **Frost reads as "slow landed":** a cryo/glacier orb impact now spikes `e.frost` to 0.6 instantly and sprays ice-blue `sparks` on each foe it actually slows, so the on-hit slow is visible.
- **Wording:** Glacier desc "deep slow field"→"heavy slow on hit"; Codex cryo role updated to "slows the cluster it hits … frost stacks with falloff"; two new Guide bullets (wave timer/early-send, on-hit frost + diminishing stacking + freeze-immune foes).
- Verified (`verify54.py` + full `verify_core.py`): wave 1 idles at countdown 0 and doesn't auto-launch (10s), manual tap starts it + arms the 20s timer; timer tip fires once on deploy with a visible toast; phantom/cinder/insulator tips all fire once and don't repeat; Glacier hit spikes frost tint (0→1); regression all green, 0 page errors.

**V5.5 — Local play telemetry (user request, inserted before the polish iter).** A `Telemetry` module logs one record per run to `localStorage` (`Store("telemetry")`, capped at 60 runs) for data-driven balancing, with a JSON export the user can share.
- **Record fields:** `ts`, `ver`, `mode` (standard/daily/lab/endless/campaign), `mod` (activeMod id), `map`/`mapIdx`, `diff` + `custom` slider snapshot, `coreStart`/`coreEnd`, `result`, `wave`, `kills`, `alloy`, `heroLvl`, `finalScrap`/`peakScrap`, `durationMs`, tallies `builds`/`upgrades`/`branches`/`sells`/`scrapSpent`, wave-flow `earlySends`/`overlapSends`/`autoLaunches`/`leaks`, a **per-wave snapshot array** (`{w,scrap,towers,core,hostiles,kills}` at each launch — the economy/defense curve), and the **final board** (`{type,lvl,branch,c,r}` per tower) + `byType` counts.
- **Hooks:** `begin()` in `reset()`; `waveSnap()` in `startWave()`; send counters in the nextwave tap; `autoLaunches` in both countdown ticks; `leaks` at the leak site; build/upgrade/branch/sell + `scrapSpent` in `doPending`/sell; **`end()` at the MODEL layer** (the victory/gameover sites in `updSpawns`/`updEnemies`, not `render()`) so it fires in real play *and* headless sims.
- **UI:** menu "📊 Play Log · N" → `S.screen === "log"`: summary (runs · win% · avg wave), the last 14 runs, **⤓ Export JSON** (Blob download `fallengrid-playlog-YYYY-MM-DD.json`), Clear, Back. `__GAME.Telemetry` exposed.
- Verified (`verify55.py` + full `verify_core.py`): a full hard run on map 3 recorded all fields, 20 wave-snapshots, 25-tower board with byType, autoLaunches 19; manual early+overlap sends counted; custom-diff run captured the slider snapshot; Play Log screen lists runs and the **Export JSON download** produced a valid array with `board`; regression all green, 0 page errors. **Next: Iter 6 · V5.6 late-game scrap sink (core repair / overclock) + run summary + colour-blind cues + SFX.**

**How to use the log for balancing:** have the user play, open Play Log → Export JSON, and share the file. Key signals: per-wave `scrap`/`towers`/`core` curves (economy pacing & where core erodes), `byType`/`board` (which towers/branches dominate → mono-strategy check), `earlySends`/`overlapSends` (does the player use the tempo lever?), `leaks` vs `wave` (difficulty spikes), `custom` (what settings they gravitate to). Compare across `map`/`diff`/`mod` to spot outliers.

**V5.6 — Late-game scrap sink + run summary + accessibility (Roadmap Iter 6 — completes the V5 line).**
- **Core repair (scrap sink + comeback):** a HUD button (2nd row, next to pause) appears only while `S.core < S.coreMax` (max = starting core, captured in `reset()`); tap spends scrap to restore 1 core at a **rising price** `repairCost() = 50 + repairs·25 + floor(wave·3)`. Gives surplus late-game scrap a use and a comeback lever; logged to telemetry (`repairs`/`repairScrap`). New `Sound.repair()` chime + `+CORE` float.
- **Run summary:** the victory/gameover panels now show a tempo line under the tag — `N towers · X early / Y overlap sends · Z leaks · [R repairs] · Ts` — read from the just-logged `Telemetry.runs[last]` (surfaces the early/overlap-send playstyle).
- **Colour-blind cue:** damage type is now a distinct **glyph per type** (`DMG_SYM` — kinetic ◉, explosive ✸, energy ✦) on the tower menu, build/upgrade confirm, and Codex tags, so the counter-triangle reads by shape, not colour alone.
- Verified (`verify56.py` + full `verify_core.py`): repair button hidden at full core, appears after damage, restores +1 core for the exact rising cost (65 then 90), telemetry counts it, hides again at max; run summary present on the victory panel with early/overlap/leaks; all three damage glyphs render in the Codex; regression all green, 0 page errors.

**V5 "Balance & Challenge" line complete (Iters 1–6 shipped): V5.0 anti-slow/anti-mono + 2 foes; V5.0.1 diminishing frost; V5.1 frost on-hit + custom difficulty + alloy coupling + anti-snowball; V5.2 challenge modes + daily briefing; V5.3 challenge lab; V5.4 wave-1 manual + onboarding + frost readability; V5.5 telemetry; V5.6 scrap sink + summary + accessibility.** Backlog remaining: mid-run save/restore, expert-mode (skip build-confirm), second hero, full colour-blind toggle, GLTF art.

# Part 7 — Active roadmap: V6 "Threat & Consequence" (from the player's exported play-log)

**Play-log findings (7 runs, all victories, V5.5 telemetry export analysed twice — second pass on request):** 0 core lost in 6/7 runs incl. Brutal; coreStart 30–35 (maxed Armory: ≈×1.7 DPS, +20% range, +35% splash, +15 core) makes Brutal's ×2.6 HP trivial; economy runs away (earned 13–17k, spent ~6k, 7–10.7k unspent; board 90% built by wave 14–16 → last 5 waves decision-free); aggression is free (17–19 early + 7–11 overlap sends, peak **324–325 hostiles, 0 leaks** — the V5.1 damper killed the scrap-print but the remaining reward is risk-free time-compression); Railgun meta persists (43/79 towers Tesla, **39/43 Railgun-b**; the enabler is the slow conveyor ≈×3.5 shot-time, so per-hit Insulator resist is irrelevant); hero unused in all 7 runs. **New findings second pass:** (a) **perverse income incentive** — Brutal *pays more* than Normal (`count×reward` = 1.6×0.85 = **1.36**; data: 16.7k vs 13.4k earned); (b) **alloy faucet enormous** — 1,240–1,464 alloy/min on hard/brutal → full Armory (~5–6k) maxes in <1h of play, so god-mode arrives almost immediately; (c) **congestion cap is effectively dead** — it gates only AUTO launches, manual sends bypass it (hence the 300+ hostile piles, also a mobile-perf hazard). The only balanced run was the Fortress lab (tight income → 3 leaks, 2 repairs, real decisions), proving the fun is in scarcity+threat.

**User decisions locked:** (1) dynamic scaling (keep earned power, scale opposition), (2) make Brutal itself harder (no new tier), (3) yes — early/overlap-sent waves become more dangerous.

**Cross-cutting tool (build first, reuse every iteration): maxed-meta harness** — simulated account with full Armory + optimal Railgun/Glacier build + aggressive early sends. Acceptance targets: maxed+optimal on Brutal must NOT finish 0-core (win possible, but with meaningful core loss; pure-mono should lose); fresh account on Normal unchanged; diverse builds beat mono-Railgun. **Process rule unchanged: export the HTML to the user after every iteration; request a fresh play-log after V6.2 to measure real-play effect.**

- **Iter 1 — V6.0 Threat Level (dynamic scaling). ✅ SHIPPED.** `threatLevel() = purchased talent levels / (14×3)` (0–1). `THREAT_HP 0.85`, `THREAT_SPD 0.12` → at full threat enemies get **+85% HP** (folded into `diffMul`) and **+12% speed** (at spawn), plus a **+40% alloy** prestige bonus in `awardAlloy`. Shown openly: menu line + amber "⚠ +85% THREAT" HUD indicator + daily-briefing line. Fresh account (threat 0) = zero change; applies across all modes/difficulties (meta-based, orthogonal). New **maxed-meta harness** (`scratchpad/harness.py`, reused every V6 iter): loads a fully-maxed Armory + optimal wall/mono/diverse build and auto-plays aggressively per difficulty. Verified (`verify60.py` + regression): threat 0 fresh / 1.0 maxed / 6/42 partial; `diffMul` ×1.85 and a real spawn's HP ×1.875 at full threat; fresh hard `diffMul`=1.7 unchanged; menu/HUD show/hide correctly; 0 page errors, regression green. **Harness read:** the maxed account now **loses core where it previously lost none** (maxed/Normal aggressive-wall: 0→9 core lost). *Coefficient 0.85 is the play-test starting value — will re-tune from the next play-log; note V6.1's Brutal rework stacks on this, so check combined effect before shipping V6.1.* Exact per-difficulty core-loss from the harness is noisy (crude auto-player over-builds on Normal / collapses on Brutal) — trust the next real play-log over the auto-player for final tuning.
- **Iter — V6.1 Codex enrichment. ✅ SHIPPED (user-requested, inserted before the balance items).** Codex Towers tab now renders each tower as an image (`drawTowerArt`→offscreen canvas→`toDataURL`, cached in `THUMBS`) for the base + both specialisations, with a full **L1→L2→L3→L4 stat table** (DMG/RNG/rate/~DPS + special tags + costs) per branch; Threats tab shows each enemy's baked 2D body sprite (`SPRITES.get("en_"+type).cv.toDataURL()`) beside HP/SPD/reward + counter. Verified (`verify61.py`): 15 tower + 13 enemy images all real data-URLs, L4/DPS present, 0 page errors. **Balance items below renumber: Brutal→V6.2, aggression→V6.3, economy→V6.4, hero→V6.5.** *User guidance folded into the economy iter (V6.4): consider adding an extra plain upgrade level BEFORE the L3 specialisation choice — gives surplus scrap more to buy (a depth-sink) and lengthens progression, complementing the income taper.*
- **Iter — V6.1.1 Dev Armory override + design checkpoint. ✅ SHIPPED (user-requested).** Menu row **"Dev · Armory Override"** (OFF / 0% / 50% / 100%): forces a synthetic talent table through `Meta.lvl` (50% = exactly 21/42 levels, distributed round-robin) so **all talent power AND the Threat Level follow the override**; persisted (`Store "devArmory"`); amber banner in menu + notice on the Armory screen; **Armory purchases locked while active** (stored talents/alloy untouched — verified). Telemetry now records `dev` + `threat` per run and the record `ver` bumped 5.5→6.1.1 (was stale). Debug: `__GAME.setDevArmory(f)`, `__GAME.devArmory`. Verified (`verify611.py` + full regression): threat 0/0.5/1.0 exact under override on the same maxed account; real spawn HP ×1.875 between 0%→100%; buy() blocked + localStorage talents intact; persists across reload; OFF restores the real account; 0 page errors.

**Play-log #2 (2026-07-17, 3 runs) — V6.0 threat validated in real play.** Same (near-maxed) account that cruised all of play-log #1: (1) **Brutal GAMEOVER at wave 6** (Twin Gates, 4 towers, 25 leaks — a rush start no longer survives); (2) **Brutal victory now costs real effort**: Outpost, **511s** (was ~134s), **3 core repairs** (342 scrap), **hero deployed and levelled to 5** (unused in all 7 previous runs), early sends collapsed **17–19 → 2**, 17 auto-launches (player rides the timer instead of compressing it), build 18 towers incl. 10 Tesla but with cryo/turret/mortar/pyre support; (3) campaign Hard win with a genuinely **diverse board** (cryo 4 / tesla 9 / mortar 6 / pyre 2 / turret 1) and 11 core lost. Residuals: peak scrap 5.5k & wave-20 float ~4k (economy taper still needed → V6.4); Tesla still the backbone (Railgun nerf still queued → V6.4); brutal-pays-more income bug still live (→ V6.2).

**Armory design assessment (user question: does the Armory even make sense — or should Brutal be near-impossible until the Armory closes the gap?).** Diagnosis: with `THREAT_HP 0.85`, full Armory (~×1.7 effective DPS) vs +85% enemy HP is **nearly self-cancelling in combat power** — what the Armory nets today is mostly economy/comfort (core +15, start scrap, income, range, alloy find). That risks the "Oblivion level-scaling" feel: you buy power and the world absorbs it. The user's progression-gate model gives the Armory a *purpose* (the key that unlocks Brutal), but a pure gate re-opens play-log #1's god-mode: a maxed account steamrolls Normal/Hard forever. **Recommendation — hybrid (difficulty is the gate, threat becomes a soft rubber-band):** (a) cut `THREAT_HP` 0.85 → **~0.40** so full Armory keeps a real net power gain (~×1.2–1.3) and upgrades feel earned again; (b) move the difficulty into the **tiers themselves (V6.2)**: Normal beatable at 0% Armory, Hard tuned around ~50%, **Brutal tuned to be near-unwinnable fresh and fair-but-hard at 100%** (hp ~3.4, core 12, `count×reward ≤ 1`, steep wave-15+ curve, anti-conveyor elites) — this *is* the user's model, implemented in base stats instead of threat; (c) keep the +40% alloy prestige bonus tied to threat so playing "up" stays rewarded; (d) print a **recommended-Armory hint on the difficulty buttons** (e.g. "BRUTAL · Armory 80%+") so the gate is communicated. **Pending: the user tests Brutal/Hard at 0/50/100% with the new dev toggle; V6.2 sets the final coefficients from that verdict.**

- **Iter — V6.4 Light Brutal/Hard rebalance. ✅ SHIPPED (this is the "V6.2" content, re-scoped after play-log #4; shipped as V6.4 since V6.3 already used 6.3).** **Play-log #4 verdict (dev-toggle Brutal test): threat 0.85 was KEPT — the Brutal Armory-gradient already matches the user's model.** Changes: (1) **Hard floor raised** `hp 1.7→2.0` (was trivial at ≥50% Armory); (2) **perverse income fixed** — `count×reward` now ≤ 1 on both paid tiers (hard `reward 0.92→0.80` → 1.25×0.80=1.0; brutal `reward 0.85→0.62` → 1.6×0.62=0.99), so higher difficulty no longer *pays more* and the brutal cut also trims the runaway economy; (3) **anti-conveyor escort** — from wave 15 each wave gains `el = min(2+⌊(n−15)/2⌋, 5)` extra **Cinder (freeze-immune) + Insulator (energy-resist) pairs**, so the frost-slow + mono-Tesla "Railgun wall" must diversify to hold the late game (reuses existing tuned units — no new art); (4) **recommended-Armory hint** under the menu difficulty row — Hard ~30%+, Brutal ~70%+, teal "ready" when met / amber "tuned for ~X%+ … you're at Y%" when under (communicates the gate; `threatLevel()`-driven). `THREAT_HP` stays 0.85 (the 0.85→0.40 cut is **cancelled** — the data showed it would flatten the Brutal gradient the user likes). File renamed `fallengrid-v6.4.html`. Verified (`verify64.py` + regression): diff table (hard hp 2.0, hard cr 1.0, brutal cr 0.99), fresh hard `diffMul`=2.0, wave-16 carries more cinders+insulators than wave-12, hint shows amber-under / teal-ready / hidden-on-normal, difficulty row keeps exactly 4 children; 0 page errors.

**Play-log #4 (2026-07-17, 5 Brutal/Outpost dev-toggle runs) — the Brutal gradient already fits the model.** 0% (coreStart 15): a lazy 2-tower run died wave 4 — floor punishes; **50%** (coreStart 25): **loss wave 19** (6 repairs, 21 leaks) then **win wave 20** (1 leak) — perfect knife-edge; **100%** (coreStart 30, threat 1): comfortable **win wave 20** (0 leaks, core untouched, 20 towers). So threat scaling *is* the progression-gate the user asked for — Brutal near-impossible cheap, fair when the Armory is built. **Hard, by contrast, was trivial at ≥50%** (→ V6.4 raises its floor). Residuals still open: economy runaway (5.9–6.9k surplus even in wins; wave-15+ scrap explosion), Tesla/Railgun still the backbone (8–10 per win) → **V6.5 economy taper + Railgun nerf + slow asymptote + extra pre-spec upgrade tier**. Data gap: no *serious* 0% Brutal build attempted.

- **Iter — V6.2 (superseded).** Original heavy Brutal rework (hp→3.4, core 12, `THREAT_HP`→0.40) — **cancelled/absorbed into V6.4** after the dev-toggle data showed the base Brutal + threat 0.85 already produces the desired gradient. Kept here only as a record of the reversed decision.
- **Iter — V6.6 Menu restructure (declutter + Campaign-first + soft map gating). ✅ SHIPPED (user-requested UX).** The old flat menu (~35 interactive elements) was split into three levels. **Top menu (lean):** Campaign is the **primary** hero button (continue at current mission), then **Free Play**, **Daily Op**, **Challenge Lab**, a compact 2×2 icon grid (Armory · Codex / Achievements · Log), and **Settings**. **Free Play submenu** (`S.screen==="freeplay"`): map picker + **Mode toggle (20 Waves / ∞ Endless)** + difficulty row + custom sliders + Armory hint + Deploy + Back — everything map-specific lives here now. **Settings submenu** (`S.screen==="settings"`): Audio (Music/SFX), Device (Haptics/Battery), Reset Campaign, and the **Dev · Armory Override** (moved out of the main flow). **Soft map gating:** a Free Play map is unlocked iff `index ≤ camp` (campaign progress) — fresh account has only Outpost open, each new map unlocks as you reach it in Campaign; locked maps show 🔒 + "clear in Campaign" and are `disabled` in the picker; a stored out-of-range `mapIndex` clamps to `campProg`. `onBack` routes `freeplay`/`settings` → menu; `reset`/`selectMap` exported on `__GAME` so the regression suite is now menu-independent (`deploy()` helper instead of clicking `#p`). File renamed `fallengrid-v6.6.html`. Verified (`verify66.py` + regression): lean top menu (no map/difficulty/dev inline; Campaign primary+first), Settings holds the moved controls (dev override still works), Free Play has picker/mode/difficulty/deploy, camp0→1 unlocked / camp4→5 unlocked / clamp works, Deploy launches standard+endless+campaign correctly, submenu Back returns to menu; 0 page errors.
- **Iter 3 — V6.3 Aggression risk + real congestion discipline. ✅ SHIPPED (brought forward while the user play-tests the Armory levels).** Early-sent waves now carry a **rush buff**: `rush = clamp(countdown / waveGap(), 0, 1)` measured at send-time and tagged onto every spawn of that wave (`{…, rush}`); `updSpawns` applies `×(1 + rush·RUSH_HP)` to HP/shield and `×(1 + rush·RUSH_SPD)` to speed (`RUSH_HP 0.35`, `RUSH_SPD 0.10`). Auto-launch and wave-1 deploy send with `countdown 0` → rush 0 (no penalty). **Advertised on the DEPLOY button before the tap** — e.g. `▶ 20s +48 ⚠34%` (countdown · early-scrap reward · rush-HP cost), so it's an informed gamble. **Manual sends are now blocked at CONGEST_CAP (45)** just like auto-launches: the button greys to `⚠ FIELD FULL`, a tap shows a "FIELD SATURATED" banner + error SFX and does not launch (telemetry `satBlocks`) — closes the 150–200-hostile piles for both balance and mobile perf. **File renamed to `fallengrid-v6.3.html`** (version now in the filename, per user request). Verified (`verify63.py` + regression): full-timer send +37.5% spawn HP, half-timer +18.8%, auto-launch unchanged; saturated field blocks both manual tap (wave frozen, banner shown, satBlocks counted) and auto-launch; 0 page errors.

**Play-log #3 (2026-07-17, +3 dev-toggle runs on V6.1.1) — why V6.3 was the right call.** User tested the dev override on **Hard/Outpost**: (a) **0% Armory** (coreStart 18, threat 0) → win but *earned* — **6 repairs** (939 scrap), 1 leak, core dipped to 12; (b) **50% Armory** → cruise, 0 leaks/0 core lost; (c) **100% Armory** (real maxed, threat 1) → cruise, 0 leaks/0 core lost. **So Hard is trivial at ≥50% Armory** — confirms the assessment that `THREAT_HP 0.85` doesn't bite a maxed account on Hard (→ V6.2 lowers threat *and* moves difficulty into the tiers). **Aggression was still free in all three**: 17–18 early sends, 6–11 overlaps, 150–200-hostile piles, scrap ballooning to 8k+ with 0 risk — exactly the pattern V6.3 now taxes (rush buff) and caps (manual congestion block). Re-test with a play-log after V6.3 to confirm early-send counts fall and surplus shrinks.
- **Iter — V6.5 Economy taper + Railgun/slow rebalance. ✅ SHIPPED.** (1) **Kill-reward taper**: `earnTaper() = wave ≤ 12 ? 1 : max(0.5, 1 − (wave−12)·0.05)` multiplies per-kill scrap in `hurt()` — flat through wave 12, then fades to ×0.6 by wave 20, floored ×0.5, so the wave-15+ scrap explosion (5.9–6.9k surplus in play-log #4) is capped. (2) **Railgun t4 nerf**: Annihilator dmg 340→280, cost 240→275. (3) **Shorter frost conveyor**: `SLOW_ASYMPTOTE` 0.28→0.34 (two-tower stack now leaves 0.379 speed vs 0.358; single-hit unchanged at 0.40). (4) **Insulator sooner + harder**: first appears wave 9 (was 10), cap 5→6, `energyResist` 0.6→0.65 (takes ~35% from energy). (5) **Alloy faucet ÷1.5** so the Armory fills over weeks, not an hour. `earnTaper`/`awardAlloy` exported on `__GAME`. Verified (`verify65.py` + regression): taper curve 1/1/0.95/0.85/0.6/0.5, Railgun DMG 280 in codex (340 gone), insulator w9 + energyResist 0.65, alloy ×(1/1.5)=333 for a 500-base normal win; frost single-hit 0.40 / stack 0.379; 0 page errors. **Deferred:** the *extra plain upgrade tier before L3 specialisation* was pulled OUT of this iter — it's a structural change (tower level array, branch-gate level, upgrade UI, codex L1→L4 tables, drawTowerArt lvl handling, all branch-at-L3 tests) and gets its own iteration **V6.6**.
**Play-log #5 (2026-07-17, 4 dev-toggle Hard runs — validates V6.4/V6.5).** All Hard on Outpost/Serpentine at 0%/50%/100% Armory. (Record `ver` still says "6.1.1" — a **stale telemetry label**, fixed in V6.7 — but the economy proves it's a V6.5 build.) **Economy taper works:** finalScrap now **1.6–3.5k** (was 5–8k pre-taper); alloy **~1.0–1.9k**/run (was 2.7–4.5k → the ÷1.5 nerf bites). **Hard @ 0% Armory is now genuinely hard** (V6.4 floor 1.7→2.0): core dropped to **4** (Outpost) and **11** (Serpentine). **Hard @ 100% still cruises** (0 leaks) — fine, Brutal is the challenge tier. Builds diverse (mortar/cryo/tesla mixes), hero used (lvl 5 in 3/4). Net: V6.4+V6.5 confirmed; the deferred extra-upgrade-tier looks **unnecessary now** (surplus already healthy at 1.6–3.5k).

- **Iter — V6.7 Campaign Mastery Medals (plan C, a+b). ✅ SHIPPED.** Campaign now has an explicit **per-mission difficulty choice** and a **medal per mission**. **Medals:** 🥉 Bronze = cleared on Normal, 🥈 Silver = Hard, 🥇 Gold = Brutal (keep highest); a **perfect (no-Core-lost) clear adds a ✦ mark** (`Medals` stores `{t: bestTier, p: bestPerfectTier}` per mission in Store key `medals`). **New Campaign screen** (`S.screen==="campaign"`): scrollable list of all 14 missions with their medal/lock (soft-gated by `camp`, mission N locked until N−1 cleared), a selected-mission detail card with the **3-tier difficulty picker** (Normal/Hard/Brutal — no Custom; auto-falls-back if Custom was set), the V6.4 Armory hint, and Deploy. The menu `cp` button opens this screen (+ shows 🥇count) instead of jumping straight into a run. **Award** (victory branch, guarded once-per-run by `S.medalChecked`, recorded *before* `runAchCheck` so completion achievements fire same-run): `Medals.record`, **first-medal alloy bonus** +100/+250/+500 for a new Bronze/Silver/Gold, and a victory-panel message (medal earned / "Perfect!" / best-kept). **Achievements:** Gold Standard (first gold), Silver Grid (all missions ≥ silver), Grid Master (all gold), Untarnished (all perfect gold). **Also fixed the stale telemetry `ver` "6.1.1"→"6.7".** Renamed `fallengrid-v6.7.html`. Verified (`verify67.py` + regression, which is now updated for the campaign-screen deploy flow): screen+picker (no custom), soft gating (fresh = only mission 1), deploy starts campaign at chosen diff/map, Brutal+perfect → Gold ✦ + 500 alloy + camp advance, once-guard (no double-award on re-render), no-downgrade, perfect-mark progression (silver→perfect silver), first-gold achievement same-run, telemetry ver 6.7, Back→menu; 0 page errors.
- **Iter — V6.7 Campaign Mastery Medals (plan C — user-chosen). ~~PLANNED~~ (see SHIPPED entry above; design notes retained).** Give the campaign an explicit **per-mission difficulty choice** + a **medal per mission**, so player *skill* (not just Armory) is challenged and rewarded with replay value. Fixes the V6.6 gap where campaign silently inherited whatever difficulty Free Play last set.
  - **Medals = difficulty mastery:** 🥉 Bronze = cleared on Normal, 🥈 Silver = Hard, 🥇 Gold = Brutal. Keep the highest ever earned per mission. (Medal emojis, distinct from the `★` map-difficulty display at L6384.)
  - **Data model:** new Store key `medals` = `{ [mapIndex]: tier }` (tier 1/2/3), JSON-encoded via `Store` (L3997). Keep `camp` as the unlock frontier so the V6.6 free-play gating (L6381) stays intact. Unlock the next mission on an **any-difficulty** clear (no double-punish).
  - **New Campaign screen** (`S.screen === "campaign"`, new branch in the render dispatch, L6353/L6379 region): a list of all 14 missions — map name + earned medal (or empty) + lock state (locked = greyed until the previous mission is cleared). The menu `cp` handler (L6370) opens this screen instead of jumping straight to `"playing"`. Tapping an unlocked mission shows the **3-tier difficulty picker** (Normal/Hard/Brutal — no Custom in campaign, to keep medals clean) + current medal + the V6.4 Armory hint, then Deploy.
  - **Medal award** at the model victory hook (L4845, has `S.campaign`/`S.mapIndex`/`S.difficulty` in scope) or beside the `camp` write (L6527): `tier = {normal:1,hard:2,brutal:3}[S.difficulty]; medals[i] = max(medals[i]||0, tier); camp = max(camp, i+1)`.
  - **Rewards:** higher tier already pays more alloy (`diffCfg().alloy` hard 1.5 / brutal 2.0, L4624); add a one-time **first-medal bonus** per tier per mission (e.g. +100/+250/+500 alloy) to pull players up the tiers.
  - **Achievements** (ACH at L4569, ctx built at L4614): add `firstGold`, `allSilver` (every mission ≥ silver), `allGold`; feed `goldCount`/`minMedal` into the `runAchCheck` ctx from the medals store.
  - **Victory screen** (L6524) shows the medal earned + a "medal upgraded!" flourish when it beats the prior best; the menu `cp` label shows medal progress (e.g. "⚔ Campaign · 🥇6 🥈2").
  - **Also fix the stale telemetry `ver: "6.1.1"` (L4662)** → real version, and add the medal/mapIdx to the record (play-logs currently mislabel every post-V6.1.1 run).
  - **Suggested split:** **V6.7a** = data model + medal award + Campaign mission-select screen + difficulty picker + unlock (core feature); **V6.7b** = medal achievements + first-medal alloy bonus + victory feedback + menu label + telemetry `ver` fix (polish). Each with its own verify + regression.
- **Iter — V6.8 Logging toggle + locked-map text + Codex story. ✅ SHIPPED (user-requested during V6.7 testing).** (1) **Play Log moved to Settings** with an on/off **toggle** — new `DEV_BUILD` flag (currently `true`; **set false for the Play release**) drives the default: `Telemetry.enabled = Store.get("logEnabled", DEV_BUILD)`, so logging defaults **on in dev, off in production**; `begin()` early-returns (records nothing) while disabled; removed the menu Play Log button; Settings has "Logging On/Off" + "View" (log Back now returns to Settings). (2) **Locked-map feedback:** campaign mission rows are no longer inert `disabled` buttons — a persistent hint ("🔒 missions unlock as you clear the campaign in order") sits under the list, and tapping a locked mission sets a specific message ("🔒 Mission N is locked — clear Mission M first."), cleared when picking an unlocked one or reopening. (3) **Codex Story tab:** new first tab with the game's lore/briefing (THE GRID HAS FALLEN + YOUR MISSION). Renamed `fallengrid-v6.8.html`. Verified (`verify68.py` + regression): no menu log button, dev-default on, toggle flips+persists+gates `begin()`, log viewer from Settings + Back→Settings, locked-tap message + persistent hint, Story tab content; 0 page errors.
- **Iter — V6.9 Buildable obstacles. ✅ SHIPPED (user-requested).** Rubble obstacles sit on buildable ground and **block tower placement**, breaking up dense tower walls. **Count scales with difficulty** — `OBST_BY_DIFF = {normal:2, hard:4, brutal:7}` (custom/other → 3), capped at 22% of buildable tiles so a map is never walled off. **Seeded per (map, difficulty)** so it's deterministic, **biased to path-adjacent tiles** (where players cram towers) and never on the path or the tutorial plot. Implementation: new `BLOCKED_SET`; `buildableBase()` (in-bounds, not path) vs `buildable()` (also not blocked); `computeObstacles()` runs in `reset()` so obstacles re-seed every run and follow difficulty changes (no 3D rebuild needed). Rendered as a rubble/rock decal in the UI-canvas overlay via `worldToScreen` (drawn right after `G3D.overlay()`), so no 3D mesh work. Tapping a blocked tile gives err SFX + haptic + a "⛔ OBSTRUCTED" banner. Telemetry records `obstacles`. Exposed `computeObstacles`/`buildable`/`blocked` on `__GAME`. Renamed `fallengrid-v6.9.html`. Verified (`verify69.py` + regression): counts 2/4/7 and scale, deterministic per map+difficulty (differ across maps), never on path/tutorial plot, blocked tile not buildable + `build()` refuses it while a free tile works, obstacles re-seed to 2 on a Normal reset, telemetry count; 0 page errors. **Balance note:** counts are a starting point — tune from the next play-log (does Brutal 7 feel fair on small maps?).
- **Iter — V6.10 Early-send rebalance (dead gap + reward/risk). ✅ SHIPPED (user-requested).** The send-early lever was too rewarding (≈125 scrap at wave 10 for one tap, easily offsetting the +35% rush) and there was dead downtime waiting for a cleared field's timer. Fix = **collapse the gap when clear + shrink both bonus and penalty** (user's chosen combo): (1) **`gapRate()` fast-forwards the between-wave countdown ×5 when the field is fully clear** (`enemies.length===0 && S.queue.length===0`), applied at both auto-launch sites (real loop + headless sim) — a cleared 20s gap collapses to ~4s, so no dead staring; while still fighting it ticks 1×. (2) **Early-send bonus cut ~76%:** coefficient `(2 + min(wave,15)·0.5)` → `(0.5 + min(wave,15)·0.1)` (wave-10 full-timer ≈ 30 scrap, was ≈125). (3) **Rush penalty cut:** `RUSH_HP` 0.35→0.15, `RUSH_SPD` 0.10→0.05 (full-timer send now +15% HP / +5% spd). Net: early-send is a **low-stakes tempo choice**, not an exploit, and there's no boring wait. Renamed `fallengrid-v6.10.html`. Verified (`verify610.py` + regression, anti-snowball expectation updated for the new coefficients): wave-10 bonus far below old, rush ≈×1.13, cleared field burns ~5s of gap per 1s real (fast-forward) while a busy field holds at 1×; 0 page errors.
- **Iter — V6.11 Early-send: true parallel overlap + no penalty. ✅ SHIPPED (user-requested, from play-log #6).** Play-log #6 showed a wave-19 snapshot of **271 "hostiles"** — mostly **queued**, not on the field: sending waves early piled them into the queue and they trickled out **sequentially**. Root cause: `updSpawns` spawned **FIFO front-only** (`while (S.queue[0].t <= 0)`), so a wave appended behind a still-spawning wave was blocked until the front drained. **Fix (1):** spawn **every** entry whose `t<=0` (filter, not front-check), so an early-sent wave overlaps the current one **in parallel** — a denser combined field. Single waves still spawn staggered (their entries carry increasing delays). **Fix (2):** removed the rush penalty (`RUSH_HP`/`RUSH_SPD` → 0) per the user — the cost of sending early is now the **denser overlapping wave** (more targets at once), not tougher enemies; the DEPLOY-button `⚠%` warning auto-hides at 0. `CONGEST_CAP` (45, gates auto + manual sends) keeps the parallel pile bounded. Renamed `fallengrid-v6.11.html`. Verified (`verify611b.py` + regression): a ready entry behind a not-ready one spawns (FIFO would spawn 0), single-wave stagger intact, early-sent wave adds field spawns immediately, full-timer send HP == auto-launch HP (no penalty); 0 page errors. *Note: telemetry `ver` still emits "6.7" — the string was only bumped in V6.7; bump it whenever the next code change touches Telemetry.begin.*
- **Iter — V6.12 Sell confirmation + late-game HP accelerator. ✅ SHIPPED (user-requested).** (1) **Sell now needs confirmation** — the tray Sell button arms a `pending: {kind:"sell"}` and a compact `drawSellConfirm()` shows the tower + refund ("Refund ⚙X · cannot be undone") with ✓ SELL / ✗; the actual removal happens in `doPending` on confirm. No more mis-tap sells. (2) **Late-game HP accelerator** to soak the persistent second-half scrap surplus (play-logs: 3.8–5.7k unspent on Normal wins): in `statsFor`, `hpMul += 0.04·lateW²/diffCfg().hp` where `lateW = max(0, wave-1-11)` (kicks in from **wave 13**). The **÷diffCfg().hp** is deliberate: after `diffMul` re-multiplies by `diffCfg().hp`, the bump becomes a **flat absolute HP amount**, so it's a big % on Normal (raider wave-19 ~263→330, +25% → forces continued building to keep DPS up) but a small % on Hard/Brutal (already stretched — Normal late-HP ratio 2.11× vs Brutal 1.86× w13→w19; Brutal still far tougher in absolute HP). Bumped telemetry `ver` 6.7→**6.12**. Renamed `fallengrid-v6.12.html`. Verified (`verify612.py` + regression): sell arms pending (tower NOT removed), confirm removes+refunds, cancel keeps; Normal wave-19 raider ~330, Normal climbs proportionally more than Brutal, Brutal still >2× absolute; telemetry ver 6.12; maxed-diverse still wins. **Balance note:** coefficient 0.04 is a starting point — from the next play-log, raise it if Normal still has a big surplus, or check Brutal isn't over-tightened.
- **Iter — V6.13 Late-game scrap sinks (Overclock + Excavate) + per-wave spend log. ✅ SHIPPED (user-requested).** Two sinks to soak the late-game surplus, plus richer logging to measure it. (1) **Overclock:** a maxed (L4) tower's menu button becomes **⚡ OVERCLOCK** — a repeatable **+10%-damage** buy (`OC_DMG 0.10`, applied in `tStats` via a copy-on-overclock so shared data isn't mutated) at a **rising cost** `overclockCost = 200·1.5^oc` (200/300/450/675…, no hard cap → infinite sink; keeps the "build stronger" loop alive past max). Instant purchase (no confirm), `t.oc` shown on the tower menu title (`⚡N`) + a `⚡OC` float. (2) **Excavate:** tapping a rubble obstacle now opens an **⛏ EXCAVATE** confirm bar (was just an OBSTRUCTED banner) — pay `excavateCost = 140·1.4^excavated` (140/196/274…) to clear the tile and open a build plot (`BLOCKED_SET.delete`, `S.excavated++`). Closes the loop with V6.9: obstacles are now a sink **and** a way to buy build room. **More obstacles** to match: `OBST_BY_DIFF` 2/4/7 → **3/6/9** (22% cap unchanged). (3) **Per-wave log** now records `spent` (cumulative scrapSpent), `maxed` (L4-tower count) and `oc` (total overclocks) in each `waveSnap`, so a play-log distinguishes "many un-upgraded L1s" from "few fully-upgraded/overclocked towers" and shows exactly when spending stalls. `tStats`/`overclockCost`/`excavateCost`/`worldToScreen` exported on `__GAME`. Renamed `fallengrid-v6.13.html`. Verified (`verify613.py` + regression): obstacle counts 3/6/9; overclock +~10% dmg, cost 200→300, only on maxed towers (non-maxed shows UPGRADE); excavate arms on blocked-tile tap → confirm clears it to buildable, cost 140→196; per-wave log has spent/maxed/oc; 0 page errors.
- **Iter — V6.14 Map difficulty overhaul: geometry-graded roster (14 → 18 maps). ✅ SHIPPED (user-requested).** The complaint: *every* map was "optimal for defense" — long snaking paths where one tower re-hits an enemy 2–3× — so the ★ rating was cosmetic and geometry never got harder. **Investigation (why a map is intrinsically hard):** defender's total damage ≈ **(path length in tiles) × (tower coverage density along the path)**. TILE=64, tower ranges ~130–260 = **2–4 tiles radius**, and the old serpentines spaced lanes **3 tiles apart** → a crook tower reaches 2–3 parallel lanes = the multi-hit. **Levers (geometry only, beyond more gates):** L1 **path directness** (pathTiles ÷ Manhattan: 3–4× winding = easy → ~1.0–1.2× near-straight = hard), L2 **lane spacing** (≥4 or no parallels kills multi-hit), L3 **edge-hug** (path on the border → only one side buildable → coverage halved), L4 **sprint lanes** (long straights → short dwell, fast foes blitz), L5 **gates + late convergence** (routes that merge only at the core split fire), L6 **buildable density / open dead-space**, L7 **chokepoint at core, not entrance**. **Plan = 4 geometry tiers** and the whole `MAPS` array was rewritten in ascending-difficulty index order: **Tier A ★1-2 starters** (4: Outpost, Serpentine, Spiral, Switchback — winding, multi-hit friendly, unchanged); **Tier B ★3** (4: Crossroads, Gauntlet, + NEW **Causeway** edge-hug sprint + winding gate, + NEW **Twin Reservoir** mirrored gates converging mid); **Tier C ★4** (6: Delta, Fracture, Crucible, **Sprawl** rebuilt into two direct L-lanes that cross once + merge late, + NEW **Highway** near-straight vertical sprint + late side-gate, + NEW **Pincer** two near-edge sprints + centre feed + big unusable open middle); **Tier D ★5** (4: Twin Gates, **Meltline** rebuilt into two 16-tile straight sprints + centre feed splitting fire 3 ways, Terminus, The Maw). Net: **+4 new** (Causeway, Twin Reservoir, Highway, Pincer), **2 rebuilt** (Sprawl, Meltline — both were redundant single serpentines), **12 kept**. Directness metric now grades cleanly: starters 2.4–8.5 (winding), Tier C/D ~1.0–1.1 (near-straight). No code paths hardcoded 14 — `MAPS.length` is used everywhere (campaign completion, medals, achievements, daily `%MAPS.length`), campaign/free-play lists already scroll, so 18 "just works". Bumped version label + telemetry `ver` 6.12→**6.14**. Renamed `fallengrid-v6.14.html`. Verified (Node geometry validator: all 18 have edge entrances, collinear in-bounds segments, a shared base, props off-path, sane 66–85% buildable; `verify614.py`: all 18 load + spawn + path to core with build room, 0 page errors; core regression suite green). **Note on saves:** reordering changed map indices, so pre-V6.14 medals/`camp` progress key to different maps — acceptable pre-release (single tester), but a real launch would need an index migration. **Balance note:** geometry is now the difficulty lever the user asked for; from the next play-log, confirm the ★4/★5 maps actually feel harder and adjust individual routes (straighter / fewer plots near path) rather than touching enemy HP.
- **Iter — V6.15 Dev Map Unlock toggle + saved pre-release analysis. ✅ SHIPPED (user-requested).** (1) **Settings → "Dev · Map Unlock"**: a toggle (`Store` key `devUnlock`) that unlocks all 18 maps for testing. Implemented by making all three `campProg` gate computations (menu label, free-play lock/clamp, campaign lock) return `MAPS.length-1` while the toggle is on. Deliberately does **NOT** touch the real `camp` progress: achievements (`campaignDone` reads `Store camp` directly) stay honest, and toggling off relocks exactly where the player actually was. (2) **`docs/ANALYSIS-prerelease.md`** — the critical pre-release game analysis (positives/negatives/priorities), saved per user request to be revisited when switching from TEST to RELEASE; includes the **release checklist** of test-only UI to strip (DEV_BUILD=false, Dev Armory Override, this Dev Map Unlock, Play-Log section, `__GAME` exports, save-index migration). Bumped label + telemetry `ver` → **6.15**. Renamed `fallengrid-v6.15.html`. Verified (`verify615.py` + core regression): fresh save 17/18 locked → toggle unlocks free-play (0 disabled options) + campaign mission 18 selectable → toggle off relocks; `camp` stays 0 throughout; 0 page errors.
- **Iter — V6.16 Roster → 25 maps: low-overlap geometry + authored obstacles. ✅ SHIPPED (user-requested, from play-log #8).** Play-log #8 (Highway/Pincer/Meltline, Normal, dev-armory 50%) confirmed V6.14's thesis: the direct maps forced real spending (Pincer 8080 spent / 973 left / 3 leaks vs Highway 3316 left) and the user "liked having to separate towers" — but the boards also showed the next weakness: **every tower still clustered on the pre-core merge zone**. Two changes: (1) **Authored obstacles** — new optional map field `blocks: [[c,r],…]`; `computeObstacles()` seeds these first (every difficulty, skips path/TUT_PLOT tiles defensively), then adds the random per-difficulty rubble on top (candidates exclude already-blocked tiles; cap = authored + min(want, 22% of open ground)). They render + excavate exactly like random rubble (same `BLOCKED_SET`), so they're also a scrap sink where it hurts. (2) **+7 new maps** (18 → 25), all designed to shrink per-tower road coverage ("small area of road attacked") and to rubble-squat the prime camp plots: **Tier B** Rake (serpentine with lanes 5 apart — beyond double-coverage range — rubble in every crook), Rimline (road rings the outer border; every tower sees one short arc; big dead center); **Tier C** Horseshoe (edge-hug U, legs 9 cols apart, zero shared coverage), Shattered Field (two direct lanes + 8-block rubble field), Terraces (wide staircase, rubble on every inside corner); **Tier D** Blockade (three near-straight gates, the whole junction camp zone is authored rubble — excavate it or defend from the second row), Ground Zero (core in the CENTER, four short gates from N/S/E/W ~10-13 tiles each, the four tightest ring plots rubbled; no perpendicular crossing at the core so no odd bridge render). Roster now A4/B6/C9/D6 sorted ascending. Bumped label + telemetry `ver` → **6.16**. Renamed `fallengrid-v6.16.html`. Verified (Node validator incl. new block checks: in-bounds, never on path, no dupes, not TUT_PLOT; `verify616.py`: 25 maps in free-play, map-0 random counts still 3/6/9, Ground Zero authored ⊆ BLOCKED_SET on all difficulties (7/10/13 total), authored block tap→excavate→buildable works, all 25 smoke-run; screenshots of Ground Zero/Blockade/Rimline render correctly; core regression green; 0 page errors). **Test-tip:** the RAF camera needs ~300 ms after `reset()` before `worldToScreen` matches the tap projection — pattern now in `verify616.py`.
- **Iter — V6.17 Armory rework: Threat retired, the wall lives in the world, economy ×5. ✅ SHIPPED (user-requested, decisions: no residual threat, no recommended-Armory-% UI, guide notes Armory as the lever for hard maps).** Analysis (measured, V6.16): (1) *treadmill* — identical 8-tower board on Outpost/Normal scored core 20 at 0% Armory, 30 at 50%, **29 at 100%** — the second half of the Armory bought nothing because Threat (+85% HP + a live +12% speed tax in `updSpawns` via `threatSpdMul`) cancelled the combat talents (~+66%); (2) *economy 10× too fast* — all 14 talents cost 5,140 Alloy total vs ~550-750 per Normal win ⇒ maxed in ~9 wins. Changes: **(A)** Threat deleted outright — `THREAT_HP/SPD`, `threatLevel/threatHpMul/threatSpdMul`, the HUD ⚠ readout, all menu/freeplay/daily banners, the alloy `(1+threat·0.4)` bonus, and the speed tax; replaced by a passive `armoryLevel()` used only for telemetry (`armory` + `mapHp` fields replace `threat`). Both "tuned for ~X%+ Armory" recommendation blocks (freeplay + campaign) removed per user. **(B)** The wall moved into the world: `MAP_HP_BY_DIFF = {1:1, 2:1, 3:1.15, 4:1.35, 5:1.6}` — a map's ★ rating is now mechanical (`mapHpMul` in `diffMul`; optional per-map `hpMul` override supported), so ★4/★5 sectors genuinely need Armory progression. **(C)** All 14 talent cost curves ×5 (total 25,700 Alloy ≈ 45+ Normal wins / ~25 Hard — a real progression to monetise later via V7's Double-Alloy rewarded ad + IAP alloy packs; deeper 5-level talents deferred). Guidance updated (no % hints anywhere, per user): Codex Guide bullet "Higher-★ sectors field genuinely tougher machines. The Armory is your lever...", Armory screen sub "your edge on the tougher maps", story fixed to twenty-five sectors. Renamed `fallengrid-v6.17.html`, label + telemetry ver → **6.17**. Verified (`verify617.py`): tier walls exact (raider w10 HP 113/130/153/181 = ×1/1.15/1.35/1.6); same fixed board now **monotonic** in Armory (Outpost dev0 core 20 → dev1 core 35; Highway 12-tower dev1 ≥ dev0); Reserves L1 costs 200; menu/freeplay/campaign/daily HTML free of Threat + recommendations; guide/armory/story notes present; telemetry `armory`+`mapHp`, no `threat`; core regression green; 0 page errors. **Balance watch:** Normal Tier-D at ×1.6 HP may now be genuinely hard at 0% Armory — that's the intent, but confirm with the next play-log that Tier A-B/Normal still onboards a fresh account comfortably.
- **Iter — V6.18 Wide-screen layout (tablets + landscape phones) + external AI review triaged. ✅ SHIPPED (user-requested).** A second-opinion AI review (pasted by the user) was verified against the actual code before acting: 4 of its 6 recommendations were **already implemented or obsolete** — object pooling exists (beamPool/orbPool/shellPool/boomPool/flashPool/scorchPool with `.visible` toggling + per-type `enemyPool` reuse), balance is already fully data-driven (TOWERS/ENEMIES/TALENTS/DIFFS/MAPS tables drive the UI), haptics already exceed the suggestion (Capacitor Haptics impact styles + navigator.vibrate fallback + error patterns + Settings toggle, V4.6), and the 300 ms touch-delay/pointerdown advice is obsolete in modern WebViews. Save-encryption = theater (skipped). Mid-run save/resume was proposed but the **user declined** (a crashed run is simply replayed; avoids localStorage churn) — do not resurrect without being asked. The one accepted item: **tablet/wide layout**. Added two CSS media queries at the end of the stylesheet (so they win the cascade): `min-width:600px` widens `.panel` to min(520px,86vw) (covers tablets AND landscape phones), and `min-width:700px and min-height:700px` (true tablets) widens to 560px and scales type (h1 42px, h2 26px, btn 16px, btn.small 13px). Discovery while testing: the game canvas already **letterboxes** its portrait column when the window is wider than tall, so landscape phones are playable (menus wide, board centered) with no extra work. Renamed `fallengrid-v6.18.html`, label + telemetry ver → **6.18**. Verified across three viewports (360×720 panel 320px/h1 34px unchanged; 800×1280 panel 560px/h1 42px; 740×360 panel 520px/h1 34px; screenshots of menu + in-game all three; 0 page errors; core regression green).
- **Iter — V6.19 Dev "Reset All" (fresh-start button). ✅ SHIPPED (user-requested).** Settings gains a **Dev · Fresh Start → \u{1F5D1} RESET ALL** button (red-tinted, below the Armory Override row) so the developer can replay the true first-time-player experience without clearing browser storage by hand. On tap it `confirm()`s, then `localStorage.clear()` (guarded) and `location.reload()` — the reload drops the in-memory `Mem` fallback too, so it also resets in sandboxed WebViews where localStorage throws. Wipes everything: Alloy, talents, medals, campaign progress, achievements, high scores, all settings, `devUnlock`, `devArmory`, logs. This directly serves the outstanding "test as a new player" need (the #1 open question is the fresh-account onboarding curve). On the release checklist to remove alongside the other Dev toggles. Renamed `fallengrid-v6.19.html`, label + telemetry ver \u2192 **6.19**. Verified (Playwright): a dirtied account (alloy 9999, camp 10, gold medals, achievements, devUnlock on, music off) \u2192 tap RESET ALL \u2192 all keys cleared, `Meta.alloy` back to 0 in memory, menu reboots, Free Play shows 24/25 maps locked again (true fresh account); core regression green; 0 page errors.
- **Iter — V6.20 New-player UX: visible Settings toggles + tutorial locked to the Auto-Gun. ✅ SHIPPED (user-requested, from the first true new-player log).** Play-log #12 was a fresh account (armory 0\u21920.14) clearing Outpost\u2192Gauntlet in campaign, all 20/20 core, 0 leaks \u2014 confirming the onboarding curve **fits** (user: "the level seems to fit"). Two UX fixes from that session: **(1) Settings on/off toggles were nearly invisible** (all `btn small ghost`, only a \u25c9/\u25cb glyph distinguishing state). Now the ON state uses the bright cyan **`active`** button style (Music/SFX/Haptics/Battery/Logging), so on vs off is unmistakable at a glance. **(2) The first-build tutorial could soft-lock:** it said "build a tower, then upgrade," but a new player built a **Tesla** (95 of 150 start scrap) and had nothing left to upgrade \u2014 stuck. Fix = **lock the first build to the Auto-Gun** (50 build + 45 upgrade = 95 \u2264 150, so the upgrade step is always affordable): during `S.tut===0` the tray palette marks every non-turret tower `banned` (dimmed, tap rejected, no "OFFLINE" text), and the Auto-Gun cell gets a pulsing cyan highlight ring. Message renamed "TAP THE PLOT, THEN PICK AUTO-GUN", the **SKIP** button enlarged + given a cyan border and bright text, and the instruction banner moved below SKIP so they no longer overlap. On the release checklist: the tutorial lock is intended to stay (good onboarding), only the Dev toggles get stripped. Renamed `fallengrid-v6.20.html`, label + telemetry ver \u2192 **6.20**. Verified (Playwright): in a tut-0 campaign run the build palette has turret buildable + cryo/mortar/tesla/pyre all `banned:true`, tapping a locked tower arms no build while Auto-Gun does; screenshots confirm the bright toggles, the highlighted gun, and the non-overlapping SKIP/banner; core regression green; 0 page errors.
- **Iter — V6.21 Campaign progress no longer rolls back on replay. ✅ SHIPPED (user-reported bug).** Replaying an already-cleared campaign mission reset progress down to that map: the victory handler wrote `Store.set("camp", min(mapIndex+1, MAPS.length-1))` unconditionally, so clearing mission 2 while at mission 7 set camp back to 2. Fix = never decrease: `Store.set("camp", min(max(Store.get("camp",0), mapIndex+1), MAPS.length-1))`. Renamed `fallengrid-v6.21.html`, label + ver \u2192 **6.21**. Verified (Playwright): at camp 6, winning an earlier mission (index 1) keeps camp 6; winning the next mission (index 6) still advances to 7; core regression green.
- **Iter — V7.0 Android APK pipeline (Capacitor + GitHub Actions, sideload; no ads yet). ✅ SHIPPED (user-requested — "get a working APK I can install", ads deferred).** Wraps the single-file game in Capacitor and builds an installable **debug APK** entirely in CI (no local Android SDK needed). Files: `app/package.json` (@capacitor core/cli/android + **haptics** + **app**, the two plugins the game references), `app/capacitor.config.json` (appId `dk.lundjacobsen.fallengrid`, name "Fallen Grid", webDir `www`), `app/keystore/debug.keystore` (a **committed stable debug key**, alias `androiddebugkey` / pass `android` — matches AGP's default debug signingConfig so builds sign with it → clean in-place updates that **preserve save data**), `app/README.md` (install + update steps), `app/.gitignore` (node_modules/android/www generated), and `.github/workflows/android.yml`. The workflow (push to the dev branch touching `app/**` or `fallengrid-v*.html`, or manual dispatch): setup Node 20 + JDK 17 + Android SDK (platform-34, build-tools 34) → `npm install` → **bundle the newest `fallengrid-v*.html` as `www/index.html`** → `cap add android` → `cap sync` → copy the stable keystore to `~/.android/debug.keystore` → `gradlew assembleDebug` → upload `fallengrid-vX.YZ.apk` as an artifact. So updating the game = push → download new APK → tap to install over the old one (progress kept). Ads (V7's AdMob/UMP) intentionally deferred to a later version. Dev toggles intentionally KEPT in this test build (per the earlier decision) for on-device testing; they come out before any public release (`docs/ANALYSIS-prerelease.md`). Target test device: Samsung Galaxy S25+ (flagship → no low-end graphics fallback needed). Config validated locally (JSON/YAML parse, keystore alias/password correct); the APK itself builds in CI (no Android SDK in the dev container).
- **Iter — V6.22 Audio overhaul: procedural reverb + layered SFX + fuller music. ✅ SHIPPED (user-requested — "sounds are simple and outdated").** The old Sound module was single-oscillator SFX through a compressor: dry, thin, beepy. Rebuilt the whole procedural synth (still **zero asset files**, so the offline APK is unchanged in size/scope): **(1) a procedural convolution reverb bus** — a decaying-noise impulse (1.7s) on a parallel wet send, plus a 12 kHz master tone-shave — gives everything space/depth instead of dry blips. **(2) Layered SFX** — each sound is now transient + body + sub with ADSR-ish envelopes, optional pitch/filter sweeps, per-shot **stereo pan** (rapid fire feels wide), and per-sound reverb sends: punchier `gun` (click+body+thump), electric detuned-saw `tesla` with crackle, crystalline FM-ish `cryo` bell, a much bigger `boom` (crack + body + sub sweep + rumble tail — the combat centrepiece), satisfying `place` chunk, 3-note `up` arpeggio, `strike` = descending whistle → boom, brass-stab `wave`, and fuller `win`/`lose` chord stingers. **(3) Music bed** — a detuned A-minor pad (4 notes × ±7 cent pairs) under a slow LFO filter, a sub pulse with tremolo, the existing wind, and a **sparse reverb-soaked pentatonic bell** every ~7 s for evolving melodic interest; all cleaned up correctly on stop (clearInterval + ramped fade). Public `Sound` API is byte-for-byte the same (gun/tesla/cryo/boom/hit/place/up/sell/repair/err/leak/shield/strike/wave/win/lose/startMusic/stopMusic/resume/suspend/toggle*+*On), so every call site is untouched. Renamed `fallengrid-v6.22.html`, label + ver \u2192 **6.22**. Verified (Playwright with autoplay enabled): a full session — enter play (builds reverb bus + music), build, upgrade, start wave, 400 combat frames (gun/tesla/boom/hit/leak fire), gameover stinger, music stop→restart — throws **0 page errors**; AudioContext + convolver graph construct without exception; core regression green. **On-device note:** the S25+ will handle the extra voices trivially; if a low-end phone ever crackles under heavy fire, the lever is a voice cap or routing SFX dry in Battery mode (not needed now).
- **Iter — V6.23 Punchier weapon SFX (waveshaper drive) + confirmed campaign-replay fix covers higher difficulty. ✅ SHIPPED (user).** (1) The auto-gun still sounded thin/8-bit. Added an optional **`drive` waveshaper** (cached tanh soft-clip curve, 2x oversample) to `osc()`, giving harmonic grit. Rebuilt `gun` as crack (highpass noise, falling cutoff) + a **filter-swept, distorted, detuned square+saw body** + a sub-sine thump — reads punchy and full, not beepy, while staying 3-4 short voices so rapid fire is still cheap. `tesla` saws got the same drive for electric grit. (2) Confirmed for the user that **V6.21's campaign fix already covers the higher-difficulty replay case**: `Store.set("camp", max(stored, mapIndex+1))` is the only camp write on victory and is difficulty-agnostic, so replaying an earlier mission on Hard/Brutal cannot re-lock later maps. Renamed `fallengrid-v6.23.html`, label + ver \u2192 **6.23**. Verified (Playwright): camp=10 → win mission 3 (index 2) on **brutal** → camp stays 10 with 14/25 maps correctly locked (not re-locked to the replayed map); heavy combat session (turret+tesla+mortar, 500 frames) with drive enabled throws 0 page errors; core regression green. **Caveat for the user:** the fix prevents FUTURE rollback but can't restore progress a pre-V6.21 build already rolled back — re-clear the next mission once, or use Settings → Dev Map Unlock.
- **Iter — V6.24 Research Tree Phase 1: Armory → 4-branch fog tree + tower gating + free/reversible fork. ✅ SHIPPED (user-requested; HTML test build only, NO APK per user).** Replaced the flat 14-talent Armory with a **4-branch research tree** (`TECH` = Ordnance/Arc/Logistics/Command, 32 nodes total, 8/branch), designed + specced in `docs/TECHTREE-spec.md` and pitched interactively (artifact). **Data/model:** `TALENTS`/`TALENT_ORDER`/old `Meta` deleted; `Meta` is now a thin Alloy wallet, the tree lives in `Tech` (`research` = {branchId:count}, `spec` = {branchId:"a"|"b"}, `bonus` = aggregated stat map, `unlockedTowers` Set). Each node has `{type, name, desc, cost, eff}` where `eff` is a stat delta (dmg/rate/range/splash/scrap/core/reward/waveb/sell/alloyf/hero/hrate/strike/pulse — the same 14 effects, redistributed) or `{tower}` unlock; `Tech.recompute()` sums owned-node effects into `Tech.bonus`, which the 14 `*Mul()` getters now read via `B()` (all repointed off `Meta.val`). **Gating:** new accounts start with **Auto-Gun + Cryo only** (`TOWER_START`); Mortar/Tesla/Pyre are `unlock` nodes; the build tray marks locked towers `🔒 ARMORY` (folded into `banned`), with a defensive `Tech.isUnlocked` guard in `doPending`. Tower *upgrades* (in-run a/b forks) are unchanged and NOT gated. **Free reversible fork (D2):** each branch has one `fork` node; Alloy buys *reaching* it, but the A/B pick is a no-cost, re-swappable toggle (`Tech.setSpec`, stored in `spec`) — try combinations without extra cost. **Capstones (D3):** Ordnance→Siege Battery, Arc→Prism are placeholder *effects* for now (the two NEW towers are a later phase); Logistics/Command capstones are effects. **UI:** the Armory screen is a full rewrite — an **overview-first accordion** (4 branch cards: icon, theme, progress bar, "next milestone" breadcrumb, one-tap RESEARCH when collapsed; expand → owned ✓-chips, one prominent research card with a tower **role hint** (dmg-type chip + role), and the whole future folded into a single **"CLASSIFIED · N ahead"** strip). Fork shows an A/B toggle. **Migration:** old flat `talents` saves are refunded to Alloy and cleared (pre-release-safe). Dev override (0/50/100%) remapped to force the first X% of every branch's nodes. Achievements `talent1`/`talentall` retargeted (research a node / complete every branch). Renamed `fallengrid-v6.24.html`, label + ver → **6.24**. **Verified:** `node --check` + guards (nextwave==1, countdown==1); `verify624.py` (9/9): fresh = {turret,cryo} + 32 nodes/0 owned; research unlocks Tesla (−140 alloy); aggregation (Munitions I → bonus.dmg 0.05); **free fork** (Precision dmg 0.18 ↔ Saturation splash 0.35, swap costs 0 alloy, swap-back exact); dev 100% → all towers + armory 1.0 + buy locked; **migration** dmg1+scrap2 → +950 alloy, talents cleared; Armory screen renders all 4 branches + RESEARCH click buys; **tray gating** (locked Tesla can't arm a build, Auto-Gun can); new-player **maxed gun+cryo clears Outpost/Normal** (victory, core 9). Core regression green; **0 page errors**. **Pushed with `[skip ci]` so NO APK builds** (user: "lav IKKE APK - kun html til test"). **Balance watch (deferred costing pass, per spec §5/§8):** gun+cryo-only is intentionally weaker than the old all-towers start — winnable on Tier A but wants a full board; confirm the new-player curve with an on-device play-log, then tune node costs (~25k total Alloy target) and capstone/fork values. **Next phases:** P2 = Siege Battery (real tower: art + `TOWERS` entry + a/b forks + sound), P3 = Prism + capstone polish; then migration re-confirm before any public release.
- **Iter — V6.25 Research Tree P2+P3: Siege Battery & Prism become REAL capstone towers. ✅ SHIPPED (user-requested; HTML test build only, NO APK).** The two capstone nodes stopped being placeholder stat-buffs and now unlock two brand-new towers, taking the roster from 5 → **7**. **Siege Battery** (Ordnance capstone, `kind:"lob"` so ground-only, `dmgType:"explosive"`, always `pierce`): the armor answer — L1-3 dmg 105/165/255 (rate ~1550→1380, splash 92→110), forks **Bombard** (rapid wide barrage, 225 @1050, splash 132 → t4 *Cataclysm* 340, splash 152) and **Breaker** (one colossal shell, 480 @1800 → t4 *Ruination* 760). **Prism** (Arc capstone, `kind:"beam"` so it hits air, `dmgType:"energy"`): L1-3 dmg 70/110/168 with chain 2/2/3, forks **Refraction** (chains far & wide, 128 @700 chain 4 → t4 *Spectrum* 190 chain 5) and **Lance** (focused pierce sniper, 300 @1080 range 340 → t4 *Singularity* 470, range 368). Both are deliberately tuned **conservative** (≈1.6-2.1× a comparable t4's DPS for ≈1.7-1.9× the scrap) — easier to buff after a play-log than to walk back. **Implementation:** `kind` still drives all mechanics (so combat/targeting/air rules needed **zero** new branches), and a new **`art` field** drives rendering — `drawTowerArt` now dispatches on `spec.art || spec.kind`, with two bespoke heads: siege = armored breech + braced heavy barrel with recoil cylinders, reinforcing bands, muzzle brake and a pulsing charge indicator; prism = a counter-rotating crystal triangle with orbiting refraction shards (5 shards on Refraction, 1 big focused shard on Lance) and refraction lines to the core. Added `flashAt` so the muzzle flash lands at each bespoke barrel tip. New **dedicated SFX** (`Sound.siege()` = deep breech crack + 62 Hz sub + low rumble tail; `Sound.prism()` = crystalline triangle/sine stack + bandpassed shimmer), routed in `fire()` by tower type so Mortar/Tesla keep their own sounds. Capstone nodes changed to `eff:{tower:"siege"}` / `eff:{tower:"prism"}` (the tower **is** the payoff; their old placeholder stat bonuses are gone). Added `glowCols` entries (drawTowers looks up `gl_<type>` for every firing tower), Codex role text, and Armory `TREE_ROLE` hints. **Adaptive build tray:** 7 slots on a 360px screen would have overflowed, so both trays now compute `tight = N > 5` and shrink gap/art-scale/fonts and drop the desc line — measured **8px → 352px, no overlap**. Renamed `fallengrid-v6.25.html`, label + ver → **6.25**. **Verified:** `node --check` + guards; `verify625.py` (9/9): fresh roster locks both; completing Ordnance/Arc unlocks Siege/Prism; **siege combat** (315 armor dmg + 315 splash to a neighbour, **0 to flyers**, pierce true); **prism combat** (280 to a flyer, chains 168 to two nearby foes); both branch ladders rise monotonically through a3/a4/b3/b4; tray fit/overlap/locked-set; 2.5 s of live RAF rendering + firing with 8 mixed-tier new towers (art + both SFX paths) → 0 errors; Codex lists both towers and all four fork names; full 7-type maxed board still wins (core 20/20). Core regression green; **0 page errors**. Pushed with `[skip ci]` — **no APK**. **Balance watch:** these are the first towers whose *scrap* cost is gated behind *Alloy* progression, so the real question is whether reaching a capstone feels worth ~820-860 Alloy — confirm with a play-log before tuning either the tower stats or the capstone prices.
- **Iter — V6.26 Wave Intel command bar + camo fairness gate. ✅ SHIPPED (user-requested; HTML test build only, NO APK).** Two fixes driven by the 2026-07-24 play-log and the bottom-bar UX review. **(1) The idle tray was dead space** — it rendered the full 7-tower palette while `trayBtns = []`, so 118 px of screen showed controls that could not be tapped. Replaced with a **Wave Intel command bar**: a header (`▸ WAVE n INBOUND/ENGAGED` + countdown), up to **three threat cards** for the next wave (enemy name, count, and a trait tag — ARMOR / AIR / SHIELD / CLOAKED / ENERGY-RES / SLOW-IMM / HEALS / SPLITS / BOSS / TANKY — each with a colour-coded severity stripe), a **targeted warning line** that only fires when the board genuinely cannot answer what is coming (`⚠ AIR INCOMING · NO ANTI-AIR IN RANGE`, `⚠ CLOAKED FOES · NO DETECTOR`, `⚠ ARMORED · NEED PIERCE OR EXPLOSIVE`), live scrap, and an action row of **three real 109×32 buttons — HERO / STRIKE / REPAIR** (with cooldown fills, Commander level, repair cost and core state). New helpers: `THREAT_TAG`, `waveIntel(n)` (cached per wave+modifier+difficulty — `waveComp` counts are deterministic, only arrival order is shuffled), `boardCan("air"|"detect"|"armor")`, `cmdBtn()`, `drawCmdRow()`, `trayIsIdle()`. The **floating Hero/Airstrike circles over the map edge are gone** while the command bar is up (they still appear in build/upgrade/confirm tray states, where the tray has no room) — `drawCmdRow` pushes into the *same* `heroBtns`/`abilityBtns` arrays the existing tap handlers already read, so **zero handler changes** were needed; REPAIR routes through `trayBtns` + a new `trayAction` branch (an earlier attempt via `hudBtns` was wiped each frame because `drawHud` rebuilds that array after the tray draws). **(2) Camo was unfair by construction** — Phantoms spawn from wave 7, but only a **Tesla** (or the Hero, 150 px radius) could reveal them, and Tesla is now gated behind Armory research. A fresh account therefore met untargetable enemies it had no way to counter. This is visible in the attached play-log: the Serpentine run with `armory: 0` and a **turret-9 + cryo-3** board **lost with 19 leaks** despite `heroLvl 5`. Fix: `waveComp` now computes `canDetect = Tech.isUnlocked("tesla") || Tech.isUnlocked("prism")`; when false, phantoms are held back entirely and an equal number of **raiders** is substituted, so wave size and pressure are unchanged but every enemy is answerable. Also fixed a **P3 oversight**: `updDetection` only checked `t.type !== "tesla"`, so the new Prism — an energy beam tower descended from Tesla — did **not** reveal camo; it now does. Codex/tip copy updated for both towers and for the new spawn rule. Renamed `fallengrid-v6.26.html`, label + ver → **6.26**. **Verified** (`verify626.py`, 9/9): no-detector accounts get **0 phantoms at waves 7/10/14/19** with raiders substituted; researching Tesla brings them back (3 at w14) with raiders 15→12 and **wave size identical at 46**; Prism reveals *and* damages a cloaked foe; HERO/STRIKE/REPAIR are real 109×32 buttons inside the 522-640 tray band; tapping tray HERO arms the deploy through the existing handler; intel counts **exactly** match `waveComp` and sort by count desc; `boardCan('air')` flips false→true once a direct-fire tower exists; the Hero FAB sits at y 600 (tray) when idle and returns to y 406 (map edge) in build mode; 2.2 s of live frames with no errors. Core regression + the V6.25 P2/P3 suite both re-run green against 6.26; **0 page errors**. Pushed with `[skip ci]` — **no APK**. **Play-log note (not yet acted on):** the same log shows the retry of that Serpentine run won but took **795 s** (13 min) vs 281 s, with a 12-turret + 3-cryo board — a gun-heavy grind that is worth watching once the camo fix lands, since phantoms were a large part of what stalled it.
- **Iter — V6.27 Trustworthy run duration (activeMs). ✅ SHIPPED (user correction).** The user pointed out that a 795 s run in the play-log was long only because they *took a break* — "det kan ikke ses i log". Correct, and it invalidated a balance flag I had raised from that number: `durationMs` was raw wall clock (`performance.now() - _t0`), so it silently counted paused **and** backgrounded time, making it useless as a pacing metric — the one thing the play-logs are analysed for. Fix: the run record gains an `_act` accumulator ticked by **`Telemetry.tick(dtMs)` placed inside the `if (S.screen === "playing")` branch of `frame()`** — the single gate that already decides whether the sim advances. Because *every* pause path (pause button, hardware back, `visibilitychange`, `blur`) funnels through `S.screen = "paused"`, this excludes all of them without touching any of those handlers. `dtMs` is already clamped to 50 ms, so a long pause contributes at most one frame. `end()` now writes **`activeMs`** (true play time), keeps **`durationMs`** as wall clock so older logs stay comparable, and adds **`pausedMs`** as the difference; both internal accumulators are stripped from the saved record. The in-game run log now reads "Ns active (+Ns paused)". Renamed `fallengrid-v6.27.html`, label + ver → **6.27**. Verified (`verify627.py`): 3 s of manual pause leaks **50 ms** (exactly one clamped frame) into activeMs; a `visibilitychange` background pause likewise leaks 50 ms and parks the screen; active time resumes accumulating at >5× the leak rate; the saved record satisfies `activeMs + pausedMs == durationMs` and carries no `_act`/`_t0`. Core regression + the V6.26 suite re-run green; 0 page errors. Pushed with `[skip ci]` — no APK. **Note for future log reads:** headless SwiftShader runs under 20 fps, so `dtMs` hits the 50 ms clamp and activeMs undercounts *in the test harness only* — on a 60 fps device dtMs is ~16.7 ms and never clamps, so `activeMs` is accurate and `pausedMs` ≈ real idle time. **Retracted:** the "13-minute gun-heavy grind" concern from the V6.26 entry — that duration was a break, not gameplay; the board was fine.
- **Iter — V6.28 Two-row build menu (feedback #5). ✅ SHIPPED (user-requested; HTML test build, NO APK).** Ships the *build* half of bottom-bar Proposal 1 — the idle half went in as V6.26's Wave Intel bar, which caused a genuine expectation gap (the two-row menu had only ever been prototyped in scratchpad, never committed; see `docs/FEEDBACK-backlog.md` #5). With 7 towers the palette was one row of **45.7 × 74 px** cells — below the 48 px Android / 44 pt iOS minimum in the crowded dimension, with six adjacent neighbours. Now **2 rows of 4** with a horizontal cell layout (art left, name + cost right): measured **81.5 × 46 px**, min-x 8 → max-x 352, no overlap. Rather than squeeze two rows into the fixed 118 px band (42 px rows hugging the gesture-nav edge), the tray **temporarily grows 22 px upward while the build menu is open** — new `BUILD_EXP`, `buildMenuOpen()` and `trayTop()`, the latter used by **both** the tray fill and the tap gate (`if (y >= PLAY_BOTTOM)` → `if (y >= trayTop())`). 22 px borrowed from a 466 px play area, only while building, is imperceptible; the rows still end at y 621, i.e. **no closer to the bottom edge than the old single row (622)**. Renamed `fallengrid-v6.28.html`, label + ver → **6.28**. **Verified** (`verify628.py`, 4/4): 7 cells across 2 rows at 81.5 × 46 px, on-screen, non-overlapping; tray lifts to 500 while building and returns to 522 when not; a tap in the **borrowed strip (500-522)** is absorbed by the tray instead of falling through to world handling and cancelling the plot selection; locked towers still refuse to arm while unlocked ones do, and confirm still places. Core regression + the V6.26 suite re-run green; 0 page errors. Pushed with `[skip ci]` — no APK. **Note:** the build-menu layout will be reworked again by the landscape project (feedback #4) — accepted knowingly, since the cramped row is hit every game and landscape is a multi-iteration effort.
- **Iter — V6.29 Onboarding: first-launch briefing + Armory nudge + ability coach-marks (feedback #1/#2/#3). ✅ SHIPPED (user-requested; HTML test build, NO APK).** Implemented by a delegated Sonnet subagent against the spec in `docs/PLAN-feedback.md` iteration B, then reviewed, corrected and re-verified by me before commit. **B0 — post-run Armory nudge (the highest-value part, and the one the play-log demanded):** the 2026-07-25 log showed four completed missions with **~3,140 Alloy earned and `armory: 0`** — the research tree was effectively invisible. New `cheapestAffordable()` scans all four `TECH` branches via `Tech.next(bi)` and returns the cheapest node the wallet covers. The victory/gameover screens now read **`+561 ⬡ ALLOY EARNED · 561 UNSPENT`** with a green **`▸ READY TO RESEARCH: <node> (⬡cost)`** line, and the **Armory button becomes the primary CTA** (Redeploy / Next Mission demoted to a plain button) whenever research is affordable; the main-menu Armory button gains a green `●` badge in the same condition. **B1 — first-launch briefing:** `INTRO_CARDS` (3 cards — the fallen Grid, hold the line, and *your edge*: Scrap builds vs Alloy researches, plus why you start with only Auto-Gun + Cryo), rendered by `renderIntro()` through the existing `show(html)` overlay, gated on `Store.get("seenIntro")`, skippable, with dot pagination, and **re-viewable from a new "Replay Briefing" button in the Codex** (routed back via `S.introFrom` so a replay never launches gameplay). **B2 — the two free abilities:** `TIPS` gained `hero` (fires in `startWave()` at wave 2 when no Commander is deployed) and `strike` (fires in `frame()` the first time the strike is off cooldown with enemies alive and unused) — both one-shot through the existing non-blocking coach-mark system, both suppressed if the ability has already been used. **My corrections to the subagent's work:** (1) it had **BEGIN land on the main menu**, deviating from the approved plan ("BEGIN → straight into Mission 1") out of a valid concern that a Codex replay must not start gameplay — but that case was already distinguished by its own `S.introFrom`, so I made `introFinish(begin)` deploy into campaign Mission 1 from a boot-time BEGIN while SKIP still goes to the menu and a Codex replay returns to the Codex; the button now reads **"▸ BEGIN MISSION 1"**. (2) `onBack()` **returned `false` on the intro screen**, so the hardware/gesture back button would have fallen through and likely exited the app on first launch — now it dismisses the briefing and returns handled. Renamed `fallengrid-v6.29.html`, label + ver → **6.29**. **Verified** (`verify629.py`, 8 groups, re-run by me after my changes): briefing shows once on a fresh account and never after BEGIN/SKIP + reload; **BEGIN → `screen:"playing"`, `campaign:true`, `mapIndex:0`**; SKIP → menu; back-button dismisses and is handled; Codex re-entry works and returns to the Codex; run-end nudge appears with an affordable node and is absent at 0 Alloy; menu badge appears/hides correctly; both tips fire once and stay suppressed when the ability was already used. Core regression + the V6.26 and V6.28 suites all re-run green; **0 page errors**. Note: the core suite's `fresh()` fixture now seeds `seenIntro=true` — a test-fixture change only, correct because a genuinely fresh account legitimately lands on the briefing now. Existing players will see the briefing once (no `seenIntro` key yet), which is the intended behaviour. Pushed with `[skip ci]` — no APK. **Acceptance test for the next play-log: `armory` should move off 0.**
- **Iter — V6.30 (C1) Dynamic, orientation-aware layout space — foundation for landscape (feedback #4). ✅ SHIPPED (HTML test build, NO APK).** Step 1 of 3 in the landscape project (`docs/PLAN-feedback.md` iteration C). The whole UI was positioned against a **fixed logical space** — `const W = 360, H = 640` with `HUD_H`, `TRAY_H`, `PLAY_TOP`, `PLAY_BOTTOM`, `PLAY_H` derived once at load. Measured in the game block: **228 references** across those seven names. Converting them from `const` to **live `let` bindings recomputed by a new `layout()`** means all 228 read the new values with **zero call-site changes** — that is what makes this project tractable. `layout()` runs at the top of `resize()` (already wired to the `resize` event), detects orientation, and in **portrait yields byte-identical numbers to the old constants** (360/640/56/118/56/522/466) — pixel-identical portrait is the acceptance test for this step. It also computes the landscape space (**640×360, HUD_H 46, PLAY_TOP 46, PLAY_BOTTOM 360, PLAY_H 314**) plus a `LANDSCAPE` flag and a `TRAY_X`/`TRAY_W` side-panel region (452/188) that **C2 will consume** — in portrait `TRAY_X` is off-screen and nothing reads it, so no behaviour changes yet. Audited every init-time capture of these values: only the declarations themselves plus `MINZOOM` (already a `let`, recomputed per map in `loadMap`) and the three.js camera aspect (reset every frame in `applyCam`), so nothing else holds a stale copy. Added a `__lay` debug getter. Renamed `fallengrid-v6.30.html`, label + ver → **6.30**. **Verified** (`verify630.py`): portrait matches the old constants exactly; rotating to landscape recomputes the space correctly; rotating **back restores portrait exactly** (no drift); and a rotation round-trip mid-run leaves the sim healthy (towers intact, still playing). Core regression + the V6.26, V6.28 and V6.29 suites all re-run green; 0 page errors. `[skip ci]` — no APK.
- **⚠ PROCESS FIX (V6.30) — the `node --check` guard was checking the WRONG script block.** The file has **four** `<script>` blocks; the extraction used "largest block", which is **three.js (607 KB)**, not the game (block #3, ~358 KB). So every `node --check` since the routine began validated three.js and would never have caught a syntax error in game code. **No bad code shipped** — the Playwright suites parse and execute the real game with `pageerror` listeners, which is strictly stronger validation, and all of them passed each iteration; the game block was re-checked at V6.30 and is clean. Replaced the ad-hoc extraction with `scratchpad/gamecheck.sh`, which selects the block by **game-only markers** (`PLAY_BOTTOM` + `function drawTray`), hard-fails unless exactly one block matches, and then runs `node --check`. Use it from now on instead of the inline "largest script" snippet.
- **Iter — V6.31 (C2) Landscape play screen: tray becomes a right-hand side panel (feedback #4). ✅ SHIPPED (HTML test build, NO APK).** Step 2 of 3 of the landscape project, built on C1's live layout bindings and implemented by a delegated Sonnet subagent, then reviewed, **bug-fixed** and re-verified by me before commit. In landscape the tray cannot stay a full-width bottom band (H is only 360), so it becomes a **188 × 314 right-hand panel** at `TRAY_X` 452, with the HUD still a full-width top band (`HUD_H` 46) and the play viewport occupying x 0-452, y 46-360. **Approach:** two new helpers — `playW()` (`LANDSCAPE ? TRAY_X : W`, the real viewport width) and **`trayRect()`**, the single source of truth for the tray rectangle (portrait `{0, trayTop(), W, H-trayTop()}` preserving V6.28's build-menu expansion; landscape `{452, 46, 188, 314}`, no expansion needed). Every tray drawing function — `drawTray`, `drawTrayIdle`, `drawCmdRow`, `drawBuildMenu`, `drawTowerMenu`, `drawConfirm`, `drawSellConfirm`, `drawExcavateConfirm` — now positions against that rect and branches per orientation; the portrait branches are the original code with `PLAY_BOTTOM`/`trayTop()` literals rewritten to `rect.y` (algebraically identical). **Landscape arrangement:** build palette **2 per row at 83 × 56 px**, Wave Intel threat cards **stacked vertically** (168 × 28), HERO/STRIKE/REPAIR **stacked vertically** (168 × 27), tower/confirm menus fully vertical. **Hit-testing:** the tray gate moved from `y >= trayTop()` to membership in `trayRect()`; the armed-airstrike and armed-hero handlers, the pan gate (`gest.inPlay`) and the drag-preview gate gained `x < TRAY_X`, so an armed strike can no longer be called down through the panel. **Camera:** `MINZOOM` (both sites) and `fitCamera()`/`clampCam()` derive from `playW()`, so the map fits the 452 px viewport instead of the full canvas; `resize()` refits only when the orientation actually flips. Floating overlays (zoom buttons, boss bar, banner, tips, armed frames, tutorial SKIP) are bounded to `playW()`. **Bug I caught in review that the subagent's tests missed:** the **DEPLOY (`nextwave`) button was still anchored to `W - nwW - 10`**, putting it at x 468-636 / y 29-57 — **overlapping the side panel**, and since `hudBtns` are hit-tested *before* `trayBtns` it also stole taps from the panel's top strip. Re-anchored to `playW()` (now x 280-448) and added a permanent regression test asserting **no HUD button may intersect `trayRect()`**. Renamed `fallengrid-v6.31.html`, label + ver → **6.31**. **Verified by me** (not just the subagent): `gamecheck.sh` clean, both guards 1, no model identifiers; `verify631.py` 8 groups (portrait tray geometry unchanged at 81.5 × 46 / 2 rows / 7 cells; landscape rect exact; all tray buttons and FABs inside it; landscape cells 83 × 56 with no overlap; a tap in the panel reaches `trayBtns`; an armed strike over the panel does not fire into the world; mid-run rotation keeps towers and keeps playing; portrait restores exactly; **no HUD/panel overlap**); plus **all five existing suites** (core regression, V6.26, V6.28, V6.29, V6.30) re-run green against 6.31. 0 page errors throughout. `[skip ci]` — no APK. **Remaining for C3:** the `show(html)` menu/overlay screens are still portrait-shaped in landscape, plus the tablet sizing pass and the Android manifest orientation setting.
- **Iter — V6.32 (C3) Landscape menus, tablet pass, and both-orientation Android config (feedback #4 COMPLETE). ✅ SHIPPED (HTML test build, NO APK).** Final step of the landscape project. **Correction to my own earlier claim, recorded because I acted on it:** I reported that the overlay menus were "effectively unusable" in landscape — that the Armory button was unreachable — based on comparing `panel.scrollHeight` (663) to `overlay.clientHeight` (360). **That was the wrong test:** `.panel` already carried `max-height: 92vh; overflow-y: auto`, so a content height exceeding the box is exactly what a scrollable element looks like. A proper probe (scroll to bottom, then check the last button's rect against the panel's) showed **every screen already scrolled correctly and every last button was reachable** in landscape. So C3.1 was a real but much smaller job than briefed: not "broken", just cramped. **What actually shipped: (1) Landscape panel treatment** — a new `@media (orientation: landscape) and (max-height: 500px)` block trades unused horizontal space for less vertical scrolling: panel width **520 → 660 px**, `max-height` 92vh → 96vh, tighter padding and heading sizes. Menu content height dropped 663 → 575 px and three more rows are visible without scrolling. The query is deliberately gated on **both** landscape orientation *and* `max-height: 500px` so portrait and tablets are untouched — and it is placed **after** the existing `min-width: 600px` / `700×700` queries, because at 740×360 those also match and would otherwise win on source order (they did, on the first attempt — caught by the verify asserting the widened width). **(2) Android orientation** — the Android project is generated in CI by `npx cap add android`, so `AndroidManifest.xml` is not checked in and must be patched after generation. Added a workflow step that sets `android:screenOrientation="fullSensor"` on MainActivity and **merges `orientation|screenSize|screenLayout|keyboardHidden` into `android:configChanges`**. The configChanges part matters as much as the orientation flag: without it Android destroys and recreates the activity on rotation, the WebView reloads, and **the player loses their run mid-mission**. YAML validated; the manifest patch itself is **unverified here** (no Android SDK in this container) — it needs a real CI run and an on-device rotation test to confirm. **(3) Tablet pass** — verified rather than changed. Renamed `fallengrid-v6.32.html`, label + ver → **6.32**. **Verified** (`verify632.py`): for **12 overlay screens** (menu, campaign, freeplay, settings, armory, howto, ach, stats, daily, lab, log, intro) across **four viewports** (landscape phone 740×360, portrait phone 360×720, tablet landscape 1280×800, tablet portrait 800×1280) the panel stays inside the viewport and the **last interactive button is reachable after scrolling** — 48 screen/viewport combinations, zero failures. Portrait panel width asserted unchanged at ~324 px. Plus all six existing suites (core regression, V6.26, V6.28, V6.29, V6.30 C1, V6.31 C2) re-run green against 6.32; 0 page errors. `[skip ci]` — no APK. **Landscape support (feedback #4) is now complete across C1-C3, with the manifest patch as the one item still needing on-device confirmation.**
- **Iter — V6.33 Landscape fills the screen: logical width now tracks the device aspect. ✅ SHIPPED (user-reported from an on-device screenshot; HTML test build, NO APK).** The user rotated their S25+ and sent a photo: landscape worked, but the canvas sat in a narrow strip with **large pillarbox bars left and right**, using roughly two-thirds of the width. **Cause:** C1 hardcoded the landscape logical space to **640×360 (16:9)**, while a phone landscape viewport is far wider — an S25+ is about **2.7:1**. Since `resize()` scales by `min(vw / W, vh / H)`, height became the binding constraint and the leftover width turned into bars. This was a defect in my own C1 choice, not in the subagent work. **Fix:** `layout()` now keeps **H fixed at 360** (so the HUD band and side panel keep their proportions and every C2 layout stays valid) and derives **W from the actual viewport aspect**, `W = clamp(round(360 * vw/vh), 560, 1120)`. `TRAY_X` follows as `W - TRAY_W`, so the side panel stays flush right and the play viewport absorbs all the extra width. Also switched both `layout()` and `resize()` to prefer **`window.visualViewport`** over `innerWidth/innerHeight` — on mobile browsers the latter lag while the URL bar shows/hides, which was a second source of mis-sizing — and added **`orientationchange`** and **`visualViewport.resize`** listeners so a rotation actually re-fits. **Measured fill** (canvas CSS size vs viewport): S25+ landscape 1050×390 → logical **969×360**, fills **100% W / 100% H** (was ~66% W); 740×416 → 100%/100%; tablet landscape 1280×800 → 100%/100%. Renamed `fallengrid-v6.33.html`, label + ver → **6.33**. Tests that pinned landscape to the old magic numbers (`W == 640`, `TRAY_X == 452`) were **relaxed to assert the relationship** instead — `W` tracks the aspect and `TRAY_X == W - 188` — which is the durable property. Verified: `gamecheck.sh` clean; **all seven suites** green against 6.33 (core regression, V6.26, V6.28, V6.29, V6.30 C1, V6.31 C2, V6.32 C3); 0 page errors. `[skip ci]` — no APK. **Known and NOT changed: portrait still letterboxes** — the portrait logical space is a fixed 360×640 (1.78:1) against an S25+ portrait viewport of about 2.17:1, so it fills the width and leaves bars top and bottom (measured 100% W / 89% H at 360×720, worse on a taller phone). The same aspect-tracking trick would fix it, but it would move `PLAY_BOTTOM`/`PLAY_H` and therefore touch every portrait layout and test, so it is left as an explicit decision for the user rather than bundled in silently.
- **Iter — V6.34 Portrait fills the screen too (mirror of the V6.33 landscape fix). ✅ SHIPPED (user-requested; HTML test build, NO APK).** V6.33 flagged that portrait had the same defect landscape did, and left it as an explicit user decision; the user asked for it. Portrait used a fixed **360×640 (1.78:1)** logical space against an S25+ portrait viewport of about **2.17:1**, so it filled the width and left letterbox bars top and bottom (measured **100% W / 89% H** at 360×720, worse on a taller phone). **Fix, mirroring landscape:** keep **W at 360** (so the HUD band, the 118 px tray and every portrait layout keep their proportions) and derive **H from the device aspect**, `H = clamp(round(360 * vh/vw), 600, 900)`. `PLAY_BOTTOM` and `PLAY_H` follow, so **all the extra height goes to the play area** — at 360×720 the play band grows from 466 → 546 px. **Measured fill after:** portrait phone 360×720 → **100% W / 100% H** (was 100/89); tablet portrait 800×1280 → 96%/100% (was 90%/100%). Landscape figures unchanged. Renamed `fallengrid-v6.34.html`, label + ver → **6.34**. **Test debt paid down:** portrait geometry was hardcoded as `H:640`, `PLAY_BOTTOM:522`, `PLAY_H:466`, tray bands `522..640`, and the build-menu lift `500`, across `verify630`, `verify631`, the V6.26 suite and the V6.28 suite. All of those are now **derived** — from `__lay.PLAY_BOTTOM`/`__lay.H` or from the same clamp formula — so the tests assert the *relationship* (tray sits at `PLAY_BOTTOM`, build menu lifts exactly 22 px, rows stay within `H`) rather than numbers that change with the device. That is the durable property and it is what let the whole suite pass unchanged on a new geometry. Verified: `gamecheck.sh` clean; **all seven suites** green (core regression, V6.26, V6.28, V6.29, V6.30 C1, V6.31 C2, V6.32 C3); 0 page errors. `[skip ci]` — no APK.
- **Iter — V6.35 Capstone towers reshaped + campaign-gated (roster collapse fix). ✅ SHIPPED (user-requested; HTML test build, NO APK).** The 2026-07-26 play-log (12 runs) showed **roster collapse**: with no research the board was `turret + cryo` and needed **10-21 towers**; at armory 0.5 it was a mixed turret/cryo/tesla/mortar roster; but from armory 0.59 the board was **only prism + siege (+cryo)** — turret, mortar, pyre and tesla vanished **entirely** — and Crossroads/normal fell to **5 towers at 20/20 core**, Gauntlet **brutal** to **5 towers at 15/15 core**. **My V6.25 analysis error:** I tuned on **DPS per scrap** (~1.6-2.1× power for 1.7-1.9× cost, called "conservative"). But `scrapSpent` stayed at 4.7k-9k while tower counts fell to 5 — the binding resource on these maps is **build plots**, not scrap, and on **DPS per *plot*** these two win decisively. **The user pushed back on nerfing, with data:** "det er kun med disse tårne jeg har kommet igennem de sidste to baner". Correct — on the late brutal maps they are *needed*, not dominant: The Maw cost 6 core, **Blockade cost 12 of 25**, and **Ground Zero was a loss**. A flat damage nerf would have broken the top end. **So the fix reshapes coverage and leaves punch alone:** every damage value is **unchanged** (Cataclysm 340, Ruination 760, Spectrum 190, Singularity 470); instead **range** is cut (siege 172→148 at L1, 222→190 at t4a; prism 238→202 and 302→256) and **siege splash** is cut (92→78, 152→118). One tower can no longer cover a whole map, so the rest of the roster keeps a reason to exist, while a Breaker shell still hits exactly as hard. **Plus a campaign gate:** both capstones carry `reqCamp: 12` — unbuyable until 12 missions are cleared, whatever the Alloy balance. New `Tech.blockedBy(bi)` returns the reason; `Tech.buy` enforces it; the Armory renders **"🔒 MISSION 12 REQUIRED"** in place of the button on both the collapsed and expanded cards; and `cheapestAffordable()` skips gated nodes so the V6.29 run-end nudge never advertises something you cannot buy. Renamed `fallengrid-v6.35.html`, label + ver → **6.35**. **Verified** (`verify635.py`): all four capstone damage figures asserted **unchanged**; range/splash cuts asserted; capstones unbuyable and towers still locked at camp 3 even with 999k Alloy, with the reason string reported; buyable and unlocked at camp 12; the nudge never names a gated capstone; the Armory shows the lock and renders **no** RESEARCH button. Plus **all eight** other suites green (core regression, V6.25 P2/P3, V6.26, V6.28, V6.29, V6.30 C1, V6.31 C2, V6.32 C3); 0 page errors. **Two stale tests fixed while re-running them** — both were latent traps, not new breakage: (1) the V6.25 suite drove capstones with an **unbounded `while(T.owned(bi) < 8) T.buy(bi)`**, which the new gate turned into an **infinite loop that hung the browser**; it now seeds `camp` and bounds the loop. (2) Its tray-overlap check sorted all seven build cells by `x`, which **falsely flags a two-row layout** (V6.28); it now checks overlap per row, matching the V6.28 suite. `[skip ci]` — no APK. **Next log should show:** a mixed roster again on mid maps, and capstones appearing only from mission 12.
- **Iter — V6.36 Bottom-bar restructure + five play-log fixes. ✅ SHIPPED (user-requested; HTML test build, NO APK).** Six points from the 2026-07-26 clean-account log (13 runs on V6.35), plus three design calls the user made up front (sliding panel · stretch the Armory to ~25k · soft wave block). **(1) Focus follows the build.** `doPending` now sets `S.selTower` to the tower it just pushed, so the upgrade menu is one tap away instead of forcing a second hunt for the plot you just tapped. **(2) Armory re-costed 10,670 → 25,380 Alloy.** The user asked whether maxing the tree after 9 missions was a reset bug: it is **not** — measured, they earned 561/561/1001/1001/1000/1001/1000/1000/1351 = **8,476** by run 9 against **8,990** of reachable nodes, so 91% was correct arithmetic on a tree that was **2.4× cheaper than the V6.17 design target of ~25,700** (spec §236). Node costs are now scaled by ladder position with a curve that leaves the start alone — index 0 unchanged, index 1 +10%, rising to ×3.2 at the capstone. The first two nodes of all four branches still total **1,230** (was 1,160), while the whole tree now paces the 25-mission campaign: **20,010** reachable before the mission-12 capstones. **(3) Bottom bar restructured.** HERO / STRIKE / CORE were a *state* of the tray — select a plot and they vanished, which is why the Airstrike appeared to live in two unrelated places (tray button *and* a floating map circle that swapped in during build mode). They are now a **permanent strip** pinned to the tray's bottom edge (`cmdRowY()`), drawn last in `drawTray` so the sheets above it can keep resetting `trayBtns`; the floating `drawHeroFab`/`drawAbility` circles are deleted outright. The tower palette left the tray entirely and became a **panel sliding in from the left** over the play area (`drawBuildPanel`, `buildPanelRect`, its own `buildBtns` array): one full-width row per tower — art, name, cost, live DMG/RNG, a damage-type glyph and a plain-language role line — plus a real **✕ close button**, since "tap the plot again to cancel" was never discoverable. Portrait rows measure **166 × 46 px** (they were 81.5 × 46 in the tray, and 45.7 × 74 before V6.28); landscape compresses to 166 × 34 and still clears the tap minimum across a 166 px width. The panel swallows background taps so they cannot fall through and cancel the selection, and a drag started on it pans nothing. `BUILD_EXP` (the 22 px the palette used to borrow) is gone; `TRAY_EXP` (46 px) replaces it and applies to the **tower/confirm sheets** instead, so those keep their exact old layout with the command strip added below. Transient coach-mark tips drop to the bottom of the viewport while the panel is open rather than covering the first tower. **(4) Commander placement matches tower placement.** It was the one placement in the game with no preview and no confirm — arming it and tapping a plot dropped it instantly. It now raises a pending sheet (`kind: "hero"`, `drawHeroConfirm`) showing damage, range, fire rate, pulse damage and what it counters, with the **range ring on the target plot**, and a ✓ DEPLOY · FREE / ✗ pair. Once deployed the Commander carries a **permanent gold range ring** in the 3D scene — dim while the pulse recharges, breathing once it is ready. **(5) Overclock reworked.** It was 200 scrap for +10% damage on one tower against ~330 for a whole maxed Auto-Gun, and the log shows it used **3 times in 13 runs**. Now **120 scrap × 1.7ⁿ**, buying **+15% damage and +6% fire rate** per stack, **capped at 3** (`OC_MAX`) — 671 scrap total for +45%/+18%. It routes through the standard confirm sheet (`pendingInfo` describes it, so the before/after stat line comes free) and overclocked towers wear a **⚡N pip** on the field, so the state is visible with the menu closed. **(6) Wave soft block.** Measured from the log: wave 14 takes **20.4s** to spawn against a 20s `WAVE_GAP`, wave 18 **30.7s**, wave 20 **34.3s** — so on the big late maps the auto-launch was stacking a fresh wave onto one still pouring in. `startWave` now records `S.waveSize`, and `autoHold()` **freezes the gap timer** (it does not restart it) while more than 20% of the current wave is still queued, releasing in the tail. A player-initiated early send is untouched — overlapping on purpose stays a deliberate risk. The hold is visible: a ⏸ next to the countdown on the DEPLOY button and in the intel bar, plus "⏸ NEXT WAVE HELD · CURRENT ONE STILL SPAWNING" where the generic hint sits, and `holdMs` goes into the telemetry so the next log can confirm it. Renamed `fallengrid-v6.36.html`, label + ver → **6.36**. **Verified** (`verify636.py`, 17 checks): tree total/reachable/first-two-nodes; focus lands on the new tower; overclock cost curve, dual stat effect and the 3-stack cap; the confirm path spends nothing until confirmed; `autoHold` true mid-spawn, false in the tail, false when clear, and a 6-second sim proving the countdown freezes and then launches once the queue drains; the strip present in idle, build and confirm states with the hero button **not moving** when the panel opens; panel row geometry, no tray overlap, no HUD button intersecting the tray (the V6.31 C2 guard); ✕ closes; hero arm → pending → range ring → deploy; the same in landscape with the panel clear of the side tray and the strip on-screen; and a full 20-wave headless victory. Plus **all nine** other suites green (core regression, V6.25 P2/P3, V6.26, V6.28, V6.29, V6.30 C1, V6.31 C2, V6.32 C3, V6.35); 0 page errors. **Three suites were updated where V6.36 deliberately changed the contract**, not to make them pass: V6.26's "FAB returns to the map edge in build mode" became "the hero button must not move"; V6.28's two-row tray assertions moved to the panel (same requirement — ≥48 px, on screen, no overlap, taps absorbed — new container) and gained the focus-follows-build check; V6.31's portrait/landscape palette geometry now asserts the panel sits inside the play viewport and clear of the side tray. `[skip ci]` — no APK.
- **Iter — V6.37 Commander into the build panel + panel toggle + range ring on demand. ✅ SHIPPED (user-requested; HTML test build, NO APK).** Three follow-ups after the user tested the V6.36 panel ("det ser rimelig godt ud"). **(1) The Commander is deployed from the build panel, the strip button is the Pulse.** V6.36 fixed *where* Hero and Strike live but left the Commander with its own placement mode — arm from the strip, then tap the map — which was still a second way to place something. It is now the **first row of the build panel** (`panelRows()` prepends `"hero"` while none is deployed): free, with damage/range, a ⚑ tag and "Levels up · air · sees cloaked", raising the same `kind: "hero"` confirm sheet V6.36 introduced. `S.heroArm`, `drawHeroArm()` and the armed-map tap branch are **deleted** — the strip button now does exactly one thing, fire the Overload Pulse, and reads "PULSE / DEPLOY FROM BUILD MENU" with a banner nudge until a Commander exists. The panel row heights hold up with the extra row: portrait stays at **46 px**, landscape tightens the row gap from 5 to 3 before the rows themselves shrink and lands at **30 px** across the full 166 px width, still inside the play viewport and clear of the side tray. **(2) The build panel is a strict toggle.** It closed on a second tap of the *same* plot but silently retargeted when you tapped a *different* one, which read as "it won't close". Any plot tap while the panel is open now closes it; the next tap opens it on the new plot. One tap, one state change. **(3) The Commander's range ring is on demand.** V6.36's permanent gold ring was visual noise — the user reported "the heroes shoot area is always shown". It now appears when you **tap the Commander** on the map (a toggle, cleared by any other world tap), for **2.5s after it deploys or levels up** (the level-up widens the range, so the flash is the point), and while the **Pulse button is held** — the same press-to-preview gesture the build and upgrade rows already use. `S.heroSel` / `S.heroRingT` replace `S.heroArm` in state. Renamed `fallengrid-v6.37.html`, label + ver → **6.37**. **Verified** (`verify637.py`, 15 checks): panel opens on a plot, closes on a *different* plot, reopens on the next tap, still closes on the same plot; the Commander is row 0 of 8 with a 166×46 hit box; pressing it previews the hero range and tapping it raises the confirm sheet without deploying; confirming deploys, clears `selTile`, drops the row back to 7 and flashes the ring; the strip button pulses a deployed Commander and places nothing without one (`heroArm` asserted gone from state); the ring is hidden by default, toggles on and off by tapping the Commander, and is cleared by a plot tap; landscape fits 8 rows on-screen and clear of the tray; and a full 20-wave headless victory. Plus **all nine** other suites green; 0 page errors. **Two suites updated where the contract changed on purpose:** V6.26 and V6.31 both asserted that tapping the strip HERO button sets `S.heroArm` — they now assert it fires the pulse and places nothing, and V6.36's hero test enters through the panel row instead of the deleted arm mode. `[skip ci]` — no APK.
- **Iter — V6.38 The onboarding actually teaches the UI. ✅ SHIPPED (user-requested; HTML test build, NO APK).** The user's report: *"the intro guide where the user learns to play needs improvement. hero is not placed before start. consider if the intro could be more explanatory of how to use the different parts of the UI and what it means."* **What was actually there:** the in-mission tutorial was **three hardcoded nudges** — build, upgrade, deploy — and then it stopped. It never mentioned the Commander, the command bar, the wave intel, or what the numbers in the top bar mean, so a new player reached wave 1 with no hero and no model of half the screen. The V6.36/V6.37 restructure had also left the Codex Guide describing the *old* controls ("tap ✷ then a target zone", "tap ⚑ then a plot"), which no longer exist. **What shipped — a data-driven guided tour** (`TUT`, 7 steps). Each step names one piece of UI, says what it is **for**, and spotlights the **real element**: (1) *the top bar* — scrap buys towers, core is your life, WAVE is progress; (2) *build* — the glowing plot, then the Auto-Gun row, then the confirm; (3) *upgrade* — and that L3 splits into two specialisations; (4) **the Commander** — a plot, the COMMANDER row, confirm, so **the hero is on the field before wave 1**, which is the specific gap reported; (5) *the command bar* — PULSE, STRIKE, CORE and what each costs; (6) *wave intel* — what is coming and what resists you; (7) *DEPLOY* — and that sending early pays bonus scrap. Steps with a `done` predicate wait for the action; the three read-only cards advance on any tap and say "TAP TO CONTINUE", with an `n/7` counter throughout and SKIP still available at every step. **Two design details that make it robust rather than brittle:** `msg`/`sub` may be **functions**, so a step follows the player mid-flow (plot → tower row → confirm) instead of stranding them on a stale instruction if they back out; and every `spot()` falls back down a chain (confirm button → panel row → a free plot) so **no step can ever leave the player without a pointer** — asserted in the suite. `only` on a step locks the build panel to the row being taught (`tutAllows()` replaced the hardcoded `S.tut === 0 && key !== "turret"`), and the taught row carries the pulsing outline via a shared `tutRowPulse()`. The instruction card sizes itself to the wrapped text, parks at whichever end of the play area the spotlight is **not**, and keeps clear of the build panel when that is open. **Briefing card 4, "YOUR SCREEN":** a labelled wireframe of the real layout (top bar · battlefield · wave intel · PULSE/STRIKE/CORE) so the first thing a new account sees is a map of where everything lives, and the live tour then walks the same four regions. It is re-readable — the Codex "Replay Briefing" button already replays the deck. **A real bug found and fixed while shooting the tour:** the SKIP button sat at `PLAY_TOP + 6`, and in landscape the DEPLOY control hangs ~7px below the shorter HUD, so their hit boxes **overlapped by 5px** — and since SKIP is hit-tested before everything else, tapping the bottom of DEPLOY on the final tour step would have **skipped the tutorial instead of sending the wave**. SKIP now clamps to `max(PLAY_TOP + 6, 60)`, with a permanent regression assertion that the two never intersect. **Codex Guide corrected** to the V6.36/V6.37 controls, plus new lines for the screen anatomy and for Overclock. Renamed `fallengrid-v6.38.html`, label + ver → **6.38**. **Verified** (`verify638.py`, 17 checks): a genuinely fresh account (full `localStorage.clear()`) lands on a 4-card briefing whose anatomy card renders as clean DOM with no stray `<p> > <div>` nesting; BEGIN opens mission 1 with the tour at step 1; the whole tour is then **driven through the real UI, tap by tap**, asserting the panel is locked to the Auto-Gun at the build step and to the Commander at the hero step, that the build step's headline follows through to "CONFIRM ✓ BUILD", that **a Commander is on the field with `wave === 0`**, that the read-only cards advance in order, that all seven spotlights resolve, that wave 1 ends the tour and persists `tutDone`, that SKIP works, that SKIP never overlaps DEPLOY, and that both landscape spotlight rects land inside the side panel. Plus **all ten** other suites green; 0 page errors. **Two suites updated for the deliberate change:** V6.29 now expects 4 briefing cards and asserts the anatomy card's labels; the core regression suite pre-marks `tutDone` alongside its existing `seenIntro` pre-mark, because it drives the UI directly and the tour's first card legitimately swallows a tap. `[skip ci]` — no APK.
- **APK — first real build of the V6 line (run #5, 2026-07-27). ✅ `fallengrid-v6.38.apk`, 3.76 MB artifact.** Built on request from `7d56902` via `workflow_dispatch`; job `89982881641`, Gradle 84s, whole run 2m06s. **What this run proved, beyond producing the file:** (1) the V6.36 file-picker fix works — the bundle step's guard requires the selected file to carry its own filename version as an in-game label, and it passed, so the APK provably contains `fallengrid-v6.38.html` and not a predecessor; (2) the **V6.32 landscape manifest patch executed for the first time**. It was added after the previous build and had never run, so it was unverified until now — the patch script `exit 1`s if `MainActivity` is not found, so a green step means the tag really was rewritten with `screenOrientation="fullSensor"` and the merged `configChanges`. **What the earlier "successful" builds actually contained:** runs #2-#4 (2026-07-23, up to V6.23) all pre-date the picker fix, so each of them bundled `fallengrid-v25.html` — the frozen **V2.5 2D build**. Any APK downloaded from those runs is not the game it claims to be; discard them. **Still unverified and only testable on hardware:** that the APK installs, that the WebView runs the 3D build at an acceptable frame rate, and that rotation genuinely keeps the run alive rather than recreating the activity. **Noted, not fixed:** `app/` has no `package-lock.json`, so `npm install` floats to the newest Capacitor on every build — two builds of the same commit are not guaranteed identical. Worth pinning before any release build.
- **Iter — V6.43 Graphics, part 3: the ground stops being flat. ✅ SHIPPED (user-requested; HTML test build, NO APK in the commit — rebuilt separately).** Prompted by the user's standard — *"maps er levende at se på, dvs. der er variationer i terræn og med genstande smidt lidt rundt over det hele"* — and by my own verification pass, which rendered all 25 maps and had to answer **no**. The props half was fine (0.14-0.25 props per buildable plot on every map, none empty). The terrain half was not: `slabH` produced 0 / 1.1 / 2.2 units of relief **on a 64-unit tile — 3.4%**, which is invisible. The V6.41 note that "the steps form plateaus" was true about their *arrangement* and said nothing about their *size*, and the size was nil. **What shipped:** `STEP = 7`, so relief goes **2.2 → 14 units**, from 3.4% to 22% of a tile and comparable to the 10-unit road trench, which is why it now reads as terraces with shadowed cliff faces. That change is only safe if everything standing on the ground follows it, so all seven prop kinds, the building and dropship landmarks, the sandbags, dead trees, barrels, wrecks and rubble were re-anchored from a hardcoded `y = 0` to `topAt(c,r)` — the suite greps the decor block for any surviving `, 0,` base. Off-path enemies (`enemyGroundY`) now read `tileH` instead of assuming 0; towers already used `tileH`. **The void is gone too:** a dark plain at the base of the cliffs plus two rings of stepped, seeded mesas, so the island sits on ground instead of hanging in black. That took three attempts worth recording — the first put the horizon at 3.2× map size, **outside the fog far plane (2600), so it was fogged to the sky colour and did nothing**; the second pulled it in but made it as bright as the play area, so it crowded the board and was worse than the void; the third measures distance **outward from the island edge** rather than as a multiple of map size (which crowds small maps and vanishes on large ones) and drops it to 0.22-0.34 of the biome ash. **A real cost showed up and was fixed:** the horizon as one big background quad cost **+12.7%** frame time under SwiftShader — over the 10% bound — because the middle is hidden under the island but still shaded and overdrawn. Rebuilt as a **frame** with the centre removed, it lands inside budget at 1208 triangles. Landmark set-pieces also went from `hash2 > 0.988` to `> 0.955`, so most maps now have something that breaks the silhouette. Renamed `fallengrid-v6.43.html`, label + ver → **6.43**. **Verified** (`verify643.py`, 27 checks): relief measured per map, 36-49 units of spread across the ground band on all five sampled maps against 2.2 before; three terrace levels present; **build space byte-identical to V6.42** (69/109/125/165/254 plots); horizon geometry present on every map; 25 towers still placeable per map; 0 page errors. Plus **all sixteen** other suites green. **Still open and stated honestly:** the audio is unchanged since V6.40 and I still cannot hear it — one music loop serves all 25 maps, there is no ambient bed and no per-biome audio, and `Sound.hit` has **0 call sites** (dead since it was written). `[skip ci]` on the commit.
- **Iter — V6.42 Graphics, part 2: blocked tiles become geometry, props cluster, enemies get an accent. ✅ SHIPPED (user-requested; HTML test build, NO APK in the commit — rebuilt separately).** **User-reported bug first:** *"de felter der er spæret, der ligger markeringen oven på fjender der kører på vejen."* Exactly right, and the cause is architectural: `drawObstacles()` painted blocked plots on the **2D UI canvas**, which sits *above* the WebGL canvas, so the marker drew over every enemy behind it. No z-order tweak can fix that — a 2D overlay cannot be occluded by 3D geometry. Blocked tiles are now **pooled 3D geometry**: a dark pad the size of the plot (so it still reads "occupied, clear me" the way the old marker did) plus five rubble chunks, seeded per tile for rotation and scale, pushed in by `G3D.setObstacles()` whenever `BLOCKED_SET` changes. `drawObstacles()` is deleted. **Props cluster (A.2).** `buildDecor` used to give every non-road tile an independent draw from one fixed distribution — spatially uniform, which is precisely what reads as confetti. Now 4-12 seeded centres per map carry the density, each with a theme (vehicle graveyard / shelled ground / dead grove / abandoned camp), tiles beside roads are thinned to 50% because those are the ones the player builds on, and the field is **normalised** so the expected count lands on target however the clusters happen to fall — without that the count swung -15% to -70% map to map on cluster luck alone. **I also relaxed my own acceptance criterion, and it matters:** the plan said hold the prop count within ±15%. Holding it is what makes clustering pointless — at the old 27% coverage the cluster cores saturate and the field flattens back into confetti. I checked what the count actually constrains: `DECOR` is read **only** by the 3D build loop, never by `buildable()`, pathing or combat, so props are purely decorative and the count is a draw-cost question, not a gameplay one. Rate dropped to 0.19; count is **-25%** and build space is **byte-identical** (asserted per map). **Enemy accents (A.3).** Every unit was a desaturated body/dark pair whose only lit element was a small eye. Each now carries an emissive strip on `bodyH` (which every shape branch sets, so it lands on the body whatever the silhouette) sized by an `ENEMY_THREAT` weight, and heavies get flank vents; the Juggernaut is loudest at 1.5. Intensity is capped because these go through the bloom pass. Renamed `fallengrid-v6.42.html`, label + ver → **6.42**; `__decor`, `__ENEMIES`, `__TOWERS` added to the debug hook for the harnesses. **Verified** (`verify642.py`, 26 checks, all before/after against V6.40): one 3D obstacle group per blocked tile on every map; **build space unchanged** (69/109/125/165/254 plots, identical); obstacle counts identical; prop nearest-neighbour **variance up 2.0-5.2×** on 4 of 5 maps — that is the clustering signature — with count 210→157; visible mesh growth stays inside the obstacle budget; frame cost not regressed; 0 page errors. Plus **all fifteen** other suites green. **Two measurement corrections worth recording.** (1) My first prop metric compared whole 24×24 thumbnails, but the subject is ~15% of those pixels for small units, so it measured mostly ground-against-ground and could not resolve anything; masking to pixels that differ from an empty-ground reference flipped the enemy result from "worst pair got worse" (3.03→2.54) to **worst pair improved, 15.65→17.70, with every one of the five worst pairs better and the median up 46.3→49.9**. (2) A "draw calls not increased" check was reading `renderer.info` outside a GL frame and reporting 1 regardless; it is replaced by a **visible-mesh** count, which shows the honest cost of the fix — about six meshes per blocked tile. **The enemy close-up harness is now real** (`tools/enemysheet.py`): it took five attempts, and the failure that mattered was that the RAF loop applies `clampCam()` *after* the camera is set, so reading `worldToScreen` immediately reports where the camera was asked to go rather than where it ended up. `[skip ci]` on the commit.
- **Iter — V6.41 Graphics, part 1: the ground stops being a grid. ✅ SHIPPED (user-requested; HTML test build, NO APK in the commit — rebuilt separately).** First half of *"analysér om du kan forbedre grafikken af både maps, tårne, fjender"*; V6.40 did the audio half. **The audit came first, and it corrected me.** I re-shot every close-up against V6.40 before writing any code, and several of my own earlier findings did not survive: the Juggernaut is *not* a grey box (it has hull, turret, barrel, muzzle brake, two track skirts with eight road wheels, an antenna); the towers are genuinely good — detailed, distinct silhouettes, strong accent rings — so **the planned tower-branch work was dropped outright**; and I nearly shipped a claim that `MeshLambertMaterial` ignores `normalMap` in r147, which I tested and it is **false**. What *did* survive was worse than I had described. **The ground grid, measured:** every tile was one **flat vertex colour** picked from two biome ashes by a per-tile hash, plus a 2×2-tile `patch` multiplier, plus a colour texture repeating every **1.5 tiles**, plus per-tile random slab heights — four independent things all beating on the same 64-unit lattice, which is why the ground read as a literal checkerboard quilt. **What shipped:** colour is now sampled at tile **corners** (`gcol`/`rcol`, memoised) from `vnoise()`, a smooth value-noise field on the tile lattice — two octaves, broad regions plus mid-scale detail plus per-corner grain. Neighbouring tiles read the *same* lattice point, so the shared edge has no discontinuity at all. `quad()` now accepts four per-corner colours so a face can carry a gradient; `slabH` draws from the same field so the broken-slab steps form **plateaus** instead of a quilt; the colour map is 256px with its own `repeat` (0.36) so it tiles every ~4 tiles while the normal map keeps its fine 1/96 projection; the `patch` term is gone. **A real bug fell out of it.** Ground decals — craters, pool rims, dropship scorch — rendered as **hard black holes**. Four hypotheses died before the right one: not the glow sprite, not SSAO, not the normal map, not the colour. `discM` wound its triangle fan **backwards**, and because the land mesh is `DoubleSide`, three.js flipped the normal for those faces, so **every ground decal has been lit from underneath since V3**. One index swap fixed 538 back-facing triangles across three maps. Decals also sat at a hardcoded `y=0`, i.e. buried under any raised slab — now `groundYAt()`. **Also deleted ~295 lines of dead 2D terrain bake** (`buildTerrain`, `groundTile`, `roadTile` and ten `draw*` helpers) — no callers since V3, found by a fixpoint reachability pass, and the source of a comment claiming there was "no hard checkerboard" that flatly contradicted the live renderer. Renamed `fallengrid-v6.41.html`, label + ver → **6.41**. **Verified** (`verify641.py`, 16 checks, every one a before/after against the V6.40 baseline rather than an absolute): the worst colour delta at shared ground corners goes **0.229 → 0.000** (map 0), **0.268 → 0.000** (map 9), **0.272 → 0.000** (map 24); back-facing land triangles go **62 → 0**, **182 → 0**, **294 → 0**; mesh count and **draw calls unchanged (1)**; frame cost not regressed (358 → 344 ms median, SwiftShader software rendering, so relative only); dead symbols all absent; 0 page errors. Plus **all fourteen** other suites green. **Still open, deliberately:** props are still placed by a uniform per-tile hash and still read as confetti — that is A.2, scheduled for V6.42 with the enemy accent pass. `[skip ci]` on the commit.
- **Iter — V6.40 Audio: offline-rendered sample bank with real variation. ✅ SHIPPED (user-requested; HTML test build, NO APK in the commit — rebuilt separately).** User ask: *"analysér om ... lyden kan forbedres og gøres mere alsidig og lyde mere naturlig"*, then *"lav en plan og udfør den"*. **The measurement that set the direction:** the V6.39 `Sound` module contained **41 oscillator calls but only 4 uses of `Math.random`**. Nearly every sound was *byte-identical every time it played*. An auto-gun firing 200 times in a wave produced the same waveform 200 times. That repetition — not the timbre — is what the ear reads as artificial, and no amount of retuning oscillators fixes it. Runtime synthesis also caps quality: it gets one frame's CPU, so no convolution tails and no multi-stage processing. **What shipped:** every sound is now rendered **offline** in Python/numpy (`tools/render_audio.py`, seeded `default_rng(20260728)`, so it is deterministic and re-runnable), encoded to Opus by ffmpeg, and embedded as base64 in **its own `<script>` block** — **17 events, 38 SFX variants, plus a 22.5 s music loop with a 1.5 s tail→head crossfade so the loop point is inaudible. 540 KB of base64, inside the 600 KB budget.** Playback decodes once on the first user gesture; every shot picks a variant (never the same one twice running) and jitters **pitch ±6%** and **level ±1.5 dB**, so even the same variant is never identical twice. `gun` gets 4 variants because it fires most; `sell`/`win`/`lose` get 1 because they are rare or deliberately fixed. Combat sounds now take **optional world coordinates** — an additive change, existing zero-arg calls still work — and 10 call sites pass them, giving stereo pan and distance attenuation from the play viewport. SFX and music got **separate buses**, and loud events (boom, strike, siege, leak, wave, win, lose) duck the music to 0.45 for ~1.2 s instead of fighting it. `AudioContext` now asks for `latencyHint: "interactive"`. **The procedural synth is untouched and still the fallback** — renamed `proc`, wired underneath — so if the bank is absent, a decode fails, or WebAudio is refused, the game sounds exactly like V6.39 rather than falling silent. **The public API did not change:** all 25 members, ~70 call sites, asserted in the suite. Renamed `fallengrid-v6.40.html`, label + ver → **6.40**; `Sound` added to `__GAME` with a `bankState()` probe. **Verified** (`verify640.py`, 24 checks): the bank ships, sits in its own block, decodes to ready, has exactly 18 events / 39 variants; gun/tesla/boom variants are measurably different buffers (mean abs sample diff 0.10–0.16); **every banked event actually reaches a sample rather than the synth**; 30 gun shots produced 30 distinct playback rates, all inside ±6%, across all 4 variants; world position drives pan (−0.69 vs +1.00 for opposite screen edges); the music bed loops at 22.5 s; the SFX toggle still mutes the bank; and **with `window.__AUDIOBANK` renamed away, `bankState()` reports −1, the procedural fallback still fires 43 oscillators, and the page throws nothing**. Plus **all thirteen** other suites green; 0 page errors. **One real bug the suites caught:** passing `hero.x` into the deferred `setTimeout(() => Sound.boom(...))` in the hero ability threw when the run ended before the 40 ms timer fired — the coordinates are now captured into locals first. **The honest limitation, stated up front and still true: I cannot hear any of this.** I can measure peak, RMS, clipping and DC offset (0 clipped samples across all 39 renders) and reason about the DSP, but whether it *sounds good* is not something I can check. `tools/preview/` holds a WAV of every render plus an `index.html` audition page; the user is the ear.
- **Iter — V6.39 The play-log export works in the app. ✅ SHIPPED (user-requested; HTML test build, NO APK in the commit — rebuilt separately).** User report: *"the export player log does not work in the app"*, with an Armory screenshot supplied instead. **Root cause, and it is a good lesson:** `Telemetry.exportJSON` built a `blob:` URL and clicked a synthetic `<a download>`. **Android's WebView has no download manager.** An anchor download only reaches native code via `WebView.setDownloadListener`, which this app does not wire, and `blob:` URLs are never handed to that listener even when it exists. So the tap did nothing — and the entire body sat inside `catch (e) {}`, so it failed **completely silently**. It works in a desktop browser, which is exactly why every Playwright suite passed it for months: *the test environment had the capability the target platform lacks*. **What shipped:** `copyJSON(done)` puts the payload on the clipboard — the only route that works in both a desktop browser and the WebView, and the one that puts the log where it actually needs to go, pasteable. `navigator.clipboard.writeText` first (Capacitor serves from `https://localhost`, so the secure-context requirement is met), `document.execCommand("copy")` for older WebViews, and `done(ok, how, kb)` **always** fires so the UI can tell the truth. The Play Log screen now reports "✓ Copied N runs (X KB) — paste it anywhere" in green, or "✗ Clipboard blocked…" in red with a pointer to the manual route. A **Show raw** toggle reveals the JSON in a read-only selectable box for long-press copying, which works even with the clipboard fully blocked. `downloadJSON()` keeps the file route for desktop browsers, and the button is **hidden inside the app** (detected via `window.Capacitor`) rather than offered and silently failing — we cannot detect that failure from JS, so the honest move is not to promise it. Renamed `fallengrid-v6.39.html`, label + ver → **6.39**. **Verified** (`verify639.py`, 10 checks, with `clipboard-read`/`clipboard-write` granted so the assertions are real): seeded runs reach telemetry; the payload is valid JSON matching the store; the screen exposes copy/status/raw; tapping Copy puts a **byte-identical, JSON-parseable** payload on the actual system clipboard and reports success; the raw box carries the same text and toggles; **with `navigator.clipboard` deleted and `execCommand` forced false the UI reports failure in red instead of failing silently**, and the manual route still works in that state; and with `window.Capacitor` faked the download button disappears while copy and raw remain. Plus **all eleven** other suites green; 0 page errors. `[skip ci]` on the commit — the APK is rebuilt on demand.
- **Armory pacing read from a device screenshot (V6.38, 8 Hard missions).** The user could not export the log, so they sent the Armory screen. It is enough to answer the pacing question: Ordnance 7/8, Arc 7/8, Logistics 7/8, **Command 0/8** — which is exactly **11,170 Alloy** spent (3,575 + 4,020 + 3,575), so **≥1,400 Alloy per mission on Hard**. Projected against the V6.36 costs, the 20,010 reachable before the mission-12 gate arrives around **mission 14**, and the full 25,380 around **mission 18 of 25** — against V6.35 where the tree maxed at mission 9. **The re-cost worked.** User also reports the difficulty feels right and clears Hard maps with almost no leaks, so the costing is left alone. **Open design finding, not yet actioned:** Command at 0/8 while every other branch sits at 7/8 is not noise. Five of Command's eight nodes buff the Commander and the Airstrike and two add Core, so **the whole branch only pays off when you are losing** — a player who defends well never has a reason to open it, leaving a quarter of the tree dead. The other three branches are unconditional multipliers on damage, rate, range and economy. Fixing this changes what the branch *is* (give it something unconditional / make the Commander load-bearing / accept it as an insurance branch and stop treating 32/32 as the target), so it is left for an explicit decision.
- **Iter — V6.44 Per-map threat identity: a map now has a profile you can prepare against. ✅ SHIPPED (user-chosen from a design review; HTML test build, NO APK).** Chosen from a five-point critical review of the project's design decisions. **The finding:** `waveComp(n)` took only the wave number. Nul references to `mapIndex`, `MAPS` or biome anywhere in the function, and `BIOMES` holds only colours (`skyTop`, `ash1`, `road`, `mote`) with no mechanical field. So all 25 sectors ran the identical 20-wave enemy script, and a map's difficulty came entirely from geometry plus one flat tier multiplier (`MAP_HP_BY_DIFF` 1/1/1.15/1.35/1.6). `docs/ANALYSIS-prerelease.md` raised this at 18 maps and it was still true at 25: the player learns the script by about mission 5, and missions 6-25 add no new tactical information. **What shipped:** an optional `MAPS[i].bias` naming one or two of five profiles, applied by `applyWaveBias()` through a `WAVE_BIAS` table of substitutions. **Every swap moves units between types in the same HP class** — air raider→wraith (34→38), cloak raider→phantom (34→46), armor brute→sentinel (105→92), shield cinder→warden (58→66), energy brute/splitter→insulator (105/110→128) — so a biased wave carries the **same unit count and, measured, within 3.5% of the same HP budget**. That is the whole safety argument: a bias changes *which* counter the wave demands, never how much wall it brings, so the curve tuned across V6.4/V6.5/V6.12 does not move. **Only Tier C/D carry a profile** (15 maps, Delta through Ground Zero); the ten Tier A/B maps have no `bias` field at all and are asserted to compose identically to each other, which is what keeps the fresh-account onboarding curve confirmed by play-log #12 provably untouched. Boss waves are excluded. The bias runs **before** V6.26's detector rule, so a cloak-biased map still substitutes phantoms away correctly on an un-researched account (verified: 0 phantoms, wave size still 57). Delivery needed no new UI: the V6.26 Wave Intel bar already reads `waveComp`, so a biased map announces itself in the threat cards and the targeted warnings on the first wave. **Two real bugs fixed in the same function.** (1) `seq.sort(() => Math.random() - 0.4)` is not a shuffle — a comparator returning random values makes the sort's result implementation-defined, and the `-0.4` bias skewed arrival order systematically toward insertion order. Replaced with Fisher-Yates; measured, the leading unit of a wave is now proportional to its count (stalker 0.042-0.065 observed against 0.053 expected) and **ten** different types can lead a wave. Counts were never affected, so `waveIntel` stayed exact throughout. (2) That fix exposed a second one: `waveIntel` sorted threat cards by count alone, and `comp` is keyed in the order types appear in the now-properly-shuffled sequence, so **with three threats tied at 5 the top-3 cards varied run to run for the same wave on the same map**. Ties now break on `THREAT_TAG`'s fixed key order. Renamed `fallengrid-v6.44.html`, label + telemetry ver → **6.44**; telemetry records `bias`; `__waveCounts`/`__WAVE_BIAS`/`__MAPS` added to the debug hook. **Verified** (`verify644.py`, 30 checks): count preserved exactly across 105 map/wave combinations; HP budget worst case 3.50%; each profile visibly moves the unit it names (air wraith 6→11, cloak phantom 4→8, armor sentinel 5→8, shield warden 3→5, energy insulator 5→8); all ten Tier A/B maps identical; boss cadence unchanged; detector rule intact under a cloak bias; intel stable across a map switch; all 25 maps queue and land a wave; a no-tower run still resolves to gameover. Plus **all seventeen** other suites green; 0 page errors. `[skip ci]` — no APK.
- **Balance measurement for V6.44, and the harness lesson in it (`tools/biasmeasure.py`).** New tool. It A/Bs the **same map with the same board, `bias` off vs on**, since `__MAPS[i].bias` is mutable at runtime — anything else confounds the profile with the map's geometry. The design intent is asymmetric and that is what it checks: a profile must cost a board that ignores the counter it names and must **not** cost a diverse board. **Result: `air` is confirmed.** Mono-Mortar (zero anti-air) on Delta leaked **711±6 → 742±8 (+4.3%)** and on Ground Zero **810±6 → 844±5 (+4.1%)**, roughly four standard deviations, while the diverse control on Delta moved **+0.8%** — the intended asymmetry, cleanly. **`energy` first read as dead and that reading was wrong.** At a 10-tower board it measured +1.0% and −0.3% against ±5% noise. The cause was the instrument, not the feature: at 10 towers the board leaks 235-438 over the run, i.e. it is drowning, and a drowning board leaks the same amount whatever the composition is. Re-run on Sprawl at 22 towers, where the board actually holds, mono-Railgun went **12±6 → 24±6 (+105%)** with the diverse control flat at **12±1 → 13±1 (+8%)**. Nothing about the game changed between those runs. **The lesson is recorded in the tool's docstring:** check the absolute leak count first — if it is in the hundreds the board is saturated and the run is measuring nothing. This is the same shape as V6.25's DPS-per-scrap error (tuning on a metric that is not the binding one) and as the bimodal auto-player blocking `PLAN-commander.md` §5.5. The Maw stays saturated even at 22 towers and is too large for this probe. **The real acceptance test is the next play-log**, per this project's standing practice: `bias` is in the telemetry, so a run on a Tier C/D map can be read directly for whether the profile changed what the player built.
- **Test debt paid down (V6.44).** Four suites carried their own `chk("version label bumped", "V6.4X ·" in html)`. That check pins a suite to the version it was written at, so **every `git mv` to a new build breaks it** — 641 and 642 had been hand-patched forward to "6.43" rather than fixed, and all three of 641/642/643 failed against 6.44 for this reason alone while their real assertions passed. Removed from all four: `run_all.sh` step 3 already enforces the filename/label agreement the APK workflow depends on, and it **derives** the version from the filename. Also new: **`H.game_block(html)`**, because a source assertion that greps the raw file is also grepping three.js — whose single minified line contains both `.sort(` and `Math.random`, which is what made the new shuffle check fail against correct code. Suites should scope source greps with it. Same family as the V6.30 "never select a script block by size" fix.
- **Next line — V7 "Google Play + Ads" (SPEC'ed, not started).** Full spec, iteration plan, and a ready ads-facade code example live in **`docs/monetization/`** (`SPEC.md` = design/balance/compliance, `PLAN.md` = V7.0–V7.4 iterations with exact code anchors, `ads.example.js` = facade with Null/AdMob backends). Headlines: interstitial after every 3rd finished run (240s min gap, onboarding grace, 6/day cap), three rewarded slots (R1 Second Wind revive — never ✦-perfect, not in Daily; R2 Double Alloy — 3/day; R3 +50% start scrap), no banners; V7.0 builds the whole flow browser-testable with a Null backend before any Android work; Capacitor + `@capacitor-community/admob` + UMP consent; target SDK 36.
- **Iter — V6.8 Hero relevance (light, optional).** One-time deploy tip + small buff; the hero is now organically used (heroLvl 5 in most serious runs) — measure before doing more.
- **Backlog (structural, deferred) — Extra pre-specialisation upgrade tier.** Insert one plain upgrade level before the L3 branch choice (L1→L2→L3 plain→L4 specialise→L5 branch-t4) as a scrap depth-sink. Touches `TOWERS[].levels`, the branch-gate level, upgrade UI, Codex tables, `drawTowerArt`, and all branch-at-L3 tests. **Low priority** — play-log #5 shows the V6.5 taper already tamed the surplus (1.6–3.5k), so the original justification has largely evaporated.

## 2.1 Where things are

```
fallengrid-v6.44.html  the current build — ship this. REAL 3D, 25 maps, 7 towers, research tree
fallengrid-v3.html     frozen: V3.6, the first real-3D build (see Part 4)
fallengrid-v2.5.html   frozen: V2.5, the last full-2D Canvas2D build
fallengrid.html        frozen: V2.4
HANDOFF.md             this document (source of truth)
README.md              repo readme — orientation for a newcomer
docs/TECHTREE-spec.md  design of record for the Armory research tree
docs/FEEDBACK-backlog.md  play-test feedback, each point with the measurement and what shipped
docs/PLAN-feedback.md     the plan those points were built from (all shipped)
docs/ANALYSIS-prerelease.md  critical pre-release review + the release checklist (unstarted)
docs/monetization/     deferred V7 monetization plan and spec
docs/PLAN-audio.md     the V6.40 audio plan, and what it actually shipped as
docs/PLAN-open-findings.md  the graphics plan (Part A) and how the Command decision was reached
docs/PLAN-commander.md      V6.43/V6.44: making the Commander load-bearing (planned, not started)
tools/README.md        how to run the harness + the environment it needs
tools/gamecheck.sh     game-block syntax check — marker-based, never selects by size
tools/verify/          the standing suite set + run_all.sh (the whole definition of done)
tools/verify/_harness.py  finds the build, the browser, and old builds from git history
tools/mapsheet.py      renders all 25 maps + measures props/relief/colour spread
tools/heromeasure.py   Commander damage share vs the tower line (the B.2 measurement)
tools/enemysheet.py    renders all 12 enemies at one camera + measures thumbnail confusability
tools/propmeasure.py   prop count and nearest-neighbour spread, before/after a baseline build
tools/biasmeasure.py   V6.44 map-bias A/B: same map, same board, bias off vs on
tools/render_audio.py  the offline sound renderer — seeded, deterministic, re-runnable
tools/audiobank.js     its output; spliced verbatim into the HTML. Never hand-edit.
tools/preview/         WAV renders + index.html audition page (gitignored — re-run the renderer)
app/ + .github/workflows/android.yml   Capacitor wrapper and the APK build
```
Each iteration is a **new version-numbered copy** (`git mv fallengrid-v6.42.html fallengrid-v6.43.html`), so every shipped build stays reproducible. **The filename version must match the version label inside the file** — the APK workflow selects by `sort -V` and fails the build if they disagree. That guard exists because `fallengrid-v2.5.html` (meaning V2.5) version-sorted as 25 and beat v6.38, so the APK would have silently bundled a 2D build from months earlier; it is now `fallengrid-v2.5.html`.
Git: work on branch `claude/design-decisions-review-ot9a2v`. No build artifacts committed.

> **The verification harness is in the repo (`tools/`), not in a session scratchpad.** It used to
> live in the scratchpad, which meant the entire standing suite set died with the conversation that
> created it and a new session could not run the definition of done at all. `docs/ANALYSIS-prerelease.md`
> flagged that risk and it went unfixed for months. Nothing in `tools/` may reference a path outside
> the repo; the before/after suites recover old builds from **git history** via `tools/verify/_harness.py`.

## 2.2 Edit and verify loop

Edit the current versioned HTML directly. It has **five** `<script>` blocks — select the game one by marker, never by size (see 2.7). After any change run the checks in 2.7 before declaring done.

## 2.3 Environment constraints

No Android SDK/emulator; `dl.google.com`/Google Maven not allowlisted. Cannot compile/sign an AAB here. Produce the web build + Capacitor project; the user builds and signs locally. Chromium is pre-installed at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (Playwright, `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1`).

## 2.4 Conventions

Single IIFE, `"use strict"`. Terse helpers (`clamp`, `lerp`, `rr`, `hash2`, `mix/lighten/darken/rgba`). Colors centralized in `C`; tuning is data-driven in `TOWERS`/`ENEMIES`/`TALENTS` — prefer editing those tables over logic. World drawing goes inside the camera block; screen UI after `restore()`. Color helpers return `rgb()/rgba()` strings so the hex scan stays clean; keep literal hex colors valid.

## 2.5 Quality assessment vs genre leaders (Kingdom Rush, Bloons TD 6, Arknights)

Mechanically the game is at genre standard: depth (counterplay triangle, branches, hero, airstrike), retention (Alloy/Armory, endless, campaign), content (10 maps, 4 biomes), feedback, audio, onboarding, and a verified balance curve. The remaining distance to the flagships:
- **Presentation** is now the #1 gap — see Part 3 for the full root-cause analysis and plan. Short version: the game is drawn in true top-down with no verticality, so nothing has fronts/sides; leaders render in a tilted 3D-ish view where everything is a volume.
- **Enemy design**: 5 traits deep (armor/shield/flying/heal/split) — now comparable to mid-tier leaders; teleport/burrow-style movement tricks remain unexplored.
- Content keeps scaling value (more maps/enemies) but is no longer structurally behind.
- No liveops/social/monetization (not a craft gap).

## 2.6 Backlog, prioritized (remaining)

1. **Art assets, pass 2 — the 10 enemies** (towers + hero done in V3.4). Rebuild each enemy with the V3.4 `Model()` toolkit as a detailed, merged, characterful creature/machine (strong silhouette + material zones + emissive), replacing the current primitive bodies in `buildEnemy`. Keep the animated parts (legs, wraith ring) separate from the merged body. Same pipeline as the towers. **Ceiling note:** code-authored detailed models (the V3.4 approach) get us to a strong, cohesive stylized look; the absolute top tier (Bloons-TD-6 hand-sculpted characters) still needs **hand-authored GLTF from a 3D artist / licensed packs** dropped in at the `buildTower`/`buildEnemy` sites — an art-production task an LLM can't author at that quality. Flagged honestly.
2. **Camera feel & accessibility** — pan inertia, zoom easing, colorblind-safe enemy/HP palette, larger-touch-target option.
3. **Android port** — Capacitor scaffold, icons/splash, signed AAB (done by the user locally).

Done and verified: endless + map roster (Plan A), balance pass + sim harness (Plan C), Hero/Commander (Plan B), biomes, graphics uplift pass 1, Visual Direction V2.1–V2.5 (V2.5 in `fallengrid-v2.5.html`), difficulty tiers, behavior enemies + campaign (14–15), REAL 3D V3.0–V3.3 (three.js, PBR, env map, normal maps, EffectComposer post — in `fallengrid-v3.html`).

---

# Part 3 — Visual & Map Direction V2

The re-evaluation asked two questions: **how do we make the game read like a 3D-rendered title**, and **how do we make map layouts feel composed rather than simple**. This part is the answer and the plan. Plans A/B/C from the previous handoff revision are complete and archived in the changelog (2.0).

## 3.1 Re-evaluation: why the game still reads "flat 2D"

Judged against Kingdom Rush (painted 3D-ish perspective) and Bloons TD 6 (real 3D renders), the current build's gap is not detail — pass 1 added plenty — it is **projection and volume**. Root causes, in order of impact:

1. **True top-down projection.** Everything is drawn from directly overhead, so no object can show a front or a side. The leaders render from a tilted ~50–65° camera: every tower, tree, and wall shows a **top AND a front face**. Verticality is the single strongest "3D" cue and we have none of it.
2. **No extrusion.** Objects are silhouettes filled with gradients. There are no side walls on the sunken roads, no plinths under towers, no wall faces on buildings' lower halves. Volume = top face + side face + cast shadow; we only have tops.
3. **No directional cast shadows.** We have contact blobs under entities, but a real scene has one global light with **elongated shadows falling one consistent direction** from everything — that is what visually welds objects to the ground plane.
4. **Per-frame vector redraw caps the detail budget.** Every tower/enemy is rebuilt from primitives 60×/s, so each can only afford a handful of gradient layers. The leaders' sprites are *renders* — hundreds of lighting passes baked offline. We can get most of that headroom by **baking our procedural art into sprites at load** and blitting.
5. **The board is a wall-to-wall rectangle.** Leaders float an irregular "island" board over a backdrop with visible cliff edges — the world reads as a diorama on a table. Ours runs edge-to-edge with hard clipping, killing the diorama depth cue.
6. **Map composition is uniform.** Decor is hash-scattered, corners are hard 90° squares, spawn/base are small glows rather than structures, and there is no elevation. Nothing anchors the eye; layouts feel generated, not designed.

**Projection decision.** Four options were weighed:

| Option | Look | Cost | Risk | Verdict |
|---|---|---|---|---|
| A. Stay top-down, more detail | incremental | low | low | insufficient — detail was pass 1; projection is the gap |
| B. **Faux-3D extrusion** (keep top-down math, draw every object with height: top + front face + directional shadow) | "tilted board" illusion, à la many premium 2D TDs | medium | low — gameplay/input math untouched | **chosen core** |
| C. World y-foreshortening (scale world y ~0.85 in the camera, full oblique view) | strongest tilt illusion | medium | medium — `screenToWorld`/tap must be updated symmetrically | optional layer on top of B, behind a flag |
| D. WebGL/three.js real 3D | true 3D | very high (renderer rewrite, breaks single-file/no-framework constraint) | high | rejected for now; documented exit if the game outgrows Canvas2D |

## 3.2 The plan — phases V2.1 → V2.5

Each phase is a separate verified commit. All must keep the 1.10 guards, the "terrain bakes once per load" rule, and 60fps on the largest map (13×22, 2× DPR).

### V2.1 — Extrusion & global light (the projection change)

**Design.** Introduce a global light constant (`LIGHT = upper-left`, matching the baked gradient) and a world-standard **elevation rendering idiom**: any object with height `h` draws (bottom→top) directional cast shadow (offset SE, length ∝ h, soft) → front/side face (darkened body color, height `h`) → top face (existing art, offset up by `h`). Apply it to the **baked terrain first** — the highest coverage per effort:
- Sunken roads: visible **inner north wall** (light catches it) and shadowed south lip, so paths become real trenches.
- Buildable ground gets subtle raised-pad edges every few tiles (broken slabs), giving the plane texture without noise.
- Props re-rendered with the idiom: buildings get full front faces with doors/windows; sandbags, barrels, rubble, wrecks get fronts + directional shadows; dead trees get long ground shadows.
- Spawn/base become **structures**: breach = ruined tunnel arch with dark interior; base = walled fortress compound around the reactor.
**Touch-points.** `buildTerrain`, `roadTile`, all `draw*` decor functions, `drawSpawnAndBase`. All baked; zero frame cost.
**Risk.** Low (visual-only, baked). **Estimate.** ~1 session.
**DoD.** Roads read as trenches with lit/shadow walls; every prop casts a consistent SE shadow; spawn/base are structures; screenshots on 3 biomes; 60fps.

### V2.2 — Map Layout 2.0 (composition)

**Design.**
- **Island board.** Bake an irregular edge mask: the outermost ring of tiles crumbles into cliff edges with visible dark rock faces (extrusion idiom) over a deep backdrop (biome-tinted void + faint distant silhouettes). The map becomes a floating diorama. Board edge never cuts a route.
- **Curved corners.** Road corner tiles render as quarter-arc asphalt (with curb + tracks following the curve) instead of squared blocks. Pure `roadTile` change keyed on the neighbor pattern; enemy waypoints unchanged (the visual arc covers the same tile).
- **Bridges at crossings.** Where two routes' tiles overlap (Crossroads, Twin Gates), render route 2's crossing tile as a **bridge deck** (planks/steel + railings + shadow cast onto the road below). Data already knows the overlap; flyers/ground logic unchanged — it is a pure render upgrade that makes crossings a landmark.
- **Authored set-pieces.** Add optional `props: [{c,r,type}]` to `MAPS[i]` for hand-placed landmarks (e.g., the dropship crash framing Outpost's first corner) so each map has a composed focal point; hash decor fills around them.
- **Elevation tier (stretch).** A per-map `plateau` region: tiles one tier up with cliff-wall faces; purely visual first (towers on it just look elevated), optional +range gameplay later.
**Touch-points.** `MAPS` data, `buildDecor`, `buildTerrain`, `roadTile`, new `drawCliffEdge`/`drawBridge` bakes.
**Risk.** Low-medium (baked; bridge draw order needs care at crossings). **Estimate.** ~1 session.
**DoD.** Every map reads as a floating island; corners curve; both crossing maps show bridges; at least 2 maps have authored set-pieces; routes/gameplay unchanged (no-tower sim still reaches gameover on all maps); 60fps.

### V2.3 — Procedural sprite baking (the detail unlock)

**Design.** A `bakeSprites()` step at load (and on map/biome change if tinted): render each **tower base** (per type × level × branch) and each **enemy body** (per type) once into offscreen canvases at 2–3× resolution with a far richer pass stack than the frame budget allows — base coat, noise/wear texture, AO pass, bevel highlights, rim light, decals. Frame loop blits sprites (`drawImage`) and keeps only the dynamic parts vector: rotating heads/cannons, legs/treads (animated), glows, muzzle flashes, shields. This is the original handoff's sprite-pipeline item, self-sourced: same architecture as dropping in a Kenney atlas later (the blit sites become the atlas API).
**Touch-points.** New `bakeSprites()`; `drawTowerArt` split into `bakeTowerBase` + dynamic head; `enemyBody` split into baked body + dynamic limbs; cache keyed by type/level/branch.
**Risk.** Medium — draw-order and rotation seams; memory for sprite canvases (bounded: ~20 towers + 7 enemies × small sizes). **Estimate.** ~1–1.5 sessions.
**DoD.** Frame loop contains no per-frame gradient construction for bodies/bases; visual quality strictly better in side-by-side screenshots; 60fps with 30 towers + 40 enemies; walk/turret animation intact.

### V2.4 — Lighting & post pass (the "render" feel)

**Design.**
- **Bloom compositing:** all emissives (accents, cores, beams, pools, breach) draw to a half-res offscreen glow layer each frame, blurred once, composited additively — real bloom instead of per-shape `shadowBlur` (also a perf win: removes many shadowBlur calls).
- **Cloud shadows:** two huge soft dark blobs drifting slowly over the terrain (screen-space multiply), selling the sun.
- **Biome color grade:** a final translucent gradient overlay per biome (already partial via baked light; extend to entities by applying it after the world layer).
- Muzzle light: firing towers briefly tint nearby ground (radial gradient, cheap, capped count).
**Touch-points.** Frame compositor (one offscreen canvas), `drawBeams`/`drawBooms`/glow call sites, `drawAtmosphere`.
**Risk.** Medium — the only phase touching per-frame architecture; must profile on 2× DPR. **Estimate.** ~1 session.
**DoD.** Bloom visibly unifies emissives; fps ≥ 58 on the largest map with heavy combat; toggling the glow layer off is a one-line fallback.

### V2.5 — Optional tilt (projection C) — TRIED AND REVERTED (decision final)

**Outcome.** Implemented in `fallengrid-v2.5.html` (`TILT = 0.85` ground foreshortening in both camera transforms, all screen↔world conversions symmetric, `uprightAt(y)` counter-scaling entities/floats/base upright) and verified mechanically sound (154-tile round-trip exact, taps accurate, 56–60fps). **User playtest verdict: rejected.** Squashing top-down-authored terrain does not create perspective — the map still reads flat 2D while ground-plane ellipses (tower range rings) imply a perspective that isn't there; the cues contradict. Reverted to `TILT = 1` (exact full-2D math; the scaffolding remains inert in the file with a comment recording the verdict).
**Lesson recorded.** An affine y-squash cannot tilt art that was authored top-down; the projection must be baked into the art itself. A genuinely 3D view is the documented **WebGL exit** (Part 3 "Out of scope") — a renderer rewrite behind the same game state — not a camera transform. Do not re-attempt tilt-by-scale.
**Presentation direction.** The game commits to the polished top-down look (V2.1–V2.4 extrusion/light/bake/bloom), where circles are circles and all cues agree.

### Sequencing & constraints

**V2.1 → V2.2 → V2.3 → V2.4 → (V2.5 if needed).** The first two are baked-only (zero frame cost, low risk) and deliver the biggest projection change; sprite baking then raises the entity ceiling; the post pass unifies it. Constraints throughout: single file, no frameworks, no external assets, `buildTerrain` never in the frame loop, 1.10 guards intact, verify per 2.7 (syntax, hex, headless run incl. a no-tower route sim per map, screenshots per biome, fps sample).

**Out of scope / exits.** Real 3D (WebGL) remains the documented exit if the game outgrows Canvas2D — it would be a renderer rewrite behind the same game state, and nothing in V2.1–V2.4 is wasted (the baked art becomes texture sources). External art packs remain compatible: V2.3's blit sites are exactly where a downloaded atlas would plug in.

## 2.7 Definition of done for any change

0. **Just run `tools/verify/run_all.sh`.** It does everything below — syntax, both guards, the
   filename/label agreement the APK workflow enforces, and every suite — against the newest
   `fallengrid-v*.html` (or a path you pass it). The steps are spelled out individually because you
   still need to understand what each one is for.
1. **Syntax + hex.** Run `tools/gamecheck.sh <file>`. It selects the game block by the markers
   `PLAY_BOTTOM` **and** `function drawTray`, hard-fails unless exactly one block matches, then runs
   `node --check` on it and scans for malformed hex literals. Since V6.40 the file has **five** blocks
   (the audio bank is its own); the markers still match exactly one, which is the whole point of them.

   > **Corrected — this step used to say "extract the largest `<script>`".** The file has four script
   > blocks and the largest is **three.js (607 KB)**, not the game (~395 KB). For an unknown number of
   > iterations that check was validating the vendored library and never the code being changed. No bad
   > code reached a build (the Playwright suites execute the real game with a `pageerror` listener and
   > would have caught a parse error), but the check itself was worthless. **Never select a script block
   > by size.**
2. **Headless logic/render.** Playwright + Chromium (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, args `--no-sandbox --enable-unsafe-swiftshader`) driving `__GAME`: assert waves resolve, auto-advance fires, no NaN in `S`/enemies across several waves (incl. a boss shield wave), and capture at least one screenshot confirming the change renders. Watch `pageerror`/console for zero errors. **Write a new `verifyNNN.py` for the iteration's own claims, and re-run every existing suite** (core regression plus the per-feature suites) against the new file — the standing set lives in `tools/verify/` — one file per iteration, eighteen as of V6.44, all run by `run_all.sh`.

   > **Two RAF traps, both of which have cost a debugging session.** (a) The tray, HUD and build panel are drawn by `requestAnimationFrame`, **not** by `render()` — wait ~250-300ms before reading `__GAME.trayBtns` / `buildBtns` / `hudBtns`, or they are stale or empty. DOM overlay screens do come from `render()` and can be read immediately. (b) Seed `localStorage.setItem('seenIntro','true')` before reloading, or a suite that clears storage lands on the first-launch briefing instead of the game — and since V6.38, seed `tutDone` too if the suite drives the UI directly, because the tour's first card legitimately swallows a tap.
3. **Performance.** Sample frame times; hold ~60fps. Never move baked terrain work into the per-frame path.
4. **Guards.** The two wave-progression guards in 1.10 remain intact.
5. State any balance or UX assumption made.

## 2.8 Android port path

1. Capacitor app, `webDir` = `www`, copy `fallengrid.html` → `www/index.html`.
2. `npm install`, `npx cap add android`.
3. Icons/splash via `@capacitor/assets`.
4. `npx cap sync`, `npx cap open android`.
5. In Android Studio set applicationId, build a signed release AAB, upload to Play Console (content rating, privacy policy, store assets are not code). No known technical rejection cause.

---

# Part 4 — V3.0 Real-3D Renderer (`fallengrid-v3.html`)

The WebGL exit documented in Part 3 was taken on user request. V3.0 replaces the world renderer with a true 3D scene; **everything else — game state, simulation, waves, economy, UI panels, HUD/tray drawing, audio, the `__GAME` sim harness — is byte-identical logic carried over from V2.5.** The 3D module only *reads* game state.

## 4.1 Architecture

- **Single file, still offline.** three.js r147 (UMD build, 607KB, global `THREE`) is embedded inline as the first `<script>`; the game IIFE follows. No CDN, no build step. r147 is pinned deliberately: it is the last release line with a UMD single-file build.
- **Two stacked canvases.** `#gl` (WebGL, z-index 1) renders the world; `#game` (Canvas2D, z-index 2, transparent) renders ALL screen-space UI exactly as before: HUD, tray, banners, FABs, boss bar, tutorial, plus the projected overlay (enemy health bars, damage floats). `frame()` clears the 2D canvas, calls `G3D.frame(shake, dt, dtMs)` then `G3D.overlay()`, then the unchanged screen-layer draw calls.
- **The `G3D` module** (IIFE inside the game IIFE, also on `window.G3D`) exposes: `buildMap()` (called by `loadMap` — replaces the 2D terrain bake), `frame(so, raw, dtMs)`, `overlay()`, `pick(sx, sy)`, `project(wx, wy, h)`, `resize(sc, dpr)`, `applyCam()`, plus `scene/camera/renderer` for debugging.

## 4.2 Camera & input (the critical contract)

`cam { x, y, zoom }` keeps its exact V2 semantics — a virtual top-down window (world coords of the viewport top-left, pixels-per-world-unit) — so **all pan/pinch/clamp/fit code is unchanged**. `applyCam()` derives the real camera each frame: view center `(cx, cz)` from the window, distance `dist = (W/zoom/2) / (tan(fov/2) * aspect)`, position at elevation `ELEV = 0.96 rad (~55°)` south of the target, `lookAt` the center (with a small aim offset so the play viewport, not the full canvas, centers the world). World mapping: 2D `(wx, wy)` → 3D `(x=wx, y=up, z=wy)`.

- `screenToWorld` = `G3D.pick`: raycast through the pixel onto the ground plane `y=0`. Exact by construction — verified 100/100 tile round-trips.
- `worldToScreen` = `G3D.project(wx, wy, 0)`: project + NDC→pixels. Used by coach marks and the overlay.
- `zoomAt` re-anchors by raycasting the same pixel after the zoom change and shifting `cam` by the world delta (perspective-safe).

## 4.3 World build (per map, `buildMap`)

Heights: buildable tile tops `y=0`, road channels sunken to `ROAD_Y=-10`, bridge decks `DECK_Y=+6`, island bottom `-46`, flyers at `+26`. The whole island — tile columns with conditional side walls (lit SE / shadowed NW faces, cliff color at the perimeter), craters, sandbags, rubble, wrecks, dead trees, buildings with emissive windows, dropships, breach tunnels, fortress base walls — is pushed into **one merged vertex-colored BufferGeometry** (Lambert) plus one emissive merged geometry (road dashes, windows, barrel tops) = 2 draw calls for all static world. Pools and breach lava are small separate meshes with pulsing opacity; the reactor core is a pulsing emissive sphere. Per-biome: sky/fog color, hemisphere tint, sun color (warm on ember). One directional sun casts real shadow maps (1024px ortho frustum sized to the world).

## 4.4 Entities & effects

- **Towers**: procedural low-poly groups per type×level×branch (rebuilt when the key changes) — plinth + rotating head (`rotation.y = -t.angle`), barrels/domes/tubes/coils per type, emissive accent tips, muzzle-flash sphere on `t.flash`, recoil offset. Cached shared unit geometries (`GEO.*`) scaled per part.
- **Enemies**: per-shape builders (stalker/raider/brute/wraith/mender/splitter/juggernaut) pooled per type; per-instance cloned body materials so **frost tints** (lerp to ice blue), **hit flashes** (emissive pulse), shield bubbles (transparent sphere, opacity from shield fraction), and the mender's expanding heal ring all animate independently. Position = sim state; ground height lerps between road/deck (smooth bridge ramps); walk wobble from `e.walk`, flyers hover with bank roll.
- **Effects, all pooled, zero allocation per frame**: beams = additive-blended stretched cylinders; explosion rings = ground rings, flashes = additive spheres; orbs/mortar shells = pooled meshes (shells fly a real arc); scorches = dark ground decals; smoke = billboarded sprites (canvas radial texture); sparks + gibs share one 500-point additive `THREE.Points` cloud with per-point color fade.
- **Selection**: range ring (thin additive ring, radius = range — a true ground-plane circle, consistent under perspective), pulsing tile highlight quad, tutorial plot spotlight.

## 4.5 Performance & quality ladder

Adaptive controller (90-frame rolling average): >30ms → shadows off + pixelRatio 1; >45ms → shadow maps disabled + 0.8× render scale. SwiftShader *software* GL settles ~40fps at 360×640 with heavy combat; real GPUs never degrade. Never rebuild geometry in the frame loop — `buildMap` runs only on map change; entity meshes rebuild only on upgrade/branch.

## 4.6 Verification additions (on top of 2.7)

Headless WebGL needs Chromium flags: `--no-sandbox --enable-unsafe-swiftshader`. Verify per change: `THREE.REVISION` loads, `G3D.renderer.getContext()` truthy, 100-tile `project→pick` round-trip exact, `tap` selects the intended tile, all-map no-tower route sims, campaign flow, biome screenshots, settled fps, zero pageerrors, and the 1.10 guards (unchanged, they live in sim/HUD code).

## 4.7 Post-processing pipeline (V3.3)

Real `EffectComposer` stack, inlined from three.js r147 `examples/js/` (legacy global-namespace variants — no ES-module bundling; ~124KB as a second `<script>` after the core): `RenderPass → SSAOPass → UnrealBloomPass → SMAAPass`. Built in `buildComposer(w,h)`, driven each frame via `composer.render()` when `post.on`. This is what recovers (and exceeds) the 2D build's bloom/grade: **UnrealBloom** gives true light-bleed on every emissive, **SSAO** adds crevice/contact shading over the whole scene, **SMAA** removes the low-poly jaggies. `SSAOPass` needs the `SimplexNoise` math addon (also inlined).

**Adaptive ladder (`quality`/`setLevel`)** is degrade-only with a 40-frame window after a 40-frame warmup: L0 full → L1 drop SSAO → L2 drop the whole composer → L3 shadows off → L4 downscale 0.8×; it jumps multiple levels when far over budget. **Normal maps + PBR + shadows stay on at every level** — they're near-free on real GPUs (measured: the no-composer floor equals V3.2). A real 60fps phone (16.7ms) never trips L0; weak devices shed post fast and land on the solid PBR floor. `G3D.setPost(on, ssao)` forces state for screenshots/debug. **Perf note:** on this container's SwiftShader *software* GL the full stack runs ~14fps and the ladder settles to the ~22fps floor; post-processing is GPU-cheap but CPU-rasterization-expensive, so those numbers do not represent real mobile GPUs (which run the full stack at vsync cap).

## 4.8 Surface materials (V3.2–V3.3)

Procedural PMREM **environment map** (`scene.environment`) so PBR metal reflects; tower/hero metal is `MeshStandardMaterial` (`std()`). **Procedural normal maps** (`normalFromHeight` bakes a heightfield canvas then Sobels it into a tangent-space normal map, tangents derived in-shader from derivatives — no explicit tangent attribute): `metalNormal()` (panel seams + rivets) on all metal, `groundNormal()` (grit + hairline cracks) on the terrain, so surfaces show real relief under the sun. Terrain also carries a `grungeTex()` albedo detail map via world-planar UVs (`toMesh(..., uvScale)`), contact-AO discs (`aoDisc`) ground every tower/hero/walker, and tower bases are beveled octagonal mounts.

## 4.9 Known deltas vs the 2D build (accepted)

Cloud-shadow and color-grade overlays are replaced by real lighting + fog + the grade the bloom/tone-map path gives; walk-cycle is bob/wobble + swinging legs rather than fully articulated; the boss juggernaut uses the top boss banner instead of a floating bar. The 2D sprite bake still runs at load purely for HUD tray icons. **The remaining ceiling is FORM, not finish:** enemies/towers are procedural primitive assemblies, so up close they read as clean stylized shapes, not sculpted models. Closing that last gap needs hand-authored GLTF art (a 3D artist / licensed asset packs), which is an art-production task, not an engineering one — see 2.6.

# Part 6 — Active roadmap: V5 "Balance & Challenge" line (`fallengrid-v4.html`)

Approved plan from player playtest feedback (mono Tesla-wall + Glacier-slow trivialises the game; difficulty ceiling too low for skilled play; daily wants to be a real ruleset variation, not a random standard map). Shipped in +0.1 increments. **PROCESS RULE: export the full HTML to the user after every iteration for download + on-device testing before continuing.** Each iteration: Playwright verify + balance harness where relevant + Codex update for new content + commit/push + export.

**Balance harness (tool, built in Iter 1, reused after):** headless auto-player that plays *named strategies* — "mono-Tesla+Glacier wall" (the dominant exploit) vs "diverse overlapping build" — and reports win-rate / core-loss / wave reached per difficulty+map. Tune until mono-strategy is no longer strictly best and diverse builds are rewarded. (The earlier one-off probe under-built and was not representative — the player beats Brutal easily.)

- **Iter 1 — V5.0 Anti-slow & anti-mono balance. ✅ SHIPPED** (see Part 5 V5.0 entry). Decisions locked: brand-new enemy types (not affixes); order kept; line relabelled **V5.0**. Global **slow floor** (an enemy can't be slowed below a min-speed %, so stacking Glacier past 1–2 is wasted → over-investing in slow becomes a disadvantage). New enemy resistances: `slowResist` (0–1); ≥1 **freeze-immune** fast unit (ignores slow → needs raw DPS/coverage); ≥1 **energy-insulated** unit (resists Tesla/energy → forces kinetic/explosive). Cost/strength retune of cryo & tesla + others so overlapping mixed coverage beats single-type spam. Also **cap wave-overlap congestion** (the 146-hostile pile-up) — ties to the health-bar artifact below. Validate mono-vs-diverse with the harness.
**Revised plan (user-approved analysis pass, incl. correction: frost slow must require the SHOT to land — no passive aura around the tower):**

- **Iter 2 — V5.1 Custom difficulty + balance plumbing.** (a) **Frost on-hit rework:** slow is applied only to foes actually splashed by a cryo orb impact; each impact stamps a **per-tower slow source** (1.2 s expiry) on the foe, and sources from distinct towers **stack with the same diminishing-returns formula** toward the soft asymptote — no static field. (b) **Custom difficulty** (new "Custom" button): sliders for start-scrap 75–300, enemy-HP ×0.5–2.0, enemy-speed ×0.5–2.0, tower-damage ×0.5–2.0, earnings ×0.5–2.0, core 5–40; persisted; folded through `diffCfg`/`dmgMul`/reset. (c) **Challenge-score → Alloy coupling** so Custom can't farm meta-progression: `alloyMul = clamp(√((hp·spd)/(dmg·earn)) · (20/core)^0.35 · (150/scrap)^0.2, 0.2, 3)`, shown live in the slider panel. (d) **Anti-snowball damper:** early-call bonus scales down with field congestion (`× max(0, 1 − hostiles/CONGEST_CAP)`) so chain-sending into a strong killbox stops printing scrap. (e) **Consolidated regression suite** (`verify_core.py`) covering route sims, campaign, confirm flow, slow curve, congestion, guards — run every iteration from now on (prevents silent interaction changes like the V5.0.1 Shatter uptime shift, which the on-hit rework also resolves: Shatter's bonus again requires landed frost).
- **Iter 3 — V5.2 Challenge Modes engine + real Daily. ✅ SHIPPED** (see Part 5 V5.2 entry). Data-driven rule-changing modes (extend `activeMod` with `waveGap`/`income`/`allowedTowers`/`core`/`dmg` fields): Sudden Death, Fortress (high scrap, little income), Specialist (1–2 tower types), Glass Cannon, Deep Freeze / No Freeze, etc., each combinable with any map. **Daily shows a briefing screen** describing exactly what the mode changes *before* the run, and the **player picks the difficulty** (incl. Custom). Streak rule: a clear counts on any difficulty; alloy scales with the chosen difficulty via the Iter-2 score. A daily is never a random map played standard.
- **Iter 4 — V5.3 Challenge Lab. ✅ SHIPPED** (see Part 5 V5.3 entry). Menu entry where the **player freely picks mode + map + difficulty** and plays it. Lab runs pay **50% alloy and no streak credit** (not a farm route). Mode descriptions shared with the daily briefing.
- **Iter 5 — V5.4 Wave pacing + onboarding + slow readability. ✅ SHIPPED** (see Part 5 V5.4 entry). Wave 1 waits for manual deploy; refreshed coach-marks (early-send bonus, build-confirm, camo→Tesla/Hero, freeze-immune/energy-resist foes, on-hit frost); frost impact visuals that read as "slow landed"; Codex/stat wording updated to the on-hit + diminishing-stack semantics.
- **Iter 6 — V5.6 Late-game scrap sink + polish. ✅ SHIPPED** (see Part 5 V5.6 entry). Core-repair with rising price (also a comeback mechanic) and/or in-run overclock; run-summary screen incl. tempo stats ("sent X waves early, +Y scrap"); colour-blind-friendly damage-type cues; more SFX variety.

**Backlog (outside the V5 line):** mid-run save/restore (mobile sessions), "expert mode" toggle that skips the build-confirm, second hero.

**Resolved:** the "green meter" artifact was a spawn-gate pile-up of full-HP bars (fixed in V5.0: congestion cap + bars hidden on undamaged foes). Decisions locked: brand-new enemy types; order kept; line relabelled V5.x.
